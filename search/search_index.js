var __index = {"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"index.html","title":"Home","text":"<p>SGP-Tools is a software suite for Sensor Placement and Informative Path Planning.</p> <p>The library includes python code for the following:</p> <ul> <li>Greedy algorithm-based approaches</li> <li>Bayesian optimization-based approaches</li> <li>Genetic algorithm-based approaches</li> <li>Sparse Gaussian process (SGP)-based approaches</li> </ul> <p> </p>"},{"location":"index.html#installation","title":"Installation","text":"<p>The library is available as a <code>pip</code> package. To install the package, run the following command:</p> <pre><code>python3 -m pip install sgptools\n</code></pre> <p>Installation from source:</p> <pre><code>git clone https://github.com/itskalvik/sgp-tools.git\ncd sgp-tools/\npython3 -m pip install -r requirements.txt\npython3 -m pip install -e .\n</code></pre> <p>Note: The requirements.txt file contains packages and their latest versions that were last verified to be working without any issues.</p>"},{"location":"index.html#quick-start","title":"Quick Start","text":"<p>Please refer to the example Jupyter notebooks demonstrating the methods included in the library \ud83d\ude04</p>"},{"location":"index.html#method-summary","title":"Method Summary","text":""},{"location":"index.html#datasets","title":"Datasets","text":"<ul> <li>High-resolution topography data can be downloaded from NOAA Digital Coast</li> <li>High-resolution bathymetry data can be downloaded from NOAA Digital Coast</li> </ul>"},{"location":"index.html#about","title":"About","text":"<p>Please consider citing the following papers if you use SGP-Tools in your academic work \ud83d\ude04</p> <pre><code>@misc{JakkalaA23SP,\nAUTHOR={Kalvik Jakkala and Srinivas Akella},\nTITLE={Efficient Sensor Placement from Regression with Sparse Gaussian Processes in Continuous and Discrete Spaces},\nNOTE= {Preprint},\nYEAR={2023},\nURL={https://itskalvik.github.io/publication/sgp-sp},\n}\n\n@inproceedings{JakkalaA24IPP,\nAUTHOR={Kalvik Jakkala and Srinivas Akella},\nTITLE={Multi-Robot Informative Path Planning from Regression with Sparse Gaussian Processes},\nbooktitle={IEEE International Conference on Robotics and Automation, {ICRA}},\nYEAR={2024},\nPUBLISHER = {{IEEE}},\nURL={https://itskalvik.github.io/publication/sgp-ipp}\n}\n</code></pre>"},{"location":"index.html#acknowledgements","title":"Acknowledgements","text":"<p>This work was funded in part by the UNC Charlotte Office of Research and Economic Development and by NSF under Award Number IIP-1919233.</p>"},{"location":"index.html#license","title":"License","text":"<p>The SGP-Tools software suite is licensed under the terms of the Apache License 2.0. See LICENSE for more information.</p>"},{"location":"blueos.html","title":"BlueOS SGP-Tools","text":""},{"location":"blueos.html#a-blueos-extension-for-autonomous-approximate-bathymetric-surveys","title":"A BlueOS Extension for Autonomous Approximate Bathymetric Surveys","text":""},{"location":"blueos.html#what-does-it-do","title":"What Does It Do?","text":"<p>Autonomous Surface Vehicles (ASVs), such as the BlueRobotics BlueBoat, are well-suited for bathymetric surveys. However, it is often the case that an exhaustive survey mapping the depth at every location in an area is infeasible or unnecessary. In such cases, we can leverage variations in the underwater terrain to determine a few critical locations for data collection, which would result in a good approximation of the area's bathymetry.</p> <p>The SGP-Tools python library provides path planners to address the above problem, known as the informative path planning problem. The BlueOS SGP-Tools extension uses this library to determine ideal locations for the ASV to collect data and controls the ASV to autonomously visit the selected locations.</p> <p>The following shows our path planner adaptively planning a path for an aerial drone with a downward-facing camera tasked with surveying a given area:</p> <p>The following shows the underwater terrain estimated using data collected by our package running on an autonomous surface vehicle equipped with the Ping2 sonar:</p>"},{"location":"blueos.html#usage","title":"Usage","text":""},{"location":"blueos.html#starting-a-mission","title":"Starting a Mission","text":"<ol> <li> <p>First, we need to define the survey area and the robot launch location. The extension can read this data from QGC plan files. The survey area and launch position must be defined using a polygon-shaped geofence drawn in QGC and saved as <code>mission.plan</code>.</p> <p> </p> </li> <li> <p>Once you have the plan file, copy it to the robot using the <code>File Browser</code> feature in BlueOS's <code>Pirate Mode</code>. The <code>mission.plan</code> file should be uploaded to the following directory: <code>/extensions/sgptools/</code></p> <p>Once the <code>mission.plan</code> file is uploaded, restart the extension to ensure that the new file is used for the mission.</p> <p> </p> </li> <li> <p>Finally, use the terminal provided by the SGP-Tools extension to start the mission with the following command:     <pre><code>ros2 launch ros_sgp_tools single_robot.launch.py\n</code></pre></p> <p> </p> </li> </ol>"},{"location":"blueos.html#viewing-the-data-after-a-mission","title":"Viewing the Data after a Mission","text":"<p>The sensor data, along with the corresponding GPS coordinates, will be logged to an HDF5 file in the <code>DATA_FOLDER</code>, where the <code>mission.plan</code> was uploaded. </p> <p>We can estimate the bathymetry of the entire survey area using the collected data and visualize a normalized version with the following command:</p> <p>\u26a0\ufe0f Do not run this during the mission, as it will disrupt the path planner <pre><code>ros2 launch ros_sgp_tools visualize_data.launch.py\n</code></pre></p> <p>The above command will publish a point cloud that can be viewed using foxglove. You can access it from a web browser at https://app.foxglove.dev/. Use the <code>open connection</code> feature and change the address from <code>localhost</code> to the IP address of the ASV.</p> <p>You can control the point cloud density using the <code>num_samples</code> parameter. You can set this from foxglove's <code>Parameters</code> panel.</p> <p>By default, the latest mission log will be visualized. You can visualize a specific mission log using the following command (replace <code>&lt;log folder name&gt;</code> with  the log folder name):</p> <pre><code>ros2 launch ros_sgp_tools visualize_data.launch.py mission_log:=&lt;log folder name&gt;\n</code></pre>"},{"location":"blueos.html#parameters","title":"Parameters","text":"<p>You can control the following extension parameters by running the following command in the terminal provided by the SGP-Tools extension:</p> <pre><code>export &lt;parameter_name&gt;=&lt;parameter_value&gt;\n</code></pre> <p>The parameters reset to their default values after rebooting. They can be made permanent by configuring the parameters using the app environment variables on the BlueOS extensions page in pirate mode.</p>"},{"location":"blueos.html#available-parameters","title":"Available Parameters:","text":"<ul> <li> <p><code>PING2_PORT</code> (<code>default: /dev/ttyUSB0</code>):</p> <ul> <li>Specifies the device to which the Ping2 sonar is mounted. You can get the device port from the <code>Ping Sonar Devices</code> page in BlueOS.</li> </ul> </li> <li> <p><code>NUM_WAYPOINTS</code> (<code>default: 20</code>):</p> <ul> <li>The number of waypoints optimized by the path planner.</li> <li>Increasing the number of waypoints results in a more complex path that covers a larger area.</li> <li>Recommend increasing only when the default setting results in a poor reconstruction of the environment, as this increases the computational cost and leads to slower online path updates.</li> </ul> </li> <li> <p><code>SAMPLING_RATE</code> (<code>default: 2</code>): </p> <ul> <li>The number of points to sample along each edge of the path during path planning. The default value of <code>2</code>  means that only the vertices of the path are used.</li> <li>The path planner assumes the data is collected only at the sampled points. Increasing the sampling rate allows the planner to better approximate the information along the entire path, thereby resulting in more informative paths.</li> <li>Recommend increasing only when the default setting results in a poor reconstruction of the environment, as this increases the computational cost and leads to slower online path updates.</li> </ul> </li> <li> <p><code>DATA_BUFFER_SIZE</code> (<code>default: 100</code>):</p> <ul> <li>The number of sensor data samples to collect before using the data to update the model parameters, which, in turn, will be used to update future waypoints.</li> <li>Increasing the buffer size will allow the planner to compute better parameter estimates, which will result in more informative paths.</li> <li>Recommend increasing only when the default setting results in a poor reconstruction of the environment, as this increases the computational cost and leads to slower online path updates.</li> </ul> </li> <li> <p><code>TRAIN_PARAM_INDUCING</code> (<code>default: False</code>):</p> <ul> <li>Enables training the inducing points of the parameter model (sparse Gaussian process) in addition to the kernel parameters during the path update.</li> <li>Enabling this feature will result in more accurate parameter estimates and more informative paths.</li> <li>Recommend increasing only when the default setting results in a poor reconstruction of the environment, as this increases the computational cost and leads to slower online path updates.</li> </ul> </li> <li> <p><code>NUM_PARAM_INDUCING</code> (<code>default: 40</code>):</p> <ul> <li>The number of inducing points used in the parameter model (sparse Gaussian process).</li> <li>Increasing the number of inducing points will result in more accurate parameter estimates and more informative paths.</li> <li>Recommend enabling only when the default setting results in a poor reconstruction of the environment, as this increases the computational cost and leads to slower online path updates.</li> </ul> </li> <li> <p><code>START_FOXGLOVE</code> (<code>default: False</code>): </p> <ul> <li>Enables foxglove, a web-based data visualization platform similar to RViz.</li> <li>Currently it does not publish any new information that is not available through Cockpit or QGC.</li> </ul> </li> <li> <p><code>ADAPTIVE_IPP</code> (<code>default: True</code>):</p> <ul> <li>Enables adaptive informative path planning.</li> <li>When enabled, it uses the data streaming from the sonar to learn the correlations in the underwater bathymetry and further optimizes the future waypoints to collect even more informative data.</li> <li>Recommend disabling it if the onboard sonar is currently unsuppored by this package. </li> </ul> </li> <li> <p><code>NAMESPACE</code> (<code>default: robot_0</code>): </p> <ul> <li>ROS2 namespace, useful when multiple ROS2 robots are operating on the same network.</li> <li>Currently, only the single robot planner is fully supported.</li> </ul> </li> <li> <p><code>DATA_TYPE</code> (<code>default: Ping2</code>): </p> <ul> <li>Type of sensor to be used by the path planner. </li> <li>Currently, only the BlueRobotics Ping Sonar is supported.</li> </ul> </li> <li> <p><code>FAKE_DATA</code> (<code>default: False</code>):</p> <ul> <li>Enables a fake sensor data publisher used only for testing. Requires <code>DATA_TYPE=SerialPing2</code>.</li> </ul> </li> </ul>"},{"location":"blueos.html#hardware-configuration","title":"Hardware Configuration","text":"<ul> <li> <p>This extension works only on 64-bit operating systems. You can install the latest version of BlueOS on 64-bit Raspberry Pi OS Lite by running the following command on the Pi (ensure the username is set to <code>pi</code>):     <pre><code>sudo su -c 'curl -fsSL https://raw.githubusercontent.com/bluerobotics/blueos-docker/master/install/install.sh | bash'\n</code></pre></p> </li> <li> <p>The extension requires over 4GB of memory+swap. Please ensure that the swap size is large enough to accommodate the extension. The extension will copy the shell script <code>config_swap.sh</code> to <code>/usr/blueos/extensions/sgptools/</code> folder on the underlying device. You can use this script to increase the swap size before starting the path planner. </p> <p>You will have to use <code>Pirate Mode</code> to access BlueOS's built-in terminal and run the script on the underlying device via the <code>red-pill</code> utility. Use the following commands to enable <code>red-pill</code> and increase the swap size:  <pre><code>red-pill\nsudo bash /usr/blueos/extensions/sgptools/config_swap.sh\n</code></pre></p> <p> </p> </li> </ul>"},{"location":"blueos.html#disclaimer","title":"Disclaimer \u26a0\ufe0f","text":"<p>This extension, when executed properly, will take control of the ASV and could potentially collide the vehicle with obstacles in the environment. Please use it with caution.</p>"},{"location":"docker.html","title":"docker-sgp-tools","text":"<p>The docker-sgp-tools package provides the following docker-compose scripts:</p> <ul> <li><code>robot-compose.yml</code>: A minimal docker container used to run SGP-Tools ROS2 package on ArduPilot-based robots.</li> <li><code>sitl-compose.yml</code>: A GUI-based docker container with ROS2, Gazebo, ArduPilot SITL, and SGP-Tools used for simulating ArduPilot vehicles and testing SGP-Tools IPP code. </li> </ul>"},{"location":"docker.html#prerequisites","title":"Prerequisites","text":"<ul> <li>docker</li> <li>docker-compose</li> <li>(Optional) WSLg</li> </ul>"},{"location":"docker.html#getting-started","title":"Getting Started","text":""},{"location":"docker.html#starting-the-containers","title":"Starting the Containers","text":"<p>Run the following commands to start the SITL docker container:</p> <pre><code>git clone --recurse-submodules https://github.com/itskalvik/docker-sgp-tools.git\ncd docker-sgp-tools\ndocker-compose -f sitl-compose.yml pull\ndocker-compose -f sitl-compose.yml up -d\ndocker-compose -f sitl-compose.yml exec sgptools bash\n</code></pre> <p>Use <code>robot-compose.yml</code> to run the minimal docker container. </p>"},{"location":"docker.html#running-sgp-tools-onlineadaptive-ipp-with-gazeboardurover-simulator","title":"Running SGP-Tools Online/Adaptive IPP with Gazebo/ArduRover Simulator","text":"<p>Use <code>docker-compose -f sitl-compose.yml exec sgptools bash</code> to get a new terminal. Run the following commands in separate terminals in the docker container:</p> <ul> <li> <p>Launch Gazebo with the AION R1 UGV:     <pre><code>gz sim -v4 -r r1_rover_runway.sdf\n</code></pre>     To simulate a BlueBoat refer to this documentation. Note that the container already has Wave Sim installed in it.</p> </li> <li> <p>Launch ArduRover SITL:     <pre><code>sim_vehicle.py -v Rover -f rover-skid --model JSON --add-param-file=$HOME/SITL_Models/Gazebo/config/r1_rover.param --console --map -N -l 35.30371178789218,-80.73099267294185,0.,0.\n</code></pre>     Note: Restart sim_vechile.py if you get the following message: <code>paramftp: bad count 1294 should be 1284</code></p> </li> <li> <p>Launch the SGP-Tools Online/Adaptive IPP method:     <pre><code>ros2 launch ros_sgp_tools single_robot.launch.py\n</code></pre></p> </li> </ul>"},{"location":"docker.html#building-the-docker-containers-from-scratch","title":"Building the Docker Containers from Scratch","text":"<p>First, setup buildx to build the containers for both arm64 and amd64 platforms:  <pre><code>docker buildx create --name multi-arch \\\n  --platform \"linux/arm64,linux/amd64\" \\\n  --driver \"docker-container\"\ndocker buildx use multi-arch\n</code></pre></p> <p>Next, clone the repo and build the container.  <pre><code>git clone --recurse-submodules https://github.com/itskalvik/docker-sgp-tools.git\ncd docker-sgp-tools\ndocker-compose -f sitl-compose.yml build \n</code></pre></p> <p>Use <code>robot-compose.yml</code> to build the minimal docker container.</p>"},{"location":"docker.html#other-commands","title":"Other commands","text":"<ul> <li> <p>The docker-compose down command stops and removes containers, networks, volumes, and images, making it suitable for completely clearing all resources deployed by an application.</p> <pre><code>docker-compose -f sitl-compose.yml down\n</code></pre> </li> <li> <p>The docker-compose stop command just pauses running containers without removing them, which is ideal for temporary halts.</p> <pre><code>docker-compose -f sitl-compose.yml stop\n</code></pre> </li> </ul>"},{"location":"docker.html#references","title":"References","text":"<p>Based on Dockerfiles from the following repos:</p> <ul> <li>https://github.com/Robotic-Decision-Making-Lab/blue</li> <li>https://github.com/ryomo/ros2-gazebo-docker</li> </ul>"},{"location":"ros2.html","title":"ros_sgp_tools","text":"<p>The ros_sgp_tools package provides a ROS2 companion package for the SGP-Tools python library that can be deployed on ArduPilot-based vehicles. </p> <ul> <li>The package can be used to run online/adaptive IPP on ArduPilot based UGVs and ASVs. </li> <li>The package can also be used with Gazebo/Ardupilot SITL.</li> <li>To use our Docker container with the preconfigured development environment, please refer to the documentation here. </li> </ul>"},{"location":"ros2.html#running-sgp-tools-onlineadaptive-ipp-with-gazeboardurover-simulator","title":"Running SGP-Tools Online/Adaptive IPP with Gazebo/ArduRover Simulator","text":"<p>Run the following commands in separate terminals:</p> <ul> <li> <p>Launch Gazebo with the AION R1 UGV:     <pre><code>gz sim -v4 -r r1_rover_runway.sdf\n</code></pre>     To simulate a BlueBoat refer to this documentation.</p> </li> <li> <p>Launch ArduRover SITL:     <pre><code>sim_vehicle.py -v Rover -f rover-skid --model JSON --add-param-file=$HOME/SITL_Models/Gazebo/config/r1_rover.param --console --map -N -l 35.30371178789218,-80.73099267294185,0.,0.\n</code></pre>     Note: </p> <ul> <li>Restart sim_vechile.py if you get the following message: <code>paramftp: bad count</code></li> <li>Ensure the MAV Console shows that the vehicle has a GPS lock before running the next command</li> <li>Ensure the MAV Map shows the vehicle before running the next command</li> </ul> </li> <li> <p>Launch the SGP-Tools Online/Adaptive IPP method:     <pre><code>ros2 launch ros_sgp_tools single_robot.launch.py\n</code></pre></p> </li> </ul>"},{"location":"ros2.html#environment-setup","title":"Environment setup","text":"<ul> <li>To use our Docker container with the preconfigured development environment, please refer to the documentation here. </li> </ul> <p>Alternatively, please follow the following instructions to configure the development envirnoment on your local machine. </p> <ul> <li>Install ROS 2 Humble <pre><code>sudo apt install ros-humble-desktop\necho \"source /opt/ros/humble/setup.bash\" &gt;&gt; ~/.bashrc\nsource ~/.bashrc\n</code></pre></li> <li>Create a ROS 2 workspace   <pre><code>mkdir -p ~/ros2_ws/src\ncd ~/ros2_ws/\ncolcon build --symlink-install\necho \"source $HOME/ros2_ws/install/setup.bash\" &gt;&gt; ~/.bashrc\nsource ~/.bashrc\n</code></pre></li> <li>Install Gazebo Garden</li> <li>Install ArduPilot SITL</li> <li>Install ardupilot_gazebo</li> <li>Get SITL_Models repo</li> <li>Setup environment variables</li> </ul> <pre><code>echo \"export GZ_VERSION=garden\" &gt;&gt; ~/.bashrc\necho \"export GZ_SIM_SYSTEM_PLUGIN_PATH=$HOME/ardupilot_gazebo/build:${GZ_SIM_SYSTEM_PLUGIN_PATH}\" &gt;&gt; ~/.bashrc\necho \"export GZ_SIM_RESOURCE_PATH=$HOME/ardupilot_gazebo/models:$HOME/ardupilot_gazebo/worlds:$HOME/SITL_Models/Gazebo/models:$HOME/SITL_Models/Gazebo/worlds:$GZ_SIM_RESOURCE_PATH\" &gt;&gt; ~/.bashrc\n</code></pre>"},{"location":"ros2.html#package-setup","title":"Package setup","text":"<ul> <li>Install apt dependencies    <pre><code>sudo apt install ros-humble-mavros* ros-$ROS_DISTRO-cv-bridge\n</code></pre></li> <li>Install pip dependencies    <pre><code>pip3 install -r requirements.txt\n</code></pre></li> <li>Clone repo and build package   <pre><code>cd ~/ros2_ws/src\ngit clone https://github.com/itskalvik/ros_sgp_tools.git\ncd ..\ncolcon build --symlink-install\n</code></pre></li> </ul>"},{"location":"api/kernels/index.html","title":"","text":""},{"location":"api/kernels/index.html#sgptools.kernels","title":"<code>sgptools.kernels</code>","text":"<p>Special kernel functions in this package:</p> <ul> <li><code>neural_kernel</code>: Provides a neural spectral kernel function that uses a mixture of multilayer perceptrons</li> </ul>"},{"location":"api/kernels/Neural-Kernel.html","title":"Neural Kernel","text":""},{"location":"api/kernels/Neural-Kernel.html#sgptools.kernels.neural_kernel","title":"<code>sgptools.kernels.neural_kernel</code>","text":"<p>Provides a neural spectral kernel function along with an initialization function</p>"},{"location":"api/kernels/Neural-Kernel.html#sgptools.kernels.neural_kernel.NeuralSpectralKernel","title":"<code>NeuralSpectralKernel</code>","text":"<p>               Bases: <code>Kernel</code></p> <p>Neural Spectral Kernel function (non-stationary kernel function).  Based on the implementation from the following repo</p> Refer to the following papers for more details <ul> <li>Neural Non-Stationary Spectral Kernel [Remes et al., 2018]</li> </ul> <p>Parameters:</p> Name Type Description Default <code>input_dim</code> <code>int</code> <p>Number of data dimensions</p> required <code>active_dims</code> <code>int</code> <p>Number of data dimensions that are used for computing the covariances</p> <code>None</code> <code>Q</code> <code>int</code> <p>Number of MLP mixture components used in the kernel function</p> <code>1</code> <code>hidden_sizes</code> <code>list</code> <p>Number of hidden units in each MLP layer. Length of the list determines the number of layers.</p> <code>[32, 32]</code> Source code in <code>sgptools/kernels/neural_kernel.py</code> <pre><code>class NeuralSpectralKernel(gpflow.kernels.Kernel):\n    \"\"\"Neural Spectral Kernel function (non-stationary kernel function). \n    Based on the implementation from the following [repo](https://github.com/sremes/nssm-gp/tree/master?tab=readme-ov-file)\n\n    Refer to the following papers for more details:\n        - Neural Non-Stationary Spectral Kernel [Remes et al., 2018]\n\n    Args:\n        input_dim (int): Number of data dimensions\n        active_dims (int): Number of data dimensions that are used for computing the covariances\n        Q (int): Number of MLP mixture components used in the kernel function\n        hidden_sizes (list): Number of hidden units in each MLP layer. Length of the list determines the number of layers.\n    \"\"\"\n    def __init__(self, input_dim, active_dims=None, Q=1, hidden_sizes=[32, 32]):\n        super().__init__(active_dims=active_dims)\n\n        self.input_dim = input_dim\n        self.Q = Q\n        self.num_hidden = len(hidden_sizes)\n\n        self.freq = []\n        self.length = []\n        self.var = []\n        for q in range(self.Q):\n            freq = keras.Sequential([layers.Dense(hidden_sizes[i], activation='selu') for i in range(self.num_hidden)] + \n                                    [layers.Dense(input_dim, activation='softplus')])\n            length = keras.Sequential([layers.Dense(hidden_sizes[i], activation='selu') for i in range(self.num_hidden)] +\n                                   [layers.Dense(input_dim, activation='softplus')])\n            var = keras.Sequential([layers.Dense(hidden_sizes[i], activation='selu') for i in range(self.num_hidden)] +\n                                   [layers.Dense(1, activation='softplus')])\n            self.freq.append(freq)\n            self.length.append(length)\n            self.var.append(var)\n\n    def K(self, X, X2=None):\n        \"\"\"Computes the covariances between/amongst the input variables\n\n        Args:\n            X (ndarray): Variables to compute the covariance matrix\n            X2 (ndarray): If passed, the covariance between X and X2 is computed. Otherwise, \n                          the covariance between X and X is computed.\n\n        Returns:\n            cov (ndarray): covariance matrix\n        \"\"\"\n        if X2 is None:\n            X2 = X\n            equal = True\n        else:\n            equal = False\n\n        kern = 0.0\n        for q in range(self.Q):\n            # compute latent function values by the neural network\n            freq, freq2 = self.freq[q](X), self.freq[q](X2)\n            lens, lens2 = self.length[q](X), self.length[q](X2)\n            var, var2 = self.var[q](X), self.var[q](X2)\n\n            # compute length-scale term\n            Xr = tf.expand_dims(X, 1)  # N1 1 D\n            X2r = tf.expand_dims(X2, 0)  # 1 N2 D\n            l1 = tf.expand_dims(lens, 1)  # N1 1 D\n            l2 = tf.expand_dims(lens2, 0)  # 1 N2 D\n            L = tf.square(l1) + tf.square(l2)  # N1 N2 D\n            #D = tf.square((Xr - X2r) / L)  # N1 N2 D\n            D = tf.square(Xr - X2r) / L  # N1 N2 D\n            D = tf.reduce_sum(D, 2)  # N1 N2\n            det = tf.sqrt(2 * l1 * l2 / L)  # N1 N2 D\n            det = tf.reduce_prod(det, 2)  # N1 N2\n            E = det * tf.exp(-D)  # N1 N2\n\n            # compute cosine term\n            muX = (tf.reduce_sum(freq * X, 1, keepdims=True)\n                   - tf.transpose(tf.reduce_sum(freq2 * X2, 1, keepdims=True)))\n            COS = tf.cos(2 * np.pi * muX)\n\n            # compute kernel variance term\n            WW = tf.matmul(var, var2, transpose_b=True)  # w*w'^T\n\n            # compute the q'th kernel component\n            kern += WW * E * COS\n        if equal:\n            return robust_kernel(kern, tf.shape(X)[0])\n        else:\n            return kern\n\n    def K_diag(self, X):\n        kd = default_jitter()\n        for q in range(self.Q):\n            kd += tf.square(self.var[q](X))\n        return tf.squeeze(kd)\n</code></pre>"},{"location":"api/kernels/Neural-Kernel.html#sgptools.kernels.neural_kernel.NeuralSpectralKernel.K","title":"<code>K(X, X2=None)</code>","text":"<p>Computes the covariances between/amongst the input variables</p> <p>Parameters:</p> Name Type Description Default <code>X</code> <code>ndarray</code> <p>Variables to compute the covariance matrix</p> required <code>X2</code> <code>ndarray</code> <p>If passed, the covariance between X and X2 is computed. Otherwise,            the covariance between X and X is computed.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>cov</code> <code>ndarray</code> <p>covariance matrix</p> Source code in <code>sgptools/kernels/neural_kernel.py</code> <pre><code>def K(self, X, X2=None):\n    \"\"\"Computes the covariances between/amongst the input variables\n\n    Args:\n        X (ndarray): Variables to compute the covariance matrix\n        X2 (ndarray): If passed, the covariance between X and X2 is computed. Otherwise, \n                      the covariance between X and X is computed.\n\n    Returns:\n        cov (ndarray): covariance matrix\n    \"\"\"\n    if X2 is None:\n        X2 = X\n        equal = True\n    else:\n        equal = False\n\n    kern = 0.0\n    for q in range(self.Q):\n        # compute latent function values by the neural network\n        freq, freq2 = self.freq[q](X), self.freq[q](X2)\n        lens, lens2 = self.length[q](X), self.length[q](X2)\n        var, var2 = self.var[q](X), self.var[q](X2)\n\n        # compute length-scale term\n        Xr = tf.expand_dims(X, 1)  # N1 1 D\n        X2r = tf.expand_dims(X2, 0)  # 1 N2 D\n        l1 = tf.expand_dims(lens, 1)  # N1 1 D\n        l2 = tf.expand_dims(lens2, 0)  # 1 N2 D\n        L = tf.square(l1) + tf.square(l2)  # N1 N2 D\n        #D = tf.square((Xr - X2r) / L)  # N1 N2 D\n        D = tf.square(Xr - X2r) / L  # N1 N2 D\n        D = tf.reduce_sum(D, 2)  # N1 N2\n        det = tf.sqrt(2 * l1 * l2 / L)  # N1 N2 D\n        det = tf.reduce_prod(det, 2)  # N1 N2\n        E = det * tf.exp(-D)  # N1 N2\n\n        # compute cosine term\n        muX = (tf.reduce_sum(freq * X, 1, keepdims=True)\n               - tf.transpose(tf.reduce_sum(freq2 * X2, 1, keepdims=True)))\n        COS = tf.cos(2 * np.pi * muX)\n\n        # compute kernel variance term\n        WW = tf.matmul(var, var2, transpose_b=True)  # w*w'^T\n\n        # compute the q'th kernel component\n        kern += WW * E * COS\n    if equal:\n        return robust_kernel(kern, tf.shape(X)[0])\n    else:\n        return kern\n</code></pre>"},{"location":"api/kernels/Neural-Kernel.html#sgptools.kernels.neural_kernel.init_neural_kernel","title":"<code>init_neural_kernel(x, y, inducing_variable, Q, n_inits=1, hidden_sizes=None)</code>","text":"<p>Helper function to initialize a Neural Spectral Kernel function (non-stationary kernel function).  Based on the implementation from the following repo</p> Refer to the following papers for more details <ul> <li>Neural Non-Stationary Spectral Kernel [Remes et al., 2018]</li> </ul> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>ndarray</code> <p>(n, d); Input training set points</p> required <code>y</code> <code>ndarray</code> <p>(n, 1); Training set labels</p> required <code>inducing_variable</code> <code>ndarray</code> <p>(m, d); Initial inducing points</p> required <code>Q</code> <code>int</code> <p>Number of MLP mixture components used in the kernel function</p> required <code>n_inits</code> <code>int</code> <p>Number of times to initalize the kernel function (returns the best model)</p> <code>1</code> <code>hidden_sizes</code> <code>list</code> <p>Number of hidden units in each MLP layer. Length of the list determines the number of layers.</p> <code>None</code> Source code in <code>sgptools/kernels/neural_kernel.py</code> <pre><code>def init_neural_kernel(x, y, inducing_variable, Q, n_inits=1, hidden_sizes=None):\n    \"\"\"Helper function to initialize a Neural Spectral Kernel function (non-stationary kernel function). \n    Based on the implementation from the following [repo](https://github.com/sremes/nssm-gp/tree/master?tab=readme-ov-file)\n\n    Refer to the following papers for more details:\n        - Neural Non-Stationary Spectral Kernel [Remes et al., 2018]\n\n    Args:\n        x (ndarray): (n, d); Input training set points\n        y (ndarray): (n, 1); Training set labels\n        inducing_variable (ndarray): (m, d); Initial inducing points\n        Q (int): Number of MLP mixture components used in the kernel function\n        n_inits (int): Number of times to initalize the kernel function (returns the best model)\n        hidden_sizes (list): Number of hidden units in each MLP layer. Length of the list determines the number of layers.\n    \"\"\"\n    x, y = data_input_to_tensor((x, y))\n\n    print('Initializing neural spectral kernel...')\n    best_loglik = -np.inf\n    best_m = None\n    N, input_dim = x.shape\n\n    for k in range(n_inits):\n        # gpflow.reset_default_graph_and_session()\n        k = NeuralSpectralKernel(input_dim=input_dim, Q=Q, \n                                    hidden_sizes=hidden_sizes)\n        model = SGPR((x, y), inducing_variable=inducing_variable, \n                        kernel=k)\n        loglik = model.elbo()\n        if loglik &gt; best_loglik:\n            best_loglik = loglik\n            best_m = model\n        del model\n        gc.collect()\n    print('Best init: %f' % best_loglik)\n\n    return best_m\n</code></pre>"},{"location":"api/models/index.html","title":"","text":""},{"location":"api/models/index.html#sgptools.models","title":"<code>sgptools.models</code>","text":"<p>Sensor placement and informative path planning methods in this package:</p> <ul> <li><code>bo</code>: Provides a Bayesian optimization based approach that maximizes mutual-information to get sensor placements</li> <li><code>cma_es</code>: Provides a genetic algorithm (CMA-ES) based approach that maximizes mutual-information to get sensor placements</li> <li><code>continuous_sgp</code>: Provides an SGP-based sensor placement approach that is optimized using gradient descent</li> <li><code>greedy_mi</code>: Provides a greedy algorithm based approach that maximizes mutual-information to get sensor placements</li> <li><code>greedy_sgp</code>: Provides an SGP-based sensor placement approach that is optimized using a greedy algorithm</li> </ul>"},{"location":"api/models/Bayesian-Optimization.html","title":"Bayesian Optimization","text":""},{"location":"api/models/Bayesian-Optimization.html#sgptools.models.bo","title":"<code>sgptools.models.bo</code>","text":""},{"location":"api/models/Bayesian-Optimization.html#sgptools.models.bo.BayesianOpt","title":"<code>BayesianOpt</code>","text":"<p>Class for optimizing sensor placements using Bayesian Optimization</p> Refer to the following papers for more details <ul> <li>UAV route planning for active disease classification [Vivaldini et al., 2019]</li> <li>Occupancy map building through Bayesian exploration [Francis et al., 2019]</li> </ul> <p>Parameters:</p> Name Type Description Default <code>X_train</code> <code>ndarray</code> <p>(n, d); Locations in the environment used to approximate the monitoring regions</p> required <code>noise_variance</code> <code>float</code> <p>data variance</p> required <code>kernel</code> <code>Kernel</code> <p>gpflow kernel function</p> required <code>transform</code> <code>Transform</code> <p>Transform object</p> <code>None</code> Source code in <code>sgptools/models/bo.py</code> <pre><code>class BayesianOpt:\n    \"\"\"Class for optimizing sensor placements using Bayesian Optimization\n\n    Refer to the following papers for more details:\n        - UAV route planning for active disease classification [Vivaldini et al., 2019]\n        - Occupancy map building through Bayesian exploration [Francis et al., 2019]\n\n    Args:\n        X_train (ndarray): (n, d); Locations in the environment used to approximate the monitoring regions\n        noise_variance (float): data variance\n        kernel (gpflow.kernels.Kernel): gpflow kernel function\n        transform (Transform): Transform object\n    \"\"\"\n    def __init__(self, X_train, noise_variance, kernel,\n                 transform=None):\n        self.X_train = X_train\n        self.noise_variance = noise_variance\n        self.kernel = kernel\n        self.num_dim = X_train.shape[-1]\n        self.transform = transform\n\n        # use the boundaries of the region as the search space\n        self.pbounds_dim = []\n        for i in range(self.num_dim):\n            self.pbounds_dim.append((np.min(X_train[:, i]), np.max(X_train[:, i])))\n\n    def objective(self, **kwargs):\n        \"\"\"Objective function (GP-based Mutual Information)\n\n        Args:\n            x&lt;i&gt; (ndarray): (1, d); Current solution sensor placement location i\n        \"\"\"\n        # MI does not depend on waypoint order (reshape to -1, num_dim)\n        X = []\n        for i in range(len(kwargs)):\n            X.append(kwargs['x{}'.format(i)])\n        X = np.array(X).reshape(-1, self.num_dim)\n        if self.transform is not None:\n            X = self.transform.expand(X)\n            constraints_loss = self.transform.constraints(X)\n\n        try:\n            mi = get_mi(X, self.X_train, self.noise_variance, self.kernel)\n            mi += constraints_loss\n            mi = mi.numpy()\n        except:\n            mi = -1e4 # if the cholskey decomposition fails\n        return mi\n\n    def optimize(self, \n                 num_sensors=10, \n                 max_steps=100,  \n                 X_init=None,\n                 init_points=10,\n                 verbose=0, \n                 seed=1234):\n        \"\"\"Optimizes the sensor placements using Bayesian Optimization without any constraints\n\n        Args:\n            num_sensors (int): Number of sensor locations to optimize.\n            max_steps (int): Maximum number of optimization steps. \n            X_init (ndarray): (m, d); Initial inducing points.\n            init_points (int): Number of random solutions used for initial exploration. \n                               Random exploration can help by diversifying the exploration space. \n            verbose (int): The level of verbosity.\n            seed (int): The algorithm will use it to seed the randomnumber generator, ensuring replicability.\n\n        Returns:\n            Xu (ndarray): (m, d); Solution sensor placement locations\n        \"\"\"\n        if X_init is None:\n            X_init = get_inducing_pts(self.X_train, num_sensors, random=True)\n        else:\n            num_sensors = len(X_init.reshape(-1, self.num_dim))\n        X_init = X_init.reshape(-1)\n\n        pbounds = {}\n        for i in range(self.num_dim*num_sensors):\n            pbounds['x{}'.format(i)] = self.pbounds_dim[i%self.num_dim]\n\n        optimizer = BayesianOptimization(f=self.objective,\n                                         pbounds=pbounds,\n                                         verbose=verbose,\n                                         random_state=seed,\n                                         allow_duplicate_points=True)\n        optimizer.maximize(init_points=init_points,\n                           n_iter=max_steps)\n\n        sol = []\n        for i in range(self.num_dim*num_sensors):\n            sol.append(optimizer.max['params']['x{}'.format(i)])\n        sol = np.array(sol).reshape(-1, self.num_dim)\n        if self.transform is not None:\n            sol = self.transform.expand(sol,\n                                        expand_sensor_model=False)\n            if not isinstance(sol, np.ndarray):\n                sol = sol.numpy()\n        return sol.reshape(-1, self.num_dim)\n</code></pre>"},{"location":"api/models/Bayesian-Optimization.html#sgptools.models.bo.BayesianOpt.objective","title":"<code>objective(**kwargs)</code>","text":"<p>Objective function (GP-based Mutual Information)</p> <p>Parameters:</p> Name Type Description Default <code>x&lt;i&gt;</code> <code>ndarray</code> <p>(1, d); Current solution sensor placement location i</p> required Source code in <code>sgptools/models/bo.py</code> <pre><code>def objective(self, **kwargs):\n    \"\"\"Objective function (GP-based Mutual Information)\n\n    Args:\n        x&lt;i&gt; (ndarray): (1, d); Current solution sensor placement location i\n    \"\"\"\n    # MI does not depend on waypoint order (reshape to -1, num_dim)\n    X = []\n    for i in range(len(kwargs)):\n        X.append(kwargs['x{}'.format(i)])\n    X = np.array(X).reshape(-1, self.num_dim)\n    if self.transform is not None:\n        X = self.transform.expand(X)\n        constraints_loss = self.transform.constraints(X)\n\n    try:\n        mi = get_mi(X, self.X_train, self.noise_variance, self.kernel)\n        mi += constraints_loss\n        mi = mi.numpy()\n    except:\n        mi = -1e4 # if the cholskey decomposition fails\n    return mi\n</code></pre>"},{"location":"api/models/Bayesian-Optimization.html#sgptools.models.bo.BayesianOpt.optimize","title":"<code>optimize(num_sensors=10, max_steps=100, X_init=None, init_points=10, verbose=0, seed=1234)</code>","text":"<p>Optimizes the sensor placements using Bayesian Optimization without any constraints</p> <p>Parameters:</p> Name Type Description Default <code>num_sensors</code> <code>int</code> <p>Number of sensor locations to optimize.</p> <code>10</code> <code>max_steps</code> <code>int</code> <p>Maximum number of optimization steps. </p> <code>100</code> <code>X_init</code> <code>ndarray</code> <p>(m, d); Initial inducing points.</p> <code>None</code> <code>init_points</code> <code>int</code> <p>Number of random solutions used for initial exploration.                 Random exploration can help by diversifying the exploration space. </p> <code>10</code> <code>verbose</code> <code>int</code> <p>The level of verbosity.</p> <code>0</code> <code>seed</code> <code>int</code> <p>The algorithm will use it to seed the randomnumber generator, ensuring replicability.</p> <code>1234</code> <p>Returns:</p> Name Type Description <code>Xu</code> <code>ndarray</code> <p>(m, d); Solution sensor placement locations</p> Source code in <code>sgptools/models/bo.py</code> <pre><code>def optimize(self, \n             num_sensors=10, \n             max_steps=100,  \n             X_init=None,\n             init_points=10,\n             verbose=0, \n             seed=1234):\n    \"\"\"Optimizes the sensor placements using Bayesian Optimization without any constraints\n\n    Args:\n        num_sensors (int): Number of sensor locations to optimize.\n        max_steps (int): Maximum number of optimization steps. \n        X_init (ndarray): (m, d); Initial inducing points.\n        init_points (int): Number of random solutions used for initial exploration. \n                           Random exploration can help by diversifying the exploration space. \n        verbose (int): The level of verbosity.\n        seed (int): The algorithm will use it to seed the randomnumber generator, ensuring replicability.\n\n    Returns:\n        Xu (ndarray): (m, d); Solution sensor placement locations\n    \"\"\"\n    if X_init is None:\n        X_init = get_inducing_pts(self.X_train, num_sensors, random=True)\n    else:\n        num_sensors = len(X_init.reshape(-1, self.num_dim))\n    X_init = X_init.reshape(-1)\n\n    pbounds = {}\n    for i in range(self.num_dim*num_sensors):\n        pbounds['x{}'.format(i)] = self.pbounds_dim[i%self.num_dim]\n\n    optimizer = BayesianOptimization(f=self.objective,\n                                     pbounds=pbounds,\n                                     verbose=verbose,\n                                     random_state=seed,\n                                     allow_duplicate_points=True)\n    optimizer.maximize(init_points=init_points,\n                       n_iter=max_steps)\n\n    sol = []\n    for i in range(self.num_dim*num_sensors):\n        sol.append(optimizer.max['params']['x{}'.format(i)])\n    sol = np.array(sol).reshape(-1, self.num_dim)\n    if self.transform is not None:\n        sol = self.transform.expand(sol,\n                                    expand_sensor_model=False)\n        if not isinstance(sol, np.ndarray):\n            sol = sol.numpy()\n    return sol.reshape(-1, self.num_dim)\n</code></pre>"},{"location":"api/models/Continuous-SGP.html","title":"Continuous SGP","text":""},{"location":"api/models/Continuous-SGP.html#sgptools.models.continuous_sgp","title":"<code>sgptools.models.continuous_sgp</code>","text":""},{"location":"api/models/Continuous-SGP.html#sgptools.models.continuous_sgp.continuous_sgp","title":"<code>continuous_sgp(num_inducing, X_train, noise_variance, kernel, transform=None, Xu_init=None, Xu_time=None, orientation=False, **kwargs)</code>","text":"<p>Get sensor placement solutions using the Continuous-SGP method</p> Refer to the following papers for more details <ul> <li>Efficient Sensor Placement from Regression with Sparse Gaussian Processes in Continuous and Discrete Spaces [Jakkala and Akella, 2023]</li> <li>Multi-Robot Informative Path Planning from Regression with Sparse Gaussian Processes [Jakkala and Akella, 2024]</li> </ul> <p>Parameters:</p> Name Type Description Default <code>num_inducing</code> <code>int</code> <p>Number of inducing points</p> required <code>X_train</code> <code>ndarray</code> <p>(n, d); Unlabeled random sampled training points</p> required <code>noise_variance</code> <code>float</code> <p>data variance</p> required <code>kernel</code> <code>Kernel</code> <p>gpflow kernel function</p> required <code>transform</code> <code>Transform</code> <p>Transform object</p> <code>None</code> <code>Xu_init</code> <code>ndarray</code> <p>(m, d); Initial inducing points</p> <code>None</code> <code>Xu_time</code> <code>ndarray</code> <p>(t, d); Temporal inducing points used in spatio-temporal models</p> <code>None</code> <code>orientation</code> <code>bool</code> <p>If True, a additionl dimension is added to the                  inducing points to represent the FoV orientation</p> <code>False</code> <p>Returns:</p> Name Type Description <code>sgpr</code> <code>AugmentedSGPR</code> <p>Optimized sparse Gaussian process model</p> <code>loss</code> <code>ndarray</code> <p>Loss values computed during training</p> Source code in <code>sgptools/models/continuous_sgp.py</code> <pre><code>def continuous_sgp(num_inducing, X_train, noise_variance, kernel, \n                   transform=None,\n                   Xu_init=None, \n                   Xu_time=None, \n                   orientation=False,\n                   **kwargs):\n    \"\"\"Get sensor placement solutions using the Continuous-SGP method\n\n    Refer to the following papers for more details:\n        - Efficient Sensor Placement from Regression with Sparse Gaussian Processes in Continuous and Discrete Spaces [[Jakkala and Akella, 2023](https://www.itskalvik.com/publication/sgp-sp/)]\n        - Multi-Robot Informative Path Planning from Regression with Sparse Gaussian Processes [[Jakkala and Akella, 2024](https://www.itskalvik.com/publication/sgp-ipp/)]\n\n    Args:\n        num_inducing (int): Number of inducing points\n        X_train (ndarray): (n, d); Unlabeled random sampled training points\n        noise_variance (float): data variance\n        kernel (gpflow.kernels.Kernel): gpflow kernel function\n        transform (Transform): Transform object\n        Xu_init (ndarray): (m, d); Initial inducing points\n        Xu_time (ndarray): (t, d); Temporal inducing points used in spatio-temporal models\n        orientation (bool): If True, a additionl dimension is added to the \n                            inducing points to represent the FoV orientation\n\n    Returns:\n        sgpr (AugmentedSGPR): Optimized sparse Gaussian process model\n        loss (ndarray): Loss values computed during training\n    \"\"\"\n    # Generate init inducing points\n    if Xu_init is None:\n        Xu_init = get_inducing_pts(X_train, num_inducing, \n                                   orientation=orientation)\n\n    # Fit spare GP\n    sgpr = AugmentedSGPR((X_train, np.zeros((len(X_train), 1)).astype(X_train.dtype)),\n                         noise_variance=noise_variance,\n                         kernel=kernel, \n                         inducing_variable=Xu_init,\n                         inducing_variable_time=Xu_time,\n                         transform=transform)\n\n    # Train the mode\n    loss = optimize_model(sgpr,\n                          kernel_grad=False, \n                          **kwargs)\n\n    return sgpr, loss\n</code></pre>"},{"location":"api/models/Genetic-Algorithm.html","title":"Genetic Algorithm","text":""},{"location":"api/models/Genetic-Algorithm.html#sgptools.models.cma_es","title":"<code>sgptools.models.cma_es</code>","text":""},{"location":"api/models/Genetic-Algorithm.html#sgptools.models.cma_es.CMA_ES","title":"<code>CMA_ES</code>","text":"<p>Class for optimizing sensor placements using CMA-ES (a genetic algorithm)</p> Refer to the following paper for more details <ul> <li>Adaptive Continuous-Space Informative Path Planning for Online Environmental Monitoring [Hitz et al., 2017]</li> </ul> <p>Parameters:</p> Name Type Description Default <code>X_train</code> <code>ndarray</code> <p>(n, d); Locations in the environment used to approximate the monitoring regions</p> required <code>noise_variance</code> <code>float</code> <p>data variance</p> required <code>kernel</code> <code>Kernel</code> <p>gpflow kernel function</p> required <code>distance_budget</code> <code>float</code> <p>Distance budget for when treating the inducing points                       as waypoints of a path</p> <code>None</code> <code>num_robots</code> <code>int</code> <p>Number of robots, used when modeling                multi-robot IPP with a distance budget</p> <code>1</code> <code>transform</code> <code>Transform</code> <p>Transform object</p> <code>None</code> Source code in <code>sgptools/models/cma_es.py</code> <pre><code>class CMA_ES:\n    \"\"\"Class for optimizing sensor placements using CMA-ES (a genetic algorithm)\n\n    Refer to the following paper for more details:\n        - Adaptive Continuous-Space Informative Path Planning for Online Environmental Monitoring [Hitz et al., 2017]\n\n    Args:\n        X_train (ndarray): (n, d); Locations in the environment used to approximate the monitoring regions\n        noise_variance (float): data variance\n        kernel (gpflow.kernels.Kernel): gpflow kernel function\n        distance_budget (float): Distance budget for when treating the inducing points \n                                 as waypoints of a path\n        num_robots (int): Number of robots, used when modeling \n                          multi-robot IPP with a distance budget\n        transform (Transform): Transform object\n    \"\"\"\n    def __init__(self, X_train, noise_variance, kernel,\n                 distance_budget=None,\n                 num_robots=1,\n                 transform=None):\n        self.boundaries = geometry.MultiPoint([[p[0], p[1]] for p in X_train]).convex_hull\n        self.X_train = X_train\n        self.noise_variance = noise_variance\n        self.kernel = kernel\n        self.num_dim = X_train.shape[-1]\n        self.distance_budget = distance_budget\n        self.num_robots = num_robots\n        self.transform = transform\n\n    def update(self, noise_variance, kernel):\n        \"\"\"Update GP noise variance and kernel function parameters\n\n        Args:\n            noise_variance (float): data variance\n            kernel (gpflow.kernels.Kernel): gpflow kernel function\n        \"\"\"\n        self.noise_variance = noise_variance\n        self.kernel = kernel\n\n    def objective(self, X):\n        \"\"\"Objective function (GP-based Mutual Information)\n\n        Args:\n            X (ndarray): (n, d); Current solution sensor placement locations\n        \"\"\"\n        # MI does not depend on waypoint order (reshape to -1, num_dim)\n        X = np.array(X).reshape(-1, self.num_dim)\n        constraints_loss = 0.0\n        if self.transform is not None:\n            X = self.transform.expand(X)\n            constraints_loss = self.transform.constraints(X)\n\n        try:\n            mi = -get_mi(X, self.X_train, self.noise_variance, self.kernel)\n            mi -= constraints_loss\n            mi = mi.numpy()\n        except:\n            mi = 0.0 # if the cholskey decomposition fails\n        return mi\n\n    def optimize(self, \n                 num_sensors=10, \n                 max_steps=5000, \n                 tol=1e-6, \n                 X_init=None,\n                 verbose=0,\n                 seed=1234):\n        \"\"\"Optimizes the sensor placements using CMA-ES without any constraints\n\n        Args:\n            num_sensors (int): Number of sensor locations to optimize\n            max_steps (int): Maximum number of optimization steps\n            tol (float): Convergence tolerance to decide when to stop optimization\n            X_init (ndarray): (m, d); Initial inducing points\n            verbose (int): The level of verbosity.\n            seed (int): The algorithm will use it to seed the randomnumber generator, ensuring replicability.\n\n        Returns:\n            Xu (ndarray): (m, d); Solution sensor placement locations\n        \"\"\"\n        sigma0 = 1.0\n\n        if X_init is None:\n            X_init = get_inducing_pts(self.X_train, num_sensors, random=True)\n        X_init = X_init.reshape(-1)\n\n        xopt, _ = cma.fmin2(self.objective, X_init, sigma0, \n                            options={'maxfevals': max_steps,\n                                     'verb_disp': verbose,\n                                     'tolfun': tol,\n                                     'seed': seed},\n                            restarts=5)\n\n        xopt = np.array(xopt).reshape(-1, self.num_dim)\n        if self.transform is not None:\n            xopt = self.transform.expand(xopt, \n                                         expand_sensor_model=False)\n            if not isinstance(xopt, np.ndarray):\n                xopt = xopt.numpy()\n        return xopt.reshape(-1, self.num_dim)\n</code></pre>"},{"location":"api/models/Genetic-Algorithm.html#sgptools.models.cma_es.CMA_ES.objective","title":"<code>objective(X)</code>","text":"<p>Objective function (GP-based Mutual Information)</p> <p>Parameters:</p> Name Type Description Default <code>X</code> <code>ndarray</code> <p>(n, d); Current solution sensor placement locations</p> required Source code in <code>sgptools/models/cma_es.py</code> <pre><code>def objective(self, X):\n    \"\"\"Objective function (GP-based Mutual Information)\n\n    Args:\n        X (ndarray): (n, d); Current solution sensor placement locations\n    \"\"\"\n    # MI does not depend on waypoint order (reshape to -1, num_dim)\n    X = np.array(X).reshape(-1, self.num_dim)\n    constraints_loss = 0.0\n    if self.transform is not None:\n        X = self.transform.expand(X)\n        constraints_loss = self.transform.constraints(X)\n\n    try:\n        mi = -get_mi(X, self.X_train, self.noise_variance, self.kernel)\n        mi -= constraints_loss\n        mi = mi.numpy()\n    except:\n        mi = 0.0 # if the cholskey decomposition fails\n    return mi\n</code></pre>"},{"location":"api/models/Genetic-Algorithm.html#sgptools.models.cma_es.CMA_ES.optimize","title":"<code>optimize(num_sensors=10, max_steps=5000, tol=1e-06, X_init=None, verbose=0, seed=1234)</code>","text":"<p>Optimizes the sensor placements using CMA-ES without any constraints</p> <p>Parameters:</p> Name Type Description Default <code>num_sensors</code> <code>int</code> <p>Number of sensor locations to optimize</p> <code>10</code> <code>max_steps</code> <code>int</code> <p>Maximum number of optimization steps</p> <code>5000</code> <code>tol</code> <code>float</code> <p>Convergence tolerance to decide when to stop optimization</p> <code>1e-06</code> <code>X_init</code> <code>ndarray</code> <p>(m, d); Initial inducing points</p> <code>None</code> <code>verbose</code> <code>int</code> <p>The level of verbosity.</p> <code>0</code> <code>seed</code> <code>int</code> <p>The algorithm will use it to seed the randomnumber generator, ensuring replicability.</p> <code>1234</code> <p>Returns:</p> Name Type Description <code>Xu</code> <code>ndarray</code> <p>(m, d); Solution sensor placement locations</p> Source code in <code>sgptools/models/cma_es.py</code> <pre><code>def optimize(self, \n             num_sensors=10, \n             max_steps=5000, \n             tol=1e-6, \n             X_init=None,\n             verbose=0,\n             seed=1234):\n    \"\"\"Optimizes the sensor placements using CMA-ES without any constraints\n\n    Args:\n        num_sensors (int): Number of sensor locations to optimize\n        max_steps (int): Maximum number of optimization steps\n        tol (float): Convergence tolerance to decide when to stop optimization\n        X_init (ndarray): (m, d); Initial inducing points\n        verbose (int): The level of verbosity.\n        seed (int): The algorithm will use it to seed the randomnumber generator, ensuring replicability.\n\n    Returns:\n        Xu (ndarray): (m, d); Solution sensor placement locations\n    \"\"\"\n    sigma0 = 1.0\n\n    if X_init is None:\n        X_init = get_inducing_pts(self.X_train, num_sensors, random=True)\n    X_init = X_init.reshape(-1)\n\n    xopt, _ = cma.fmin2(self.objective, X_init, sigma0, \n                        options={'maxfevals': max_steps,\n                                 'verb_disp': verbose,\n                                 'tolfun': tol,\n                                 'seed': seed},\n                        restarts=5)\n\n    xopt = np.array(xopt).reshape(-1, self.num_dim)\n    if self.transform is not None:\n        xopt = self.transform.expand(xopt, \n                                     expand_sensor_model=False)\n        if not isinstance(xopt, np.ndarray):\n            xopt = xopt.numpy()\n    return xopt.reshape(-1, self.num_dim)\n</code></pre>"},{"location":"api/models/Genetic-Algorithm.html#sgptools.models.cma_es.CMA_ES.update","title":"<code>update(noise_variance, kernel)</code>","text":"<p>Update GP noise variance and kernel function parameters</p> <p>Parameters:</p> Name Type Description Default <code>noise_variance</code> <code>float</code> <p>data variance</p> required <code>kernel</code> <code>Kernel</code> <p>gpflow kernel function</p> required Source code in <code>sgptools/models/cma_es.py</code> <pre><code>def update(self, noise_variance, kernel):\n    \"\"\"Update GP noise variance and kernel function parameters\n\n    Args:\n        noise_variance (float): data variance\n        kernel (gpflow.kernels.Kernel): gpflow kernel function\n    \"\"\"\n    self.noise_variance = noise_variance\n    self.kernel = kernel\n</code></pre>"},{"location":"api/models/Greedy-MI.html","title":"Greedy MI","text":""},{"location":"api/models/Greedy-MI.html#sgptools.models.greedy_mi","title":"<code>sgptools.models.greedy_mi</code>","text":""},{"location":"api/models/Greedy-MI.html#sgptools.models.greedy_mi.GreedyMI","title":"<code>GreedyMI</code>","text":"<p>Helper class to compute mutual information using a Gaussian process for a given set of sensor locations. Used by <code>get_greedy_mi_sol</code> function to compute the solution sensor placements using the Greedy-MI method.</p> Refer to the following papers for more details <ul> <li>Near-Optimal Sensor Placements in Gaussian Processes: Theory, Efficient Algorithms and Empirical Studies [Krause et al., 2008]</li> <li>Data-driven learning and planning for environmental sampling [Ma et al., 2018]</li> </ul> <p>Parameters:</p> Name Type Description Default <code>S</code> <code>ndarray</code> <p>(n, d); Candidate sensor placement locations</p> required <code>V</code> <code>ndarray</code> <p>(n, d); Locations in the environment used to approximate the monitoring regions</p> required <code>noise_variance</code> <code>float</code> <p>data variance</p> required <code>kernel</code> <code>Kernel</code> <p>gpflow kernel function</p> required <code>transform</code> <code>Transform</code> <p>Transform object</p> <code>None</code> Source code in <code>sgptools/models/greedy_mi.py</code> <pre><code>class GreedyMI:\n    \"\"\"Helper class to compute mutual information using a Gaussian process for a given set of sensor locations.\n    Used by `get_greedy_mi_sol` function to compute the solution sensor placements using the Greedy-MI method.\n\n    Refer to the following papers for more details:\n        - Near-Optimal Sensor Placements in Gaussian Processes: Theory, Efficient Algorithms and Empirical Studies [Krause et al., 2008]\n        - Data-driven learning and planning for environmental sampling [Ma et al., 2018]\n\n    Args:\n        S (ndarray): (n, d); Candidate sensor placement locations\n        V (ndarray): (n, d); Locations in the environment used to approximate the monitoring regions\n        noise_variance (float): data variance\n        kernel (gpflow.kernels.Kernel): gpflow kernel function\n        transform (Transform): Transform object\n    \"\"\"\n    def __init__(self, S, V, noise_variance, kernel, \n                 transform=None):\n        self.S = S\n        self.V = V\n        self.kernel = kernel\n        self.input_dim = S.shape[1]\n        self.noise_variance = noise_variance\n        self.transform = transform\n\n    def mutual_info(self, x):\n        \"\"\"Computes mutual information using the points `x` \n\n        Args:\n            x (ndarray): (n); Indices of the solution placement locations\n\n        Returns:\n            MI (float): Mutual information between the placement x and candidate locations\n        \"\"\"\n        x = np.array(x).reshape(-1).astype(int)\n        A = self.S[x[:-1]].reshape(-1, self.input_dim)\n        y = self.S[x[-1]].reshape(-1, self.input_dim)\n\n        if len(A) == 0:\n            sigma_a = 1.0\n        else:\n            if self.transform is not None:\n                A = self.transform.expand(A)\n            a_gp = GPR(data=(A, np.zeros((len(A), 1))),\n                       kernel=self.kernel,\n                       noise_variance=self.noise_variance)\n            _, sigma_a = a_gp.predict_f(y)\n\n        # Remove locations in A\u222ay from V to build A bar (Refer to Krause et al., 2008)\n        V_ = self.V.copy()\n        V_rows = V_.view([('', V_.dtype)] * V_.shape[1])\n\n        if self.transform is not None:\n            solution = self.S[x].reshape(-1, self.input_dim)\n            A_ = self.transform.expand(solution)\n        else:\n            A_ = self.S[x]\n        A_rows = A_.view([('', V_.dtype)] * A_.shape[1])\n\n        V_ = np.setdiff1d(V_rows, A_rows).view(V_.dtype).reshape(-1, V_.shape[1])\n\n        self.v_gp = GPR(data=(V_, np.zeros((len(V_), 1))), \n                        kernel=self.kernel,\n                        noise_variance=self.noise_variance)\n        _, sigma_v = self.v_gp.predict_f(y)\n\n        return (sigma_a/sigma_v).numpy().squeeze()\n</code></pre>"},{"location":"api/models/Greedy-MI.html#sgptools.models.greedy_mi.GreedyMI.mutual_info","title":"<code>mutual_info(x)</code>","text":"<p>Computes mutual information using the points <code>x</code> </p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>ndarray</code> <p>(n); Indices of the solution placement locations</p> required <p>Returns:</p> Name Type Description <code>MI</code> <code>float</code> <p>Mutual information between the placement x and candidate locations</p> Source code in <code>sgptools/models/greedy_mi.py</code> <pre><code>def mutual_info(self, x):\n    \"\"\"Computes mutual information using the points `x` \n\n    Args:\n        x (ndarray): (n); Indices of the solution placement locations\n\n    Returns:\n        MI (float): Mutual information between the placement x and candidate locations\n    \"\"\"\n    x = np.array(x).reshape(-1).astype(int)\n    A = self.S[x[:-1]].reshape(-1, self.input_dim)\n    y = self.S[x[-1]].reshape(-1, self.input_dim)\n\n    if len(A) == 0:\n        sigma_a = 1.0\n    else:\n        if self.transform is not None:\n            A = self.transform.expand(A)\n        a_gp = GPR(data=(A, np.zeros((len(A), 1))),\n                   kernel=self.kernel,\n                   noise_variance=self.noise_variance)\n        _, sigma_a = a_gp.predict_f(y)\n\n    # Remove locations in A\u222ay from V to build A bar (Refer to Krause et al., 2008)\n    V_ = self.V.copy()\n    V_rows = V_.view([('', V_.dtype)] * V_.shape[1])\n\n    if self.transform is not None:\n        solution = self.S[x].reshape(-1, self.input_dim)\n        A_ = self.transform.expand(solution)\n    else:\n        A_ = self.S[x]\n    A_rows = A_.view([('', V_.dtype)] * A_.shape[1])\n\n    V_ = np.setdiff1d(V_rows, A_rows).view(V_.dtype).reshape(-1, V_.shape[1])\n\n    self.v_gp = GPR(data=(V_, np.zeros((len(V_), 1))), \n                    kernel=self.kernel,\n                    noise_variance=self.noise_variance)\n    _, sigma_v = self.v_gp.predict_f(y)\n\n    return (sigma_a/sigma_v).numpy().squeeze()\n</code></pre>"},{"location":"api/models/Greedy-MI.html#sgptools.models.greedy_mi.get_greedy_mi_sol","title":"<code>get_greedy_mi_sol(num_sensors, candidates, X_train, noise_variance, kernel, transform=None, optimizer='naive')</code>","text":"<p>Get sensor placement solutions using the GP-based mutual information approach (submodular objective function).  Uses a greedy algorithm to select sensor placements from a given discrete set of candidates locations.</p> Refer to the following papers for more details <ul> <li>Near-Optimal Sensor Placements in Gaussian Processes: Theory, Efficient Algorithms and Empirical Studies [Krause et al., 2008]</li> <li>Data-driven learning and planning for environmental sampling [Ma et al., 2018]</li> </ul> <p>Parameters:</p> Name Type Description Default <code>num_sensors</code> <code>int</code> <p>Number of sensor locations to optimize</p> required <code>candidates</code> <code>ndarray</code> <p>(n, d); Candidate sensor placement locations</p> required <code>X_train</code> <code>ndarray</code> <p>(n, d); Locations in the environment used to approximate the monitoring regions</p> required <code>noise_variance</code> <code>float</code> <p>data variance</p> required <code>kernel</code> <code>Kernel</code> <p>gpflow kernel function</p> required <code>transform</code> <code>Transform</code> <p>Transform object</p> <code>None</code> <code>optimizer</code> <code>str</code> <p>Name of an optimizer available in the apricot library</p> <code>'naive'</code> <p>Returns:</p> Name Type Description <code>Xu</code> <code>ndarray</code> <p>(m, d); Solution sensor placement locations</p> Source code in <code>sgptools/models/greedy_mi.py</code> <pre><code>def get_greedy_mi_sol(num_sensors, candidates, X_train, noise_variance, kernel, \n                      transform=None, optimizer='naive'):\n    \"\"\"Get sensor placement solutions using the GP-based mutual information approach (submodular objective function). \n    Uses a greedy algorithm to select sensor placements from a given discrete set of candidates locations.\n\n    Refer to the following papers for more details:\n        - Near-Optimal Sensor Placements in Gaussian Processes: Theory, Efficient Algorithms and Empirical Studies [Krause et al., 2008]\n        - Data-driven learning and planning for environmental sampling [Ma et al., 2018]\n\n    Args:\n        num_sensors (int): Number of sensor locations to optimize\n        candidates (ndarray): (n, d); Candidate sensor placement locations\n        X_train (ndarray): (n, d); Locations in the environment used to approximate the monitoring regions\n        noise_variance (float): data variance\n        kernel (gpflow.kernels.Kernel): gpflow kernel function\n        transform (Transform): Transform object\n        optimizer (str): Name of an optimizer available in the apricot library\n\n    Returns:\n        Xu (ndarray): (m, d); Solution sensor placement locations\n    \"\"\"\n    mi_model = GreedyMI(candidates, X_train, noise_variance, kernel, transform)\n    model = CustomSelection(num_sensors,\n                            mi_model.mutual_info,\n                            optimizer=optimizer,\n                            verbose=False)\n    sol = model.fit_transform(np.arange(len(candidates)).reshape(-1, 1))\n    return candidates[sol.reshape(-1)]\n</code></pre>"},{"location":"api/models/Greedy-SGP.html","title":"Greedy SGP","text":""},{"location":"api/models/Greedy-SGP.html#sgptools.models.greedy_sgp","title":"<code>sgptools.models.greedy_sgp</code>","text":""},{"location":"api/models/Greedy-SGP.html#sgptools.models.greedy_sgp.GreedySGP","title":"<code>GreedySGP</code>","text":"<p>Helper class to compute SGP's ELBO/optimization bound for a given set of sensor locations. Used by <code>get_greedy_sgp_sol</code> function to compute the solution sensor placements using the Greedy-SGP method.</p> Refer to the following papers for more details <ul> <li>Efficient Sensor Placement from Regression with Sparse Gaussian Processes in Continuous and Discrete Spaces [Jakkala and Akella, 2023]</li> </ul> <p>Parameters:</p> Name Type Description Default <code>num_inducing</code> <code>int</code> <p>Number of inducing points</p> required <code>S</code> <code>ndarray</code> <p>(n, d); Candidate sensor placement locations</p> required <code>V</code> <code>ndarray</code> <p>(n, d); Locations in the environment used to approximate the monitoring regions</p> required <code>noise_variance</code> <code>float</code> <p>Data noise variance</p> required <code>kernel</code> <code>Kernel</code> <p>gpflow kernel function</p> required <code>transform</code> <code>Transform</code> <p>Transform object</p> <code>None</code> Source code in <code>sgptools/models/greedy_sgp.py</code> <pre><code>class GreedySGP:\n    \"\"\"Helper class to compute SGP's ELBO/optimization bound for a given set of sensor locations.\n    Used by `get_greedy_sgp_sol` function to compute the solution sensor placements using the Greedy-SGP method.\n\n    Refer to the following papers for more details:\n        - Efficient Sensor Placement from Regression with Sparse Gaussian Processes in Continuous and Discrete Spaces [[Jakkala and Akella, 2023](https://www.itskalvik.com/publication/sgp-sp/)]\n\n    Args:\n        num_inducing (int): Number of inducing points\n        S (ndarray): (n, d); Candidate sensor placement locations\n        V (ndarray): (n, d); Locations in the environment used to approximate the monitoring regions\n        noise_variance (float): Data noise variance\n        kernel (gpflow.kernels.Kernel): gpflow kernel function\n        transform (Transform): Transform object\n    \"\"\"\n    def __init__(self, num_inducing, S, V, noise_variance, kernel, \n                 transform=None):\n        self.sgp = AugmentedSGPR((V, np.zeros((len(V), 1))),\n                                 noise_variance=noise_variance,\n                                 kernel=kernel, \n                                 inducing_variable=S[:num_inducing],\n                                 transform=transform)\n        self.locs = S\n        self.num_inducing = num_inducing\n        self.inducing_dim = S.shape[1]\n\n    def bound(self, x):\n        \"\"\"Computes the SGP's optimization bound using the inducing points `x` \n\n        Args:\n            x (ndarray): (n); Indices of the solution placement locations\n\n        Returns:\n            elbo (float): Evidence lower bound/SGP's optimization bound value\n        \"\"\"\n        x = np.array(x).reshape(-1).astype(int)\n        Xu = np.ones((self.num_inducing, self.inducing_dim), dtype=np.float32)\n\n        # Initialize all inducing points at the first solution placement location.\n        # Ensures that the number of inducing points is always fixed and no additional\n        # information is passed to the SGP\n        Xu *= self.locs[x][0]\n\n        # Copy all given solution placements to the inducing points set\n        Xu[-len(x):] = self.locs[x]\n\n        # Update the SGP inducing points\n        self.sgp.inducing_variable.Z.assign(Xu)\n        return self.sgp.elbo().numpy() # return the ELBO\n</code></pre>"},{"location":"api/models/Greedy-SGP.html#sgptools.models.greedy_sgp.GreedySGP.bound","title":"<code>bound(x)</code>","text":"<p>Computes the SGP's optimization bound using the inducing points <code>x</code> </p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>ndarray</code> <p>(n); Indices of the solution placement locations</p> required <p>Returns:</p> Name Type Description <code>elbo</code> <code>float</code> <p>Evidence lower bound/SGP's optimization bound value</p> Source code in <code>sgptools/models/greedy_sgp.py</code> <pre><code>def bound(self, x):\n    \"\"\"Computes the SGP's optimization bound using the inducing points `x` \n\n    Args:\n        x (ndarray): (n); Indices of the solution placement locations\n\n    Returns:\n        elbo (float): Evidence lower bound/SGP's optimization bound value\n    \"\"\"\n    x = np.array(x).reshape(-1).astype(int)\n    Xu = np.ones((self.num_inducing, self.inducing_dim), dtype=np.float32)\n\n    # Initialize all inducing points at the first solution placement location.\n    # Ensures that the number of inducing points is always fixed and no additional\n    # information is passed to the SGP\n    Xu *= self.locs[x][0]\n\n    # Copy all given solution placements to the inducing points set\n    Xu[-len(x):] = self.locs[x]\n\n    # Update the SGP inducing points\n    self.sgp.inducing_variable.Z.assign(Xu)\n    return self.sgp.elbo().numpy() # return the ELBO\n</code></pre>"},{"location":"api/models/Greedy-SGP.html#sgptools.models.greedy_sgp.get_greedy_sgp_sol","title":"<code>get_greedy_sgp_sol(num_sensors, candidates, X_train, noise_variance, kernel, transform=None)</code>","text":"<p>Get sensor placement solutions using the Greedy-SGP method. Uses a greedy algorithm to  select sensor placements from a given discrete set of candidates locations.</p> Refer to the following papers for more details <ul> <li>Efficient Sensor Placement from Regression with Sparse Gaussian Processes in Continuous and Discrete Spaces [Jakkala and Akella, 2023]</li> </ul> <p>Parameters:</p> Name Type Description Default <code>num_sensors</code> <code>int</code> <p>Number of sensor locations to optimize</p> required <code>candidates</code> <code>ndarray</code> <p>(n, d); Candidate sensor placement locations</p> required <code>X_train</code> <code>ndarray</code> <p>(n, d); Locations in the environment used to approximate the monitoring regions</p> required <code>noise_variance</code> <code>float</code> <p>data variance</p> required <code>kernel</code> <code>Kernel</code> <p>gpflow kernel function</p> required <code>transform</code> <code>Transform</code> <p>Transform object</p> <code>None</code> <p>Returns:</p> Name Type Description <code>Xu</code> <code>ndarray</code> <p>(m, d); Solution sensor placement locations</p> Source code in <code>sgptools/models/greedy_sgp.py</code> <pre><code>def get_greedy_sgp_sol(num_sensors, candidates, X_train, noise_variance, kernel, \n                       transform=None):\n    \"\"\"Get sensor placement solutions using the Greedy-SGP method. Uses a greedy algorithm to \n    select sensor placements from a given discrete set of candidates locations.\n\n    Refer to the following papers for more details:\n        - Efficient Sensor Placement from Regression with Sparse Gaussian Processes in Continuous and Discrete Spaces [[Jakkala and Akella, 2023](https://www.itskalvik.com/publication/sgp-sp/)]\n\n    Args:\n        num_sensors (int): Number of sensor locations to optimize\n        candidates (ndarray): (n, d); Candidate sensor placement locations\n        X_train (ndarray): (n, d); Locations in the environment used to approximate the monitoring regions\n        noise_variance (float): data variance\n        kernel (gpflow.kernels.Kernel): gpflow kernel function\n        transform (Transform): Transform object\n\n    Returns:\n        Xu (ndarray): (m, d); Solution sensor placement locations\n    \"\"\"\n    sgp_model = GreedySGP(num_sensors, candidates, X_train, \n                          noise_variance, kernel, transform=transform)\n    model = CustomSelection(num_sensors,\n                            sgp_model.bound,\n                            optimizer='naive',\n                            verbose=False)\n    sol = model.fit_transform(np.arange(len(candidates)).reshape(-1, 1))\n    return candidates[sol.reshape(-1)]\n</code></pre>"},{"location":"api/models/core/index.html","title":"","text":""},{"location":"api/models/core/index.html#sgptools.models.core","title":"<code>sgptools.models.core</code>","text":"<p>Core modules in this package:</p> <ul> <li><code>augmented_gpr</code>: Provides a Gaussian process model with expand and aggregate functions</li> <li><code>augmented_sgpr</code>: Provides a sparse Gaussian process model with update, expand, and aggregate functions</li> <li><code>osgpr</code>: Provides a streaming sparse Gaussian process model along with initialization function</li> <li><code>transformations</code>: Provides transforms to model complex sensor field of views and handle informative path planning</li> </ul>"},{"location":"api/models/core/AugmentedGPR.html","title":"AugmentedGPR","text":""},{"location":"api/models/core/AugmentedGPR.html#sgptools.models.core.augmented_gpr","title":"<code>sgptools.models.core.augmented_gpr</code>","text":"<p>Provides a Gaussian process model with expand and aggregate functions</p>"},{"location":"api/models/core/AugmentedGPR.html#sgptools.models.core.augmented_gpr.AugmentedGPR","title":"<code>AugmentedGPR</code>","text":"<p>               Bases: <code>GPR</code></p> <p>GPR model from the GPFlow library augmented to use a transform object's expand and aggregate functions on the data points where necessary.  </p> Refer to the following papers for more details <ul> <li>Efficient Sensor Placement from Regression with Sparse Gaussian Processes in Continuous and Discrete Spaces [Jakkala and Akella, 2023]</li> <li>Multi-Robot Informative Path Planning from Regression with Sparse Gaussian Processes [Jakkala and Akella, 2024]</li> </ul> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>tuple</code> <p>(X, y) ndarrays with inputs (n, d) and labels (n, 1)</p> required <code>kernel</code> <code>Kernel</code> <p>gpflow kernel function</p> required <code>noise_variance</code> <code>float</code> <p>data variance</p> required <code>transform</code> <code>Transform</code> <p>Transform object</p> <code>None</code> Source code in <code>sgptools/models/core/augmented_gpr.py</code> <pre><code>class AugmentedGPR(GPR):\n    \"\"\"GPR model from the GPFlow library augmented to use a transform object's\n    expand and aggregate functions on the data points where necessary.  \n\n    Refer to the following papers for more details:\n        - Efficient Sensor Placement from Regression with Sparse Gaussian Processes in Continuous and Discrete Spaces [Jakkala and Akella, 2023]\n        - Multi-Robot Informative Path Planning from Regression with Sparse Gaussian Processes [Jakkala and Akella, 2024]\n\n    Args:\n        data (tuple): (X, y) ndarrays with inputs (n, d) and labels (n, 1)\n        kernel (gpflow.kernels.Kernel): gpflow kernel function\n        noise_variance (float): data variance\n        transform (Transform): Transform object\n    \"\"\"\n    def __init__(\n        self,\n        *args,\n        transform=None,\n        **kwargs\n    ):\n        super().__init__(\n            *args,\n            **kwargs\n        )\n        if transform is None:\n            self.transform = Transform()\n        else:\n            self.transform = transform\n\n    def predict_f(\n        self, Xnew: InputData, \n        full_cov: bool = True, \n        full_output_cov: bool = False,\n        aggregate_train: bool = False,\n    ) -&gt; MeanAndVariance:\n        assert_params_false(self.predict_f, full_output_cov=full_output_cov)\n        if self.transform is not None:\n            Xnew = self.transform.expand(Xnew)\n\n        X, Y = self.data\n        err = Y - self.mean_function(X)\n\n        kmm = self.kernel(X)\n        knn = self.kernel(Xnew, full_cov=full_cov)\n        kmn = self.kernel(X, Xnew)\n        kmm_plus_s = add_likelihood_noise_cov(kmm, self.likelihood, X)\n\n        if self.transform is not None:\n            kmn = self.transform.aggregate(tf.transpose(kmn))\n            kmn = tf.transpose(kmn)\n            knn = self.transform.aggregate(knn)\n\n        if aggregate_train:\n            kmm_plus_s = self.transform.aggregate(kmm_plus_s)\n            err = self.transform.aggregate(err)\n            # reduce kmn only if it was not reduced before\n            # which can when train and test data are the same size\n            if kmn.shape[0] != kmn.shape[1]:\n                kmn = self.transform.aggregate(kmn)\n\n        conditional = gpflow.conditionals.base_conditional\n        f_mean_zero, f_var = conditional(\n            kmn, kmm_plus_s, knn, err, full_cov=full_cov, white=False\n        )  # [N, P], [N, P] or [P, N, N]\n        f_mean = f_mean_zero + self.mean_function(Xnew)\n        return f_mean, f_var\n</code></pre>"},{"location":"api/models/core/AugmentedSGPR.html","title":"AugmentedSGPR","text":""},{"location":"api/models/core/AugmentedSGPR.html#sgptools.models.core.augmented_sgpr","title":"<code>sgptools.models.core.augmented_sgpr</code>","text":"<p>Provides a sparse Gaussian process model with update, expand, and aggregate functions</p>"},{"location":"api/models/core/AugmentedSGPR.html#sgptools.models.core.augmented_sgpr.AugmentedSGPR","title":"<code>AugmentedSGPR</code>","text":"<p>               Bases: <code>SGPR</code></p> <p>SGPR model from the GPFlow library augmented to use a transform object's expand and aggregate functions on the inducing points where necessary. The object has an additional update function to update the kernel and noise variance parameters  (currently, the online updates part works only with RBF kernels).  </p> Refer to the following papers for more details <ul> <li>Efficient Sensor Placement from Regression with Sparse Gaussian Processes in Continuous and Discrete Spaces [Jakkala and Akella, 2023]</li> <li>Multi-Robot Informative Path Planning from Regression with Sparse Gaussian Processes [Jakkala and Akella, 2024]</li> </ul> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>tuple</code> <p>(X, y) ndarrays with inputs (n, d) and labels (n, 1)</p> required <code>kernel</code> <code>Kernel</code> <p>gpflow kernel function</p> required <code>noise_variance</code> <code>float</code> <p>data variance</p> required <code>inducing_variable</code> <code>ndarray</code> <p>(m, d); Initial inducing points</p> required <code>transform</code> <code>Transform</code> <p>Transform object</p> <code>None</code> <code>inducing_variable_time</code> <code>ndarray</code> <p>(m, d); Temporal dimensions of the inducing points,                                  used when modeling spatio-temporal IPP</p> <code>None</code> Source code in <code>sgptools/models/core/augmented_sgpr.py</code> <pre><code>class AugmentedSGPR(SGPR):\n    \"\"\"SGPR model from the GPFlow library augmented to use a transform object's\n    expand and aggregate functions on the inducing points where necessary. The object\n    has an additional update function to update the kernel and noise variance parameters \n    (currently, the online updates part works only with RBF kernels).  \n\n\n    Refer to the following papers for more details:\n        - Efficient Sensor Placement from Regression with Sparse Gaussian Processes in Continuous and Discrete Spaces [Jakkala and Akella, 2023]\n        - Multi-Robot Informative Path Planning from Regression with Sparse Gaussian Processes [Jakkala and Akella, 2024]\n\n    Args:\n        data (tuple): (X, y) ndarrays with inputs (n, d) and labels (n, 1)\n        kernel (gpflow.kernels.Kernel): gpflow kernel function\n        noise_variance (float): data variance\n        inducing_variable (ndarray): (m, d); Initial inducing points\n        transform (Transform): Transform object\n        inducing_variable_time (ndarray): (m, d); Temporal dimensions of the inducing points, \n                                            used when modeling spatio-temporal IPP\n    \"\"\"\n    def __init__(\n        self,\n        *args,\n        transform=None,\n        inducing_variable_time=None,\n        **kwargs\n    ):\n        super().__init__(\n            *args,\n            **kwargs\n        )\n        if transform is None:\n            self.transform = Transform()\n        else:\n            self.transform = transform\n\n        if inducing_variable_time is not None:\n            self.inducing_variable_time = inducingpoint_wrapper(inducing_variable_time)\n            self.transform.inducing_variable_time = self.inducing_variable_time\n        else:\n            self.inducing_variable_time = None\n\n    def update(self, noise_variance, kernel):\n        \"\"\"Update SGP noise variance and kernel function parameters\n\n        Args:\n            noise_variance (float): data variance\n            kernel (gpflow.kernels.Kernel): gpflow kernel function\n        \"\"\"\n        self.likelihood.variance.assign(noise_variance)\n        self.kernel.lengthscales.assign(kernel.lengthscales)\n        self.kernel.variance.assign(kernel.variance)\n\n    def _common_calculation(self) -&gt; \"SGPR.CommonTensors\":\n        \"\"\"\n        Matrices used in log-det calculation\n        :return: A , B, LB, AAT with :math:`LL\u1d40 = K\u1d64\u1d64 , A = L\u207b\u00b9K_{uf}/\u03c3, AAT = AA\u1d40,\n            B = AAT+I, LBLB\u1d40 = B`\n            A is M x N, B is M x M, LB is M x M, AAT is M x M\n        \"\"\"\n        x, _ = self.data\n\n        iv = self.inducing_variable.Z  # [M]\n        iv = self.transform.expand(iv)\n\n        kuf = self.kernel(iv, x)\n        kuf = self.transform.aggregate(kuf)\n\n        kuu = self.kernel(iv) + 1e-6 * tf.eye(tf.shape(iv)[0], dtype=iv.dtype)\n        kuu = self.transform.aggregate(kuu)\n\n        L = tf.linalg.cholesky(kuu)\n\n        sigma_sq = self.likelihood.variance\n        sigma = tf.sqrt(sigma_sq)\n\n        # Compute intermediate matrices\n        A = tf.linalg.triangular_solve(L, kuf, lower=True) / sigma\n        AAT = tf.linalg.matmul(A, A, transpose_b=True)\n        B = add_noise_cov(AAT, tf.cast(1.0, AAT.dtype))\n        LB = tf.linalg.cholesky(B)\n\n        return self.CommonTensors(sigma_sq, sigma, A, B, LB, AAT, L)\n\n    def elbo(self) -&gt; tf.Tensor:\n        \"\"\"\n        Construct a tensorflow function to compute the bound on the marginal\n        likelihood. For a derivation of the terms in here, see the associated\n        SGPR notebook.\n        \"\"\"\n        common = self._common_calculation()\n        output_shape = tf.shape(self.data[-1])\n        num_data = to_default_float(output_shape[0])\n        output_dim = to_default_float(output_shape[1])\n        const = -0.5 * num_data * output_dim * np.log(2 * np.pi)\n        logdet = self.logdet_term(common)\n        quad = self.quad_term(common)\n        constraints = self.transform.constraints(self.inducing_variable.Z)\n        return const + logdet + quad + constraints\n\n    def predict_f(\n        self, Xnew: InputData, full_cov: bool = False, full_output_cov: bool = False\n    ) -&gt; MeanAndVariance:\n\n        # could copy into posterior into a fused version\n        \"\"\"\n        Compute the mean and variance of the latent function at some new points\n        Xnew. For a derivation of the terms in here, see the associated SGPR\n        notebook.\n        \"\"\"\n        X_data, Y_data = self.data\n\n        iv = self.inducing_variable.Z\n        iv = self.transform.expand(iv)\n\n        num_inducing = tf.shape(iv)[0]\n\n        err = Y_data - self.mean_function(X_data)\n        kuf = self.kernel(iv, X_data)\n        kuu = self.kernel(iv) + 1e-6 * tf.eye(num_inducing, dtype=iv.dtype)\n        Kus = self.kernel(iv, Xnew)\n        sigma = tf.sqrt(self.likelihood.variance)\n        L = tf.linalg.cholesky(kuu)\n        A = tf.linalg.triangular_solve(L, kuf, lower=True) / sigma\n        B = tf.linalg.matmul(A, A, transpose_b=True) + tf.eye(\n            num_inducing, dtype=default_float()\n        )  # cache qinv\n        LB = tf.linalg.cholesky(B)\n        Aerr = tf.linalg.matmul(A, err)\n        c = tf.linalg.triangular_solve(LB, Aerr, lower=True) / sigma\n        tmp1 = tf.linalg.triangular_solve(L, Kus, lower=True)\n        tmp2 = tf.linalg.triangular_solve(LB, tmp1, lower=True)\n        mean = tf.linalg.matmul(tmp2, c, transpose_a=True)\n        if full_cov:\n            var = (\n                self.kernel(Xnew)\n                + tf.linalg.matmul(tmp2, tmp2, transpose_a=True)\n                - tf.linalg.matmul(tmp1, tmp1, transpose_a=True)\n            )\n            var = tf.tile(var[None, ...], [self.num_latent_gps, 1, 1])  # [P, N, N]\n        else:\n            var = (\n                self.kernel(Xnew, full_cov=False)\n                + tf.reduce_sum(tf.square(tmp2), 0)\n                - tf.reduce_sum(tf.square(tmp1), 0)\n            )\n            var = tf.tile(var[:, None], [1, self.num_latent_gps])\n\n        return mean + self.mean_function(Xnew), var\n</code></pre>"},{"location":"api/models/core/AugmentedSGPR.html#sgptools.models.core.augmented_sgpr.AugmentedSGPR.elbo","title":"<code>elbo()</code>","text":"<p>Construct a tensorflow function to compute the bound on the marginal likelihood. For a derivation of the terms in here, see the associated SGPR notebook.</p> Source code in <code>sgptools/models/core/augmented_sgpr.py</code> <pre><code>def elbo(self) -&gt; tf.Tensor:\n    \"\"\"\n    Construct a tensorflow function to compute the bound on the marginal\n    likelihood. For a derivation of the terms in here, see the associated\n    SGPR notebook.\n    \"\"\"\n    common = self._common_calculation()\n    output_shape = tf.shape(self.data[-1])\n    num_data = to_default_float(output_shape[0])\n    output_dim = to_default_float(output_shape[1])\n    const = -0.5 * num_data * output_dim * np.log(2 * np.pi)\n    logdet = self.logdet_term(common)\n    quad = self.quad_term(common)\n    constraints = self.transform.constraints(self.inducing_variable.Z)\n    return const + logdet + quad + constraints\n</code></pre>"},{"location":"api/models/core/AugmentedSGPR.html#sgptools.models.core.augmented_sgpr.AugmentedSGPR.predict_f","title":"<code>predict_f(Xnew, full_cov=False, full_output_cov=False)</code>","text":"<p>Compute the mean and variance of the latent function at some new points Xnew. For a derivation of the terms in here, see the associated SGPR notebook.</p> Source code in <code>sgptools/models/core/augmented_sgpr.py</code> <pre><code>def predict_f(\n    self, Xnew: InputData, full_cov: bool = False, full_output_cov: bool = False\n) -&gt; MeanAndVariance:\n\n    # could copy into posterior into a fused version\n    \"\"\"\n    Compute the mean and variance of the latent function at some new points\n    Xnew. For a derivation of the terms in here, see the associated SGPR\n    notebook.\n    \"\"\"\n    X_data, Y_data = self.data\n\n    iv = self.inducing_variable.Z\n    iv = self.transform.expand(iv)\n\n    num_inducing = tf.shape(iv)[0]\n\n    err = Y_data - self.mean_function(X_data)\n    kuf = self.kernel(iv, X_data)\n    kuu = self.kernel(iv) + 1e-6 * tf.eye(num_inducing, dtype=iv.dtype)\n    Kus = self.kernel(iv, Xnew)\n    sigma = tf.sqrt(self.likelihood.variance)\n    L = tf.linalg.cholesky(kuu)\n    A = tf.linalg.triangular_solve(L, kuf, lower=True) / sigma\n    B = tf.linalg.matmul(A, A, transpose_b=True) + tf.eye(\n        num_inducing, dtype=default_float()\n    )  # cache qinv\n    LB = tf.linalg.cholesky(B)\n    Aerr = tf.linalg.matmul(A, err)\n    c = tf.linalg.triangular_solve(LB, Aerr, lower=True) / sigma\n    tmp1 = tf.linalg.triangular_solve(L, Kus, lower=True)\n    tmp2 = tf.linalg.triangular_solve(LB, tmp1, lower=True)\n    mean = tf.linalg.matmul(tmp2, c, transpose_a=True)\n    if full_cov:\n        var = (\n            self.kernel(Xnew)\n            + tf.linalg.matmul(tmp2, tmp2, transpose_a=True)\n            - tf.linalg.matmul(tmp1, tmp1, transpose_a=True)\n        )\n        var = tf.tile(var[None, ...], [self.num_latent_gps, 1, 1])  # [P, N, N]\n    else:\n        var = (\n            self.kernel(Xnew, full_cov=False)\n            + tf.reduce_sum(tf.square(tmp2), 0)\n            - tf.reduce_sum(tf.square(tmp1), 0)\n        )\n        var = tf.tile(var[:, None], [1, self.num_latent_gps])\n\n    return mean + self.mean_function(Xnew), var\n</code></pre>"},{"location":"api/models/core/AugmentedSGPR.html#sgptools.models.core.augmented_sgpr.AugmentedSGPR.update","title":"<code>update(noise_variance, kernel)</code>","text":"<p>Update SGP noise variance and kernel function parameters</p> <p>Parameters:</p> Name Type Description Default <code>noise_variance</code> <code>float</code> <p>data variance</p> required <code>kernel</code> <code>Kernel</code> <p>gpflow kernel function</p> required Source code in <code>sgptools/models/core/augmented_sgpr.py</code> <pre><code>def update(self, noise_variance, kernel):\n    \"\"\"Update SGP noise variance and kernel function parameters\n\n    Args:\n        noise_variance (float): data variance\n        kernel (gpflow.kernels.Kernel): gpflow kernel function\n    \"\"\"\n    self.likelihood.variance.assign(noise_variance)\n    self.kernel.lengthscales.assign(kernel.lengthscales)\n    self.kernel.variance.assign(kernel.variance)\n</code></pre>"},{"location":"api/models/core/OSGPR.html","title":"OSGPR","text":""},{"location":"api/models/core/OSGPR.html#sgptools.models.core.osgpr","title":"<code>sgptools.models.core.osgpr</code>","text":"<p>Provides a streaming sparse Gaussian process model along with initialization function</p>"},{"location":"api/models/core/OSGPR.html#sgptools.models.core.osgpr.OSGPR_VFE","title":"<code>OSGPR_VFE</code>","text":"<p>               Bases: <code>GPModel</code>, <code>InternalDataTrainingLossMixin</code></p> <p>Online Sparse Variational GP regression model from streaming_sparse_gp</p> Refer to the following paper for more details <ul> <li>Streaming Gaussian process approximations [Bui et al., 2017]</li> </ul> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>tuple</code> <p>(X, y) ndarrays with inputs (n, d) and labels (n, 1)</p> required <code>kernel</code> <code>Kernel</code> <p>gpflow kernel function</p> required <code>mu_old</code> <code>ndarray</code> <p>mean of old <code>q(u)</code>; here <code>u</code> are the latents corresponding to the inducing points <code>Z_old</code></p> required <code>Su_old</code> <code>ndarray</code> <p>posterior covariance of old <code>q(u)</code></p> required <code>Kaa_old</code> <code>ndarray</code> <p>prior covariance of old <code>q(u)</code></p> required <code>Z_old</code> <code>ndarray</code> <p>(m_old, d): Old initial inducing points</p> required <code>Z</code> <code>ndarray</code> <p>(m_new, d): New initial inducing points</p> required <code>mean_function</code> <code>function</code> <p>GP mean function</p> <code>None</code> Source code in <code>sgptools/models/core/osgpr.py</code> <pre><code>class OSGPR_VFE(GPModel, InternalDataTrainingLossMixin):\n    \"\"\"Online Sparse Variational GP regression model from [streaming_sparse_gp](https://github.com/thangbui/streaming_sparse_gp/tree/master)\n\n    Refer to the following paper for more details:\n        - Streaming Gaussian process approximations [Bui et al., 2017]\n\n    Args:\n        data (tuple): (X, y) ndarrays with inputs (n, d) and labels (n, 1)\n        kernel (gpflow.kernels.Kernel): gpflow kernel function\n        mu_old (ndarray): mean of old `q(u)`; here `u` are the latents corresponding to the inducing points `Z_old`\n        Su_old (ndarray): posterior covariance of old `q(u)`\n        Kaa_old (ndarray): prior covariance of old `q(u)`\n        Z_old (ndarray): (m_old, d): Old initial inducing points\n        Z (ndarray): (m_new, d): New initial inducing points\n        mean_function (function): GP mean function\n    \"\"\"\n    def __init__(self, data, kernel, mu_old, Su_old, Kaa_old, Z_old, Z, mean_function=None):\n        self.X, self.Y = self.data = gpflow.models.util.data_input_to_tensor(data)\n        likelihood = gpflow.likelihoods.Gaussian()\n        num_latent_gps = GPModel.calc_num_latent_gps_from_data(data, kernel, likelihood)\n        super().__init__(kernel, likelihood, mean_function, num_latent_gps)\n\n        self.inducing_variable = InducingPoints(Z)\n        self.num_data = self.X.shape[0]\n\n        self.mu_old = tf.Variable(mu_old, shape=tf.TensorShape(None), trainable=False)\n        self.M_old = Z_old.shape[0]\n        self.Su_old = tf.Variable(Su_old, shape=tf.TensorShape(None), trainable=False)\n        self.Kaa_old = tf.Variable(Kaa_old, shape=tf.TensorShape(None), trainable=False)\n        self.Z_old = tf.Variable(Z_old, shape=tf.TensorShape(None), trainable=False)\n\n    def init_Z(self):\n        M = self.inducing_variable.Z.shape[0]\n        M_old = int(0.7 * M)\n        M_new = M - M_old\n        old_Z = self.Z_old.numpy()[np.random.permutation(M)[0:M_old], :]\n        new_Z = self.X.numpy()[np.random.permutation(self.X.shape[0])[0:M_new], :]\n        Z = np.vstack((old_Z, new_Z))\n        return Z\n\n    def update(self, data, inducing_variable=None, update_inducing=True):\n        \"\"\"Configure the OSGPR to adapt to a new batch of data. \n        Note: The OSGPR needs to be trained using gradient-based approaches after update.\n\n        Args:\n            data (tuple): (X, y) ndarrays with new batch of inputs (n, d) and labels (n, ndim)\n            inducing_variable (ndarray): (m_new, d): New initial inducing points\n            update_inducing (bool): Whether to update the inducing points\n        \"\"\"\n        self.X, self.Y = self.data = gpflow.models.util.data_input_to_tensor(data)\n        self.num_data = self.X.shape[0]\n\n        # Update the inducing points        \n        self.Z_old.assign(self.inducing_variable.Z.numpy())\n        if inducing_variable is None and update_inducing:\n            inducing_variable = self.init_Z()\n        if inducing_variable is not None:\n            self.inducing_variable.Z.assign(inducing_variable)\n\n        # Get posterior mean and covariance for the old inducing points\n        mu_old, Su_old = self.predict_f(self.Z_old, full_cov=True)\n        self.mu_old.assign(mu_old.numpy())\n        self.Su_old.assign(Su_old.numpy())\n\n        # Get the prior covariance matrix for the old inducing points\n        Kaa_old = self.kernel(self.Z_old)\n        self.Kaa_old.assign(Kaa_old.numpy())\n\n    def _common_terms(self):\n        Mb = self.inducing_variable.num_inducing\n        Ma = self.M_old\n        # jitter = gpflow.default_jitter()\n        jitter = gpflow.utilities.to_default_float(1e-4)\n        sigma2 = self.likelihood.variance\n        sigma = tf.sqrt(sigma2)\n\n        Saa = self.Su_old\n        ma = self.mu_old\n\n        # a is old inducing points, b is new\n        # f is training points\n        # s is test points\n        Kbf = covariances.Kuf(self.inducing_variable, self.kernel, self.X)\n        Kbb = covariances.Kuu(self.inducing_variable, self.kernel, jitter=jitter)\n        Kba = covariances.Kuf(self.inducing_variable, self.kernel, self.Z_old)\n        Kaa_cur = gpflow.utilities.add_noise_cov(self.kernel(self.Z_old), jitter)\n        Kaa = gpflow.utilities.add_noise_cov(self.Kaa_old, jitter)\n\n        err = self.Y - self.mean_function(self.X)\n\n        Sainv_ma = tf.linalg.solve(Saa, ma)\n        Sinv_y = self.Y / sigma2\n        c1 = tf.matmul(Kbf, Sinv_y)\n        c2 = tf.matmul(Kba, Sainv_ma)\n        c = c1 + c2\n\n        Lb = tf.linalg.cholesky(Kbb)\n        Lbinv_c = tf.linalg.triangular_solve(Lb, c, lower=True)\n        Lbinv_Kba = tf.linalg.triangular_solve(Lb, Kba, lower=True)\n        Lbinv_Kbf = tf.linalg.triangular_solve(Lb, Kbf, lower=True) / sigma\n        d1 = tf.matmul(Lbinv_Kbf, Lbinv_Kbf, transpose_b=True)\n\n        LSa = tf.linalg.cholesky(Saa)\n        Kab_Lbinv = tf.linalg.matrix_transpose(Lbinv_Kba)\n        LSainv_Kab_Lbinv = tf.linalg.triangular_solve(\n            LSa, Kab_Lbinv, lower=True)\n        d2 = tf.matmul(LSainv_Kab_Lbinv, LSainv_Kab_Lbinv, transpose_a=True)\n\n        La = tf.linalg.cholesky(Kaa)\n        Lainv_Kab_Lbinv = tf.linalg.triangular_solve(\n            La, Kab_Lbinv, lower=True)\n        d3 = tf.matmul(Lainv_Kab_Lbinv, Lainv_Kab_Lbinv, transpose_a=True)\n\n        D = tf.eye(Mb, dtype=gpflow.default_float()) + d1 + d2 - d3\n        D = gpflow.utilities.add_noise_cov(D, jitter)\n        LD = tf.linalg.cholesky(D)\n\n        LDinv_Lbinv_c = tf.linalg.triangular_solve(LD, Lbinv_c, lower=True)\n\n        return (Kbf, Kba, Kaa, Kaa_cur, La, Kbb, Lb, D, LD,\n                Lbinv_Kba, LDinv_Lbinv_c, err, d1)\n\n    def maximum_log_likelihood_objective(self):\n        \"\"\"\n        Construct a tensorflow function to compute the bound on the marginal\n        likelihood. \n        \"\"\"\n\n        Mb = self.inducing_variable.num_inducing\n        Ma = self.M_old\n        jitter = gpflow.default_jitter()\n        # jitter = gpflow.utilities.to_default_float(1e-4)\n        sigma2 = self.likelihood.variance\n        sigma = tf.sqrt(sigma2)\n        N = self.num_data\n\n        Saa = self.Su_old\n        ma = self.mu_old\n\n        # a is old inducing points, b is new\n        # f is training points\n        Kfdiag = self.kernel(self.X, full_cov=False)\n        (Kbf, Kba, Kaa, Kaa_cur, La, Kbb, Lb, D, LD,\n            Lbinv_Kba, LDinv_Lbinv_c, err, Qff) = self._common_terms()\n\n        LSa = tf.linalg.cholesky(Saa)\n        Lainv_ma = tf.linalg.triangular_solve(LSa, ma, lower=True)\n\n        # constant term\n        bound = -0.5 * N * np.log(2 * np.pi)\n        # quadratic term\n        bound += -0.5 * tf.reduce_sum(tf.square(err)) / sigma2\n        # bound += -0.5 * tf.reduce_sum(ma * Sainv_ma)\n        bound += -0.5 * tf.reduce_sum(tf.square(Lainv_ma))\n        bound += 0.5 * tf.reduce_sum(tf.square(LDinv_Lbinv_c))\n        # log det term\n        bound += -0.5 * N * tf.reduce_sum(tf.math.log(sigma2))\n        bound += - tf.reduce_sum(tf.math.log(tf.linalg.diag_part(LD)))\n\n        # delta 1: trace term\n        bound += -0.5 * tf.reduce_sum(Kfdiag) / sigma2\n        bound += 0.5 * tf.reduce_sum(tf.linalg.diag_part(Qff))\n\n        # delta 2: a and b difference\n        bound += tf.reduce_sum(tf.math.log(tf.linalg.diag_part(La)))\n        bound += - tf.reduce_sum(tf.math.log(tf.linalg.diag_part(LSa)))\n\n        Kaadiff = Kaa_cur - tf.matmul(Lbinv_Kba, Lbinv_Kba, transpose_a=True)\n        Sainv_Kaadiff = tf.linalg.solve(Saa, Kaadiff)\n        Kainv_Kaadiff = tf.linalg.solve(Kaa, Kaadiff)\n\n        bound += -0.5 * tf.reduce_sum(\n            tf.linalg.diag_part(Sainv_Kaadiff) - tf.linalg.diag_part(Kainv_Kaadiff))\n\n        return bound\n\n    def predict_f(self, Xnew, full_cov=False):\n        \"\"\"\n        Compute the mean and variance of the latent function at some new points\n        Xnew. \n        \"\"\"\n\n        # jitter = gpflow.default_jitter()\n        jitter = gpflow.utilities.to_default_float(1e-4)\n\n        # a is old inducing points, b is new\n        # f is training points\n        # s is test points\n        Kbs = covariances.Kuf(self.inducing_variable, self.kernel, Xnew)\n        (Kbf, Kba, Kaa, Kaa_cur, La, Kbb, Lb, D, LD,\n            Lbinv_Kba, LDinv_Lbinv_c, err, Qff) = self._common_terms()\n\n        Lbinv_Kbs = tf.linalg.triangular_solve(Lb, Kbs, lower=True)\n        LDinv_Lbinv_Kbs = tf.linalg.triangular_solve(LD, Lbinv_Kbs, lower=True)\n        mean = tf.matmul(LDinv_Lbinv_Kbs, LDinv_Lbinv_c, transpose_a=True)\n\n        if full_cov:\n            Kss = self.kernel(Xnew) + jitter * tf.eye(tf.shape(Xnew)[0], dtype=gpflow.default_float())\n            var1 = Kss\n            var2 = - tf.matmul(Lbinv_Kbs, Lbinv_Kbs, transpose_a=True)\n            var3 = tf.matmul(LDinv_Lbinv_Kbs, LDinv_Lbinv_Kbs, transpose_a=True)\n            var = var1 + var2 + var3\n        else:\n            var1 = self.kernel(Xnew, full_cov=False)\n            var2 = -tf.reduce_sum(tf.square(Lbinv_Kbs), axis=0)\n            var3 = tf.reduce_sum(tf.square(LDinv_Lbinv_Kbs), axis=0)\n            var = var1 + var2 + var3\n\n        return mean + self.mean_function(Xnew), var\n</code></pre>"},{"location":"api/models/core/OSGPR.html#sgptools.models.core.osgpr.OSGPR_VFE.maximum_log_likelihood_objective","title":"<code>maximum_log_likelihood_objective()</code>","text":"<p>Construct a tensorflow function to compute the bound on the marginal likelihood.</p> Source code in <code>sgptools/models/core/osgpr.py</code> <pre><code>def maximum_log_likelihood_objective(self):\n    \"\"\"\n    Construct a tensorflow function to compute the bound on the marginal\n    likelihood. \n    \"\"\"\n\n    Mb = self.inducing_variable.num_inducing\n    Ma = self.M_old\n    jitter = gpflow.default_jitter()\n    # jitter = gpflow.utilities.to_default_float(1e-4)\n    sigma2 = self.likelihood.variance\n    sigma = tf.sqrt(sigma2)\n    N = self.num_data\n\n    Saa = self.Su_old\n    ma = self.mu_old\n\n    # a is old inducing points, b is new\n    # f is training points\n    Kfdiag = self.kernel(self.X, full_cov=False)\n    (Kbf, Kba, Kaa, Kaa_cur, La, Kbb, Lb, D, LD,\n        Lbinv_Kba, LDinv_Lbinv_c, err, Qff) = self._common_terms()\n\n    LSa = tf.linalg.cholesky(Saa)\n    Lainv_ma = tf.linalg.triangular_solve(LSa, ma, lower=True)\n\n    # constant term\n    bound = -0.5 * N * np.log(2 * np.pi)\n    # quadratic term\n    bound += -0.5 * tf.reduce_sum(tf.square(err)) / sigma2\n    # bound += -0.5 * tf.reduce_sum(ma * Sainv_ma)\n    bound += -0.5 * tf.reduce_sum(tf.square(Lainv_ma))\n    bound += 0.5 * tf.reduce_sum(tf.square(LDinv_Lbinv_c))\n    # log det term\n    bound += -0.5 * N * tf.reduce_sum(tf.math.log(sigma2))\n    bound += - tf.reduce_sum(tf.math.log(tf.linalg.diag_part(LD)))\n\n    # delta 1: trace term\n    bound += -0.5 * tf.reduce_sum(Kfdiag) / sigma2\n    bound += 0.5 * tf.reduce_sum(tf.linalg.diag_part(Qff))\n\n    # delta 2: a and b difference\n    bound += tf.reduce_sum(tf.math.log(tf.linalg.diag_part(La)))\n    bound += - tf.reduce_sum(tf.math.log(tf.linalg.diag_part(LSa)))\n\n    Kaadiff = Kaa_cur - tf.matmul(Lbinv_Kba, Lbinv_Kba, transpose_a=True)\n    Sainv_Kaadiff = tf.linalg.solve(Saa, Kaadiff)\n    Kainv_Kaadiff = tf.linalg.solve(Kaa, Kaadiff)\n\n    bound += -0.5 * tf.reduce_sum(\n        tf.linalg.diag_part(Sainv_Kaadiff) - tf.linalg.diag_part(Kainv_Kaadiff))\n\n    return bound\n</code></pre>"},{"location":"api/models/core/OSGPR.html#sgptools.models.core.osgpr.OSGPR_VFE.predict_f","title":"<code>predict_f(Xnew, full_cov=False)</code>","text":"<p>Compute the mean and variance of the latent function at some new points Xnew.</p> Source code in <code>sgptools/models/core/osgpr.py</code> <pre><code>def predict_f(self, Xnew, full_cov=False):\n    \"\"\"\n    Compute the mean and variance of the latent function at some new points\n    Xnew. \n    \"\"\"\n\n    # jitter = gpflow.default_jitter()\n    jitter = gpflow.utilities.to_default_float(1e-4)\n\n    # a is old inducing points, b is new\n    # f is training points\n    # s is test points\n    Kbs = covariances.Kuf(self.inducing_variable, self.kernel, Xnew)\n    (Kbf, Kba, Kaa, Kaa_cur, La, Kbb, Lb, D, LD,\n        Lbinv_Kba, LDinv_Lbinv_c, err, Qff) = self._common_terms()\n\n    Lbinv_Kbs = tf.linalg.triangular_solve(Lb, Kbs, lower=True)\n    LDinv_Lbinv_Kbs = tf.linalg.triangular_solve(LD, Lbinv_Kbs, lower=True)\n    mean = tf.matmul(LDinv_Lbinv_Kbs, LDinv_Lbinv_c, transpose_a=True)\n\n    if full_cov:\n        Kss = self.kernel(Xnew) + jitter * tf.eye(tf.shape(Xnew)[0], dtype=gpflow.default_float())\n        var1 = Kss\n        var2 = - tf.matmul(Lbinv_Kbs, Lbinv_Kbs, transpose_a=True)\n        var3 = tf.matmul(LDinv_Lbinv_Kbs, LDinv_Lbinv_Kbs, transpose_a=True)\n        var = var1 + var2 + var3\n    else:\n        var1 = self.kernel(Xnew, full_cov=False)\n        var2 = -tf.reduce_sum(tf.square(Lbinv_Kbs), axis=0)\n        var3 = tf.reduce_sum(tf.square(LDinv_Lbinv_Kbs), axis=0)\n        var = var1 + var2 + var3\n\n    return mean + self.mean_function(Xnew), var\n</code></pre>"},{"location":"api/models/core/OSGPR.html#sgptools.models.core.osgpr.OSGPR_VFE.update","title":"<code>update(data, inducing_variable=None, update_inducing=True)</code>","text":"<p>Configure the OSGPR to adapt to a new batch of data.  Note: The OSGPR needs to be trained using gradient-based approaches after update.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>tuple</code> <p>(X, y) ndarrays with new batch of inputs (n, d) and labels (n, ndim)</p> required <code>inducing_variable</code> <code>ndarray</code> <p>(m_new, d): New initial inducing points</p> <code>None</code> <code>update_inducing</code> <code>bool</code> <p>Whether to update the inducing points</p> <code>True</code> Source code in <code>sgptools/models/core/osgpr.py</code> <pre><code>def update(self, data, inducing_variable=None, update_inducing=True):\n    \"\"\"Configure the OSGPR to adapt to a new batch of data. \n    Note: The OSGPR needs to be trained using gradient-based approaches after update.\n\n    Args:\n        data (tuple): (X, y) ndarrays with new batch of inputs (n, d) and labels (n, ndim)\n        inducing_variable (ndarray): (m_new, d): New initial inducing points\n        update_inducing (bool): Whether to update the inducing points\n    \"\"\"\n    self.X, self.Y = self.data = gpflow.models.util.data_input_to_tensor(data)\n    self.num_data = self.X.shape[0]\n\n    # Update the inducing points        \n    self.Z_old.assign(self.inducing_variable.Z.numpy())\n    if inducing_variable is None and update_inducing:\n        inducing_variable = self.init_Z()\n    if inducing_variable is not None:\n        self.inducing_variable.Z.assign(inducing_variable)\n\n    # Get posterior mean and covariance for the old inducing points\n    mu_old, Su_old = self.predict_f(self.Z_old, full_cov=True)\n    self.mu_old.assign(mu_old.numpy())\n    self.Su_old.assign(Su_old.numpy())\n\n    # Get the prior covariance matrix for the old inducing points\n    Kaa_old = self.kernel(self.Z_old)\n    self.Kaa_old.assign(Kaa_old.numpy())\n</code></pre>"},{"location":"api/models/core/OSGPR.html#sgptools.models.core.osgpr.init_osgpr","title":"<code>init_osgpr(X_train, num_inducing=10, lengthscales=1.0, variance=1.0, noise_variance=0.001, kernel=None, ndim=1)</code>","text":"<p>Initialize a VFE OSGPR model with an RBF kernel with  unit variance and lengthcales, and 0.001 noise variance. Used in the Online Continuous SGP approach. </p> <p>Parameters:</p> Name Type Description Default <code>X_train</code> <code>ndarray</code> <p>(n, d); Unlabeled random sampled training points.              They only effect the initial inducing point locations,              i.e., limits them to the bounds of the data</p> required <code>num_inducing</code> <code>int</code> <p>Number of inducing points</p> <code>10</code> <code>lengthscales</code> <code>float or list</code> <p>Kernel lengthscale(s), if passed as a list,                      each element corresponds to each data dimension</p> <code>1.0</code> <code>variance</code> <code>float</code> <p>Kernel variance</p> <code>1.0</code> <code>noise_variance</code> <code>float</code> <p>Data noise variance</p> <code>0.001</code> <code>kernel</code> <code>Kernel</code> <p>gpflow kernel function</p> <code>None</code> <code>ndim</code> <code>int</code> <p>Number of output dimensions</p> <code>1</code> <p>Returns:</p> Name Type Description <code>online_param</code> <code>OSGPR_VFE</code> <p>Initialized online sparse Gaussian process model</p> Source code in <code>sgptools/models/core/osgpr.py</code> <pre><code>def init_osgpr(X_train, \n               num_inducing=10, \n               lengthscales=1.0, \n               variance=1.0,\n               noise_variance=0.001,\n               kernel=None,\n               ndim=1):\n    \"\"\"Initialize a VFE OSGPR model with an RBF kernel with \n    unit variance and lengthcales, and 0.001 noise variance.\n    Used in the Online Continuous SGP approach. \n\n    Args:\n        X_train (ndarray): (n, d); Unlabeled random sampled training points. \n                        They only effect the initial inducing point locations, \n                        i.e., limits them to the bounds of the data\n        num_inducing (int): Number of inducing points\n        lengthscales (float or list): Kernel lengthscale(s), if passed as a list, \n                                each element corresponds to each data dimension\n        variance (float): Kernel variance\n        noise_variance (float): Data noise variance\n        kernel (gpflow.kernels.Kernel): gpflow kernel function\n        ndim (int): Number of output dimensions\n\n    Returns:\n        online_param (OSGPR_VFE): Initialized online sparse Gaussian process model\n    \"\"\"\n\n    if kernel is None:\n        kernel = gpflow.kernels.SquaredExponential(lengthscales=lengthscales, \n                                                   variance=variance)\n\n    y_train = np.zeros((len(X_train), ndim), dtype=X_train.dtype)\n    Z_init = get_inducing_pts(X_train, num_inducing)\n    init_param = gpflow.models.SGPR((X_train, y_train),\n                                    kernel, \n                                    inducing_variable=Z_init, \n                                    noise_variance=noise_variance)\n\n    # Initialize the OSGPR model using the parameters from the SGPR model\n    # The X_train and y_train here will be overwritten in the online phase \n    X_train = np.zeros([2, X_train.shape[-1]], dtype=X_train.dtype)\n    y_train =  np.zeros([2, ndim], dtype=X_train.dtype)\n    Zopt = init_param.inducing_variable.Z.numpy()\n    mu, Su = init_param.predict_f(Zopt, full_cov=True)\n    Kaa = init_param.kernel(Zopt)\n    online_param = OSGPR_VFE((X_train[:2], y_train[:2]),\n                             init_param.kernel,\n                             mu, Su[0], Kaa,\n                             Zopt, Zopt)\n    online_param.likelihood.variance.assign(init_param.likelihood.variance)\n\n    return online_param\n</code></pre>"},{"location":"api/models/core/Transformations.html","title":"Transformations","text":""},{"location":"api/models/core/Transformations.html#sgptools.models.core.transformations","title":"<code>sgptools.models.core.transformations</code>","text":"<p>Provides transforms to model complex sensor field of views and handle informative path planning</p>"},{"location":"api/models/core/Transformations.html#sgptools.models.core.transformations.IPPTransform","title":"<code>IPPTransform</code>","text":"<p>               Bases: <code>Transform</code></p> <p>Transform to model IPP problems</p> Usage details <ul> <li>For point sensing, set <code>sampling_rate = 2</code></li> <li>For continuous sensing, set <code>sampling_rate &gt; 2</code> (account for the information along the path)</li> <li>For continuous sensing with aggregation, set <code>sampling_rate &gt; 2</code> and <code>aggregate_fov = True</code> (faster but solution quality is a bit diminished)</li> <li>If using a non-point FoV model with continuous sampling, only the FoV inducing points are aggregated</li> <li>For multi-robot case, set <code>num_robots &gt; 1</code></li> <li>For onlineIPP use <code>update_fixed</code> to freeze the visited waypoints</li> </ul> <p>Parameters:</p> Name Type Description Default <code>sampling_rate</code> <code>int</code> <p>Number of points to sample between each pair of inducing points</p> <code>2</code> <code>distance_budget</code> <code>float</code> <p>Distance budget for the path</p> <code>None</code> <code>num_robots</code> <code>int</code> <p>Number of robots</p> <code>1</code> <code>Xu_fixed</code> <code>ndarray</code> <p>(num_robots, num_visited, num_dim); Visited waypoints that don't need to be optimized</p> <code>None</code> <code>num_dim</code> <code>int</code> <p>Number of dimensions of the inducing points</p> <code>2</code> <code>sensor_model</code> <code>Transform</code> <p>Transform object to expand each inducing point to <code>p</code> points                        approximating each sensor's FoV</p> <code>None</code> <code>aggregate_fov</code> <code>bool</code> <p>Used only when sampling_rate &gt; 2, i.e., when using a continuous sensing model.                    If <code>True</code>, covariances corresponding to interpolated inducing points along each edge                   of the path are aggregated to reduce the matrix inversion cost.</p> <code>False</code> Source code in <code>sgptools/models/core/transformations.py</code> <pre><code>class IPPTransform(Transform):\n    \"\"\"Transform to model IPP problems\n\n    Usage details: \n        * For point sensing, set `sampling_rate = 2`\n        * For continuous sensing, set `sampling_rate &gt; 2` (account for the information along the path)\n        * For continuous sensing with aggregation, set `sampling_rate &gt; 2` and `aggregate_fov = True` (faster but solution quality is a bit diminished)\n        * If using a non-point FoV model with continuous sampling, only the FoV inducing points are aggregated\n        * For multi-robot case, set `num_robots &gt; 1`\n        * For onlineIPP use `update_fixed` to freeze the visited waypoints\n\n    Args:\n        sampling_rate (int): Number of points to sample between each pair of inducing points\n        distance_budget (float): Distance budget for the path\n        num_robots (int): Number of robots\n        Xu_fixed (ndarray): (num_robots, num_visited, num_dim); Visited waypoints that don't need to be optimized\n        num_dim (int): Number of dimensions of the inducing points\n        sensor_model (Transform): Transform object to expand each inducing point to `p` points \n                                  approximating each sensor's FoV\n        aggregate_fov (bool): Used only when sampling_rate &gt; 2, i.e., when using a continuous sensing model. \n                              If `True`, covariances corresponding to interpolated inducing points along each edge\n                              of the path are aggregated to reduce the matrix inversion cost.\n    \"\"\"\n    def __init__(self, \n                 sampling_rate=2, \n                 distance_budget=None, \n                 num_robots=1,\n                 Xu_fixed=None,\n                 num_dim=2,\n                 sensor_model=None,\n                 aggregate_fov=False,\n                 **kwargs):\n        super().__init__(**kwargs)\n        if sampling_rate &lt; 2:\n            raise ValueError('Sampling rate must be greater than 2.')\n\n        self.sampling_rate = sampling_rate\n        self.distance_budget = distance_budget\n        self.num_robots = num_robots\n        self.num_dim = num_dim\n        self.sensor_model = sensor_model\n\n        # Set aggregation size to sampling rate if aggregate_fov is True\n        # and sampling rate is enabled (greater than 2)\n        if aggregate_fov:\n            if self.sensor_model is not None:\n                self.sensor_model.enable_aggregation()\n            elif sampling_rate &gt; 2:\n                self.aggregation_size = sampling_rate\n\n        # Initilize variable to store visited waypoints for onlineIPP\n        if Xu_fixed is not None:\n            self.Xu_fixed = tf.Variable(Xu_fixed, \n                                        shape=tf.TensorShape(None), \n                                        trainable=False)\n        else:\n            self.Xu_fixed = None\n\n    def update_Xu_fixed(self, Xu_fixed):\n        \"\"\"Function to update the visited waypoints\n\n        Args:\n            Xu_fixed (ndarray): numpy array (num_robots, num_visited_waypoints, num_dim)\n        \"\"\"\n        self.num_fixed = Xu_fixed.shape[1]\n        if self.Xu_fixed is not None:\n            self.Xu_fixed.assign(Xu_fixed)\n        else:\n            self.Xu_fixed = tf.Variable(Xu_fixed, \n                                        shape=tf.TensorShape(None), \n                                        trainable=False)\n\n    def expand(self, Xu, expand_sensor_model=True):\n        \"\"\"Sample points between each pair of inducing points to form the path\n\n        Args:\n            Xu (ndarray): (num_robots x num_inducing, num_dim); Inducing points in the num_dim dimensional space\n            expand_sensor_model (bool): Only add the fixed inducing points without other sensor/path transforms, \n                                        used for online IPP\n\n        Returns:\n            Xu (ndarray): Expansion transformed inducing points\n        \"\"\"\n        # If using single-robot offline IPP with point sensing, return inducing points as is.\n        if self.sampling_rate == 2 and self.Xu_fixed is None and self.sensor_model is None:\n            return Xu\n\n        Xu = tf.reshape(Xu, (self.num_robots, -1, self.num_dim))\n\n        # If using online IPP, add visited waypoints that won't be optimized anymore\n        if self.Xu_fixed is not None:\n            Xu = tf.concat([self.Xu_fixed, Xu[:, self.num_fixed:]], axis=1)\n\n        if not expand_sensor_model:\n            return tf.reshape(Xu, (-1, self.num_dim))\n\n        # Interpolate additional inducing points between waypoints to approximate \n        # the continuous data sensing model\n        if self.sampling_rate &gt; 2:\n            Xu = tf.linspace(Xu[:, :-1], Xu[:, 1:], self.sampling_rate)\n            Xu = tf.transpose(Xu, perm=[1, 2, 0, 3])\n            Xu = tf.reshape(Xu, (self.num_robots, -1, self.num_dim))\n\n        if self.sensor_model is not None:\n            Xu_ = []\n            for i in range(self.num_robots):\n                Xu_.append(self.sensor_model.expand(Xu[i]))\n            Xu = tf.concat(Xu_, axis=0)\n            return Xu\n\n        Xu = tf.reshape(Xu, (-1, self.num_dim))\n        return Xu\n\n    def aggregate(self, k):\n        \"\"\"Applies the aggregation transform to kernel matrices. Checks `sensor_model` \n           and uses the appropriate aggregation transform. \n\n        Args:\n            k (tensor): (mp, mp)/(mp, n); Kernel matrix. \n                        `m` is the number of inducing points,\n                        `p` is the number of points each inducing point is mapped,\n                        `n` is the number of training data points.\n\n        Returns:\n            k (tensor): (m, m)/(m, n); Aggregated kernel matrix\n        \"\"\"\n        if self.sensor_model is not None:\n            return self.sensor_model.aggregate(k)\n        else:\n            return super().aggregate(k)\n\n    def constraints(self, Xu):\n        \"\"\"Computes the distance constraint term that is added to the SGP's optimization function.\n        Each robot can be assigned a different distance budget.\n\n        Args:\n            Xu (ndarray): Inducing points from which to compute the distance constraints\n\n        Returns:\n            loss (float): distance constraint term\n        \"\"\"\n        if self.distance_budget is None:\n            return 0.\n        else:\n            # Only do fixed points expansion transform\n            Xu = self.expand(Xu, expand_sensor_model=False)\n            dist = self.distance(Xu)-self.distance_budget\n            dist = tf.reduce_sum(tf.nn.relu(dist))\n            loss = -dist*self.constraint_weight\n            return loss\n\n    def distance(self, Xu):\n        \"\"\"Computes the distance incured by sequentially visiting the inducing points\n        Args:\n            Xu (ndarray): (m, num_dim); Inducing points from which to compute the path lengths\n                          `m` is the number of inducing points\n                          `num_dim` dimension of the data collection environment\n        Returns:\n            dist (float or tensor of floats): path length(s)\n        \"\"\"\n        Xu = tf.reshape(Xu, (self.num_robots, -1, self.num_dim))\n        if self.sensor_model is not None:\n            dists = []\n            for i in range(self.num_robots):\n                dists.append(self.sensor_model.distance(Xu[i]))\n            dists = tf.concat(dists, axis=0)\n            return dists\n        else:\n            # Assumes 2D waypoints by default\n            dist = tf.norm(Xu[:, 1:, :2] - Xu[:, :-1, :2], axis=-1)\n            dist = tf.reduce_sum(dist, axis=1)\n            return dist\n</code></pre>"},{"location":"api/models/core/Transformations.html#sgptools.models.core.transformations.IPPTransform.aggregate","title":"<code>aggregate(k)</code>","text":"<p>Applies the aggregation transform to kernel matrices. Checks <code>sensor_model</code>     and uses the appropriate aggregation transform. </p> <p>Parameters:</p> Name Type Description Default <code>k</code> <code>tensor</code> <p>(mp, mp)/(mp, n); Kernel matrix.          <code>m</code> is the number of inducing points,         <code>p</code> is the number of points each inducing point is mapped,         <code>n</code> is the number of training data points.</p> required <p>Returns:</p> Name Type Description <code>k</code> <code>tensor</code> <p>(m, m)/(m, n); Aggregated kernel matrix</p> Source code in <code>sgptools/models/core/transformations.py</code> <pre><code>def aggregate(self, k):\n    \"\"\"Applies the aggregation transform to kernel matrices. Checks `sensor_model` \n       and uses the appropriate aggregation transform. \n\n    Args:\n        k (tensor): (mp, mp)/(mp, n); Kernel matrix. \n                    `m` is the number of inducing points,\n                    `p` is the number of points each inducing point is mapped,\n                    `n` is the number of training data points.\n\n    Returns:\n        k (tensor): (m, m)/(m, n); Aggregated kernel matrix\n    \"\"\"\n    if self.sensor_model is not None:\n        return self.sensor_model.aggregate(k)\n    else:\n        return super().aggregate(k)\n</code></pre>"},{"location":"api/models/core/Transformations.html#sgptools.models.core.transformations.IPPTransform.constraints","title":"<code>constraints(Xu)</code>","text":"<p>Computes the distance constraint term that is added to the SGP's optimization function. Each robot can be assigned a different distance budget.</p> <p>Parameters:</p> Name Type Description Default <code>Xu</code> <code>ndarray</code> <p>Inducing points from which to compute the distance constraints</p> required <p>Returns:</p> Name Type Description <code>loss</code> <code>float</code> <p>distance constraint term</p> Source code in <code>sgptools/models/core/transformations.py</code> <pre><code>def constraints(self, Xu):\n    \"\"\"Computes the distance constraint term that is added to the SGP's optimization function.\n    Each robot can be assigned a different distance budget.\n\n    Args:\n        Xu (ndarray): Inducing points from which to compute the distance constraints\n\n    Returns:\n        loss (float): distance constraint term\n    \"\"\"\n    if self.distance_budget is None:\n        return 0.\n    else:\n        # Only do fixed points expansion transform\n        Xu = self.expand(Xu, expand_sensor_model=False)\n        dist = self.distance(Xu)-self.distance_budget\n        dist = tf.reduce_sum(tf.nn.relu(dist))\n        loss = -dist*self.constraint_weight\n        return loss\n</code></pre>"},{"location":"api/models/core/Transformations.html#sgptools.models.core.transformations.IPPTransform.distance","title":"<code>distance(Xu)</code>","text":"<p>Computes the distance incured by sequentially visiting the inducing points Args:     Xu (ndarray): (m, num_dim); Inducing points from which to compute the path lengths                   <code>m</code> is the number of inducing points                   <code>num_dim</code> dimension of the data collection environment Returns:     dist (float or tensor of floats): path length(s)</p> Source code in <code>sgptools/models/core/transformations.py</code> <pre><code>def distance(self, Xu):\n    \"\"\"Computes the distance incured by sequentially visiting the inducing points\n    Args:\n        Xu (ndarray): (m, num_dim); Inducing points from which to compute the path lengths\n                      `m` is the number of inducing points\n                      `num_dim` dimension of the data collection environment\n    Returns:\n        dist (float or tensor of floats): path length(s)\n    \"\"\"\n    Xu = tf.reshape(Xu, (self.num_robots, -1, self.num_dim))\n    if self.sensor_model is not None:\n        dists = []\n        for i in range(self.num_robots):\n            dists.append(self.sensor_model.distance(Xu[i]))\n        dists = tf.concat(dists, axis=0)\n        return dists\n    else:\n        # Assumes 2D waypoints by default\n        dist = tf.norm(Xu[:, 1:, :2] - Xu[:, :-1, :2], axis=-1)\n        dist = tf.reduce_sum(dist, axis=1)\n        return dist\n</code></pre>"},{"location":"api/models/core/Transformations.html#sgptools.models.core.transformations.IPPTransform.expand","title":"<code>expand(Xu, expand_sensor_model=True)</code>","text":"<p>Sample points between each pair of inducing points to form the path</p> <p>Parameters:</p> Name Type Description Default <code>Xu</code> <code>ndarray</code> <p>(num_robots x num_inducing, num_dim); Inducing points in the num_dim dimensional space</p> required <code>expand_sensor_model</code> <code>bool</code> <p>Only add the fixed inducing points without other sensor/path transforms,                          used for online IPP</p> <code>True</code> <p>Returns:</p> Name Type Description <code>Xu</code> <code>ndarray</code> <p>Expansion transformed inducing points</p> Source code in <code>sgptools/models/core/transformations.py</code> <pre><code>def expand(self, Xu, expand_sensor_model=True):\n    \"\"\"Sample points between each pair of inducing points to form the path\n\n    Args:\n        Xu (ndarray): (num_robots x num_inducing, num_dim); Inducing points in the num_dim dimensional space\n        expand_sensor_model (bool): Only add the fixed inducing points without other sensor/path transforms, \n                                    used for online IPP\n\n    Returns:\n        Xu (ndarray): Expansion transformed inducing points\n    \"\"\"\n    # If using single-robot offline IPP with point sensing, return inducing points as is.\n    if self.sampling_rate == 2 and self.Xu_fixed is None and self.sensor_model is None:\n        return Xu\n\n    Xu = tf.reshape(Xu, (self.num_robots, -1, self.num_dim))\n\n    # If using online IPP, add visited waypoints that won't be optimized anymore\n    if self.Xu_fixed is not None:\n        Xu = tf.concat([self.Xu_fixed, Xu[:, self.num_fixed:]], axis=1)\n\n    if not expand_sensor_model:\n        return tf.reshape(Xu, (-1, self.num_dim))\n\n    # Interpolate additional inducing points between waypoints to approximate \n    # the continuous data sensing model\n    if self.sampling_rate &gt; 2:\n        Xu = tf.linspace(Xu[:, :-1], Xu[:, 1:], self.sampling_rate)\n        Xu = tf.transpose(Xu, perm=[1, 2, 0, 3])\n        Xu = tf.reshape(Xu, (self.num_robots, -1, self.num_dim))\n\n    if self.sensor_model is not None:\n        Xu_ = []\n        for i in range(self.num_robots):\n            Xu_.append(self.sensor_model.expand(Xu[i]))\n        Xu = tf.concat(Xu_, axis=0)\n        return Xu\n\n    Xu = tf.reshape(Xu, (-1, self.num_dim))\n    return Xu\n</code></pre>"},{"location":"api/models/core/Transformations.html#sgptools.models.core.transformations.IPPTransform.update_Xu_fixed","title":"<code>update_Xu_fixed(Xu_fixed)</code>","text":"<p>Function to update the visited waypoints</p> <p>Parameters:</p> Name Type Description Default <code>Xu_fixed</code> <code>ndarray</code> <p>numpy array (num_robots, num_visited_waypoints, num_dim)</p> required Source code in <code>sgptools/models/core/transformations.py</code> <pre><code>def update_Xu_fixed(self, Xu_fixed):\n    \"\"\"Function to update the visited waypoints\n\n    Args:\n        Xu_fixed (ndarray): numpy array (num_robots, num_visited_waypoints, num_dim)\n    \"\"\"\n    self.num_fixed = Xu_fixed.shape[1]\n    if self.Xu_fixed is not None:\n        self.Xu_fixed.assign(Xu_fixed)\n    else:\n        self.Xu_fixed = tf.Variable(Xu_fixed, \n                                    shape=tf.TensorShape(None), \n                                    trainable=False)\n</code></pre>"},{"location":"api/models/core/Transformations.html#sgptools.models.core.transformations.SquareHeightTransform","title":"<code>SquareHeightTransform</code>","text":"<p>               Bases: <code>Transform</code></p> <p>Non-point Transform to model a height-dependent square FoV</p> <p>Parameters:</p> Name Type Description Default <code>num_side</code> <code>int</code> <p>Number of points along each side of the FoV</p> required <code>aggregate_fov</code> <code>bool</code> <p>If <code>True</code>, covariances corresponding to interpolated inducing points used to                    approximate the sensor FoV are aggregated to reduce the matrix inversion cost</p> <code>False</code> Source code in <code>sgptools/models/core/transformations.py</code> <pre><code>class SquareHeightTransform(Transform):\n    \"\"\"Non-point Transform to model a height-dependent square FoV\n\n    Args:\n        num_side (int): Number of points along each side of the FoV\n        aggregate_fov (bool): If `True`, covariances corresponding to interpolated inducing points used to \n                              approximate the sensor FoV are aggregated to reduce the matrix inversion cost\n    \"\"\"\n    def __init__(self, num_side, aggregate_fov=False, **kwargs):\n        super().__init__(**kwargs)\n        self.num_side = num_side\n\n        if aggregate_fov:\n            self.enable_aggregation()\n\n    def enable_aggregation(self, size=None):\n        \"\"\"Enable FoV covariance aggregation, which reduces the covariance matrix inversion cost by reducing the \n        covariance matrix size.\n\n        Args:\n            size (int): If None, all the interpolated inducing points within the FoV are aggregated. Alternatively, \n                        the number of inducing points to aggregate can be explicitly defined using this variable.\n        \"\"\"\n        if size is None:\n            self.aggregation_size = self.num_side**2\n        else:\n            self.aggregation_size = size\n\n    def expand(self, Xu):     \n        \"\"\"\n        Applies the expansion transform to the inducing points\n\n        Args:\n            Xu (ndarray): (m, 3); Inducing points in the 3D position space.\n                        `m` is the number of inducing points,\n                        `3` is the dimension of the space (x, y, z)\n\n        Returns:\n            Xu (ndarray): (mp, 2); Inducing points in input space.\n                        `p` is the number of points each inducing point is mapped \n                        to in order to form the FoV.\n        \"\"\"\n        x, y, h = tf.split(Xu, num_or_size_splits=3, axis=1)\n        x = tf.reshape(x, [-1,])\n        y = tf.reshape(y, [-1,])\n        h = tf.reshape(h, [-1,])\n\n        delta = h / (self.num_side - 1)\n\n        pts = []\n        for i in range(self.num_side):\n            pts.append(tf.linspace([x - h/2, y - (h/2) + (delta * i)], \n                                   [x + h/2, y - (h/2) + (delta * i)], \n                                   self.num_side, \n                                   axis=1))\n        xy = tf.concat(pts, axis=1)\n        xy = tf.transpose(xy, [2, 1, 0])\n        xy = tf.reshape(xy, [-1, 2])\n        xy = self._reshape(xy, tf.shape(Xu)[0])\n        return xy\n\n    def _reshape(self, X, num_inducing):\n        \"\"\"Reorder the inducing points to be in the correct order for aggregation with square height FoV\n\n        Args:\n            X (ndarray): (mp, 2); Inducing points in input space. `p` is the number of points each \n                        inducing point is mapped to in order to form the FoV.\n\n        Returns:\n            Xu (ndarray): (mp, 2); Reorder inducing points\n        \"\"\"\n        X = tf.reshape(X, (num_inducing, -1, self.num_side, self.num_side, 2))\n        X = tf.transpose(X, (0, 2, 1, 3, 4))\n        X = tf.reshape(X, (-1, 2))\n        return X\n\n    def distance(self, Xu):\n        \"\"\"Computes the distance incured by sequentially visiting the inducing points\n        Args:\n            Xu (ndarray): (m, 3); Inducing points from which to compute the path lengths.\n                          `m` is the number of inducing points.\n\n        Returns:\n            dist (float): path lengths\n        \"\"\"\n        Xu = tf.reshape(Xu, (-1, 3))\n        dist = tf.norm(Xu[1:] - Xu[:-1], axis=-1)\n        dist = tf.reduce_sum(dist, axis=0)\n        return dist\n</code></pre>"},{"location":"api/models/core/Transformations.html#sgptools.models.core.transformations.SquareHeightTransform.distance","title":"<code>distance(Xu)</code>","text":"<p>Computes the distance incured by sequentially visiting the inducing points Args:     Xu (ndarray): (m, 3); Inducing points from which to compute the path lengths.                   <code>m</code> is the number of inducing points.</p> <p>Returns:</p> Name Type Description <code>dist</code> <code>float</code> <p>path lengths</p> Source code in <code>sgptools/models/core/transformations.py</code> <pre><code>def distance(self, Xu):\n    \"\"\"Computes the distance incured by sequentially visiting the inducing points\n    Args:\n        Xu (ndarray): (m, 3); Inducing points from which to compute the path lengths.\n                      `m` is the number of inducing points.\n\n    Returns:\n        dist (float): path lengths\n    \"\"\"\n    Xu = tf.reshape(Xu, (-1, 3))\n    dist = tf.norm(Xu[1:] - Xu[:-1], axis=-1)\n    dist = tf.reduce_sum(dist, axis=0)\n    return dist\n</code></pre>"},{"location":"api/models/core/Transformations.html#sgptools.models.core.transformations.SquareHeightTransform.enable_aggregation","title":"<code>enable_aggregation(size=None)</code>","text":"<p>Enable FoV covariance aggregation, which reduces the covariance matrix inversion cost by reducing the  covariance matrix size.</p> <p>Parameters:</p> Name Type Description Default <code>size</code> <code>int</code> <p>If None, all the interpolated inducing points within the FoV are aggregated. Alternatively,          the number of inducing points to aggregate can be explicitly defined using this variable.</p> <code>None</code> Source code in <code>sgptools/models/core/transformations.py</code> <pre><code>def enable_aggregation(self, size=None):\n    \"\"\"Enable FoV covariance aggregation, which reduces the covariance matrix inversion cost by reducing the \n    covariance matrix size.\n\n    Args:\n        size (int): If None, all the interpolated inducing points within the FoV are aggregated. Alternatively, \n                    the number of inducing points to aggregate can be explicitly defined using this variable.\n    \"\"\"\n    if size is None:\n        self.aggregation_size = self.num_side**2\n    else:\n        self.aggregation_size = size\n</code></pre>"},{"location":"api/models/core/Transformations.html#sgptools.models.core.transformations.SquareHeightTransform.expand","title":"<code>expand(Xu)</code>","text":"<p>Applies the expansion transform to the inducing points</p> <p>Parameters:</p> Name Type Description Default <code>Xu</code> <code>ndarray</code> <p>(m, 3); Inducing points in the 3D position space.         <code>m</code> is the number of inducing points,         <code>3</code> is the dimension of the space (x, y, z)</p> required <p>Returns:</p> Name Type Description <code>Xu</code> <code>ndarray</code> <p>(mp, 2); Inducing points in input space.         <code>p</code> is the number of points each inducing point is mapped          to in order to form the FoV.</p> Source code in <code>sgptools/models/core/transformations.py</code> <pre><code>def expand(self, Xu):     \n    \"\"\"\n    Applies the expansion transform to the inducing points\n\n    Args:\n        Xu (ndarray): (m, 3); Inducing points in the 3D position space.\n                    `m` is the number of inducing points,\n                    `3` is the dimension of the space (x, y, z)\n\n    Returns:\n        Xu (ndarray): (mp, 2); Inducing points in input space.\n                    `p` is the number of points each inducing point is mapped \n                    to in order to form the FoV.\n    \"\"\"\n    x, y, h = tf.split(Xu, num_or_size_splits=3, axis=1)\n    x = tf.reshape(x, [-1,])\n    y = tf.reshape(y, [-1,])\n    h = tf.reshape(h, [-1,])\n\n    delta = h / (self.num_side - 1)\n\n    pts = []\n    for i in range(self.num_side):\n        pts.append(tf.linspace([x - h/2, y - (h/2) + (delta * i)], \n                               [x + h/2, y - (h/2) + (delta * i)], \n                               self.num_side, \n                               axis=1))\n    xy = tf.concat(pts, axis=1)\n    xy = tf.transpose(xy, [2, 1, 0])\n    xy = tf.reshape(xy, [-1, 2])\n    xy = self._reshape(xy, tf.shape(Xu)[0])\n    return xy\n</code></pre>"},{"location":"api/models/core/Transformations.html#sgptools.models.core.transformations.SquareTransform","title":"<code>SquareTransform</code>","text":"<p>               Bases: <code>Transform</code></p> <p>Non-point Transform to model a square FoV. Only works for single robot cases. </p> <p>Parameters:</p> Name Type Description Default <code>length</code> <code>float</code> <p>Length of the square FoV</p> required <code>num_side</code> <code>int</code> <p>Number of points along each side of the FoV</p> required <code>aggregate_fov</code> <code>bool</code> <p>If <code>True</code>, covariances corresponding to interpolated inducing points used to                    approximate the sensor FoV are aggregated to reduce the matrix inversion cost</p> <code>False</code> Source code in <code>sgptools/models/core/transformations.py</code> <pre><code>class SquareTransform(Transform):\n    \"\"\"Non-point Transform to model a square FoV. Only works for single robot cases. \n\n    Args:\n        length (float): Length of the square FoV\n        num_side (int): Number of points along each side of the FoV\n        aggregate_fov (bool): If `True`, covariances corresponding to interpolated inducing points used to \n                              approximate the sensor FoV are aggregated to reduce the matrix inversion cost\n    \"\"\"\n    def __init__(self, length, num_side, aggregate_fov=False, **kwargs):\n        super().__init__(**kwargs)\n        self.length = length\n        self.num_side = num_side\n        self.length_factor=length/(self.num_side)\n        self.num_length = int(length/self.length_factor)\n\n        if aggregate_fov:\n            self.enable_aggregation()\n\n    def enable_aggregation(self, size=None):\n        \"\"\"Enable FoV covariance aggregation, which reduces the covariance matrix inversion cost by reducing the \n        covariance matrix size.\n\n        Args:\n            size (int): If None, all the interpolated inducing points within the FoV are aggregated. Alternatively, \n                        the number of inducing points to aggregate can be explicitly defined using this variable.\n        \"\"\"\n        if size is None:\n            self.aggregation_size = self.num_side**2\n        else:\n            self.aggregation_size = size\n\n    def expand(self, Xu):\n        \"\"\"Applies the expansion transformation to the inducing points\n\n        Args:\n            Xu (ndarray): (m, 3); Inducing points in the position and orientation space.\n                          `m` is the number of inducing points,\n                          `3` is the dimension of the space (x, y, angle in radians)\n\n        Returns:\n            Xu (ndarray): (mp, 2); Inducing points in input space.\n                        `p` is the number of points each inducing point is mapped \n                         to in order to form the FoV.\n        \"\"\"\n        x, y, theta = tf.split(Xu, num_or_size_splits=3, axis=1)\n        x     = tf.reshape(x, [-1,])\n        y     = tf.reshape(y, [-1,])\n        theta = tf.reshape(theta, [-1,])\n\n        points = []\n        for i in range(-int(np.floor((self.num_side)/2)), int(np.ceil((self.num_side)/2))):\n            points.append(tf.linspace([(x + (i * self.length_factor) * tf.cos(theta)) - self.length/2 * tf.cos(theta+np.pi/2), \n                                       (y + (i * self.length_factor) * tf.sin(theta)) - self.length/2 * tf.sin(theta+np.pi/2)], \n                                      [(x + (i * self.length_factor) * tf.cos(theta)) + self.length/2 * tf.cos(theta+np.pi/2), \n                                       (y + (i * self.length_factor) * tf.sin(theta)) + self.length/2 * tf.sin(theta+np.pi/2)], \n                                      self.num_side, axis=1))\n        xy = tf.concat(points, axis=1)\n        xy = tf.transpose(xy, [2, 1, 0])\n        xy = tf.reshape(xy, (-1, 2))\n        return xy\n\n    def distance(self, Xu):\n        \"\"\"Computes the distance incured by sequentially visiting the inducing points\n        Args:\n            Xu (ndarray): (m, 3); Inducing points from which to compute the path lengths.\n                          `m` is the number of inducing points.\n\n        Returns:\n            dist (float): path lengths\n        \"\"\"\n        Xu = tf.reshape(Xu, (-1, 3))[:, :2]\n        dist = tf.norm(Xu[1:] - Xu[:-1], axis=-1)\n        dist = tf.reduce_sum(dist, axis=0)\n        return dist\n</code></pre>"},{"location":"api/models/core/Transformations.html#sgptools.models.core.transformations.SquareTransform.distance","title":"<code>distance(Xu)</code>","text":"<p>Computes the distance incured by sequentially visiting the inducing points Args:     Xu (ndarray): (m, 3); Inducing points from which to compute the path lengths.                   <code>m</code> is the number of inducing points.</p> <p>Returns:</p> Name Type Description <code>dist</code> <code>float</code> <p>path lengths</p> Source code in <code>sgptools/models/core/transformations.py</code> <pre><code>def distance(self, Xu):\n    \"\"\"Computes the distance incured by sequentially visiting the inducing points\n    Args:\n        Xu (ndarray): (m, 3); Inducing points from which to compute the path lengths.\n                      `m` is the number of inducing points.\n\n    Returns:\n        dist (float): path lengths\n    \"\"\"\n    Xu = tf.reshape(Xu, (-1, 3))[:, :2]\n    dist = tf.norm(Xu[1:] - Xu[:-1], axis=-1)\n    dist = tf.reduce_sum(dist, axis=0)\n    return dist\n</code></pre>"},{"location":"api/models/core/Transformations.html#sgptools.models.core.transformations.SquareTransform.enable_aggregation","title":"<code>enable_aggregation(size=None)</code>","text":"<p>Enable FoV covariance aggregation, which reduces the covariance matrix inversion cost by reducing the  covariance matrix size.</p> <p>Parameters:</p> Name Type Description Default <code>size</code> <code>int</code> <p>If None, all the interpolated inducing points within the FoV are aggregated. Alternatively,          the number of inducing points to aggregate can be explicitly defined using this variable.</p> <code>None</code> Source code in <code>sgptools/models/core/transformations.py</code> <pre><code>def enable_aggregation(self, size=None):\n    \"\"\"Enable FoV covariance aggregation, which reduces the covariance matrix inversion cost by reducing the \n    covariance matrix size.\n\n    Args:\n        size (int): If None, all the interpolated inducing points within the FoV are aggregated. Alternatively, \n                    the number of inducing points to aggregate can be explicitly defined using this variable.\n    \"\"\"\n    if size is None:\n        self.aggregation_size = self.num_side**2\n    else:\n        self.aggregation_size = size\n</code></pre>"},{"location":"api/models/core/Transformations.html#sgptools.models.core.transformations.SquareTransform.expand","title":"<code>expand(Xu)</code>","text":"<p>Applies the expansion transformation to the inducing points</p> <p>Parameters:</p> Name Type Description Default <code>Xu</code> <code>ndarray</code> <p>(m, 3); Inducing points in the position and orientation space.           <code>m</code> is the number of inducing points,           <code>3</code> is the dimension of the space (x, y, angle in radians)</p> required <p>Returns:</p> Name Type Description <code>Xu</code> <code>ndarray</code> <p>(mp, 2); Inducing points in input space.         <code>p</code> is the number of points each inducing point is mapped           to in order to form the FoV.</p> Source code in <code>sgptools/models/core/transformations.py</code> <pre><code>def expand(self, Xu):\n    \"\"\"Applies the expansion transformation to the inducing points\n\n    Args:\n        Xu (ndarray): (m, 3); Inducing points in the position and orientation space.\n                      `m` is the number of inducing points,\n                      `3` is the dimension of the space (x, y, angle in radians)\n\n    Returns:\n        Xu (ndarray): (mp, 2); Inducing points in input space.\n                    `p` is the number of points each inducing point is mapped \n                     to in order to form the FoV.\n    \"\"\"\n    x, y, theta = tf.split(Xu, num_or_size_splits=3, axis=1)\n    x     = tf.reshape(x, [-1,])\n    y     = tf.reshape(y, [-1,])\n    theta = tf.reshape(theta, [-1,])\n\n    points = []\n    for i in range(-int(np.floor((self.num_side)/2)), int(np.ceil((self.num_side)/2))):\n        points.append(tf.linspace([(x + (i * self.length_factor) * tf.cos(theta)) - self.length/2 * tf.cos(theta+np.pi/2), \n                                   (y + (i * self.length_factor) * tf.sin(theta)) - self.length/2 * tf.sin(theta+np.pi/2)], \n                                  [(x + (i * self.length_factor) * tf.cos(theta)) + self.length/2 * tf.cos(theta+np.pi/2), \n                                   (y + (i * self.length_factor) * tf.sin(theta)) + self.length/2 * tf.sin(theta+np.pi/2)], \n                                  self.num_side, axis=1))\n    xy = tf.concat(points, axis=1)\n    xy = tf.transpose(xy, [2, 1, 0])\n    xy = tf.reshape(xy, (-1, 2))\n    return xy\n</code></pre>"},{"location":"api/models/core/Transformations.html#sgptools.models.core.transformations.Transform","title":"<code>Transform</code>","text":"<p>Base class for transformations of the inducing points, including expansion and aggregation transforms.</p> Refer to the following papers for more details <ul> <li>Efficient Sensor Placement from Regression with Sparse Gaussian Processes in Continuous and Discrete Spaces [Jakkala and Akella, 2023]</li> <li>Multi-Robot Informative Path Planning from Regression with Sparse Gaussian Processes [Jakkala and Akella, 2024]</li> </ul> <p>Parameters:</p> Name Type Description Default <code>aggregation_size</code> <code>int</code> <p>Number of consecutive inducing points to aggregate</p> <code>None</code> <code>constraint_weight</code> <code>float</code> <p>Weight term that controls the importance of the                         constraint terms in the SGP's optimization objective</p> <code>1.0</code> Source code in <code>sgptools/models/core/transformations.py</code> <pre><code>class Transform:\n    \"\"\"Base class for transformations of the inducing points, including expansion and aggregation transforms.\n\n    Refer to the following papers for more details:\n        - Efficient Sensor Placement from Regression with Sparse Gaussian Processes in Continuous and Discrete Spaces [Jakkala and Akella, 2023]\n        - Multi-Robot Informative Path Planning from Regression with Sparse Gaussian Processes [Jakkala and Akella, 2024]\n\n    Args:\n        aggregation_size (int): Number of consecutive inducing points to aggregate\n        constraint_weight (float): Weight term that controls the importance of the \n                                   constraint terms in the SGP's optimization objective \n    \"\"\"\n    def __init__(self, \n                 aggregation_size=None, \n                 constraint_weight=1.0,\n                 **kwargs):\n        self.aggregation_size = aggregation_size\n        self.constraint_weight = constraint_weight\n\n    def expand(self, Xu):\n        \"\"\"Applies the expansion transform to the inducing points\n\n        Args:\n            Xu (ndarray): Expansion transformed inducing points\n        \"\"\"\n        return Xu\n\n    def aggregate(self, k):\n        \"\"\"Applies the aggregation transform to kernel matrices\n\n        Args:\n            k (tensor): (mp, mp)/(mp, n); Kernel matrix. \n                        `m` is the number of inducing points,\n                        `p` is the number of points each inducing point is mapped,\n                        `n` is the number of training data points.\n\n        Returns:\n            k (tensor): (m, m)/(m, n); Aggregated kernel matrix\n        \"\"\"\n        if self.aggregation_size is None:\n            return k\n\n        if k.shape[0] == k.shape[1]:\n            # Handle Kuu which is a square matrix\n            k = tf.expand_dims(tf.expand_dims(k, axis=0), axis=-1)\n            k = tf.nn.avg_pool(k,\n                               ksize=[1, self.aggregation_size, self.aggregation_size, 1],\n                               strides=[1, self.aggregation_size, self.aggregation_size, 1],\n                               padding='VALID')\n            k = tf.squeeze(k, axis=[0, -1])\n        else:\n            # Handle Kuf which is a rectangular matrix\n            k = tf.expand_dims(k, axis=0)\n            k = tf.nn.avg_pool(k,\n                               ksize=[1, self.aggregation_size, 1],\n                               strides=[1, self.aggregation_size, 1],\n                               padding='VALID')\n            k = tf.squeeze(k, axis=[0])\n        return k\n\n    def constraints(self, Xu):\n        \"\"\"Computes the constraint terms that are added to the SGP's optimization function\n\n        Args:\n            Xu (ndarray): Inducing points from which to compute the constraints\n\n        Returns:\n            c (float): constraint terms (eg., distance constraint)\n        \"\"\"\n        return 0.\n</code></pre>"},{"location":"api/models/core/Transformations.html#sgptools.models.core.transformations.Transform.aggregate","title":"<code>aggregate(k)</code>","text":"<p>Applies the aggregation transform to kernel matrices</p> <p>Parameters:</p> Name Type Description Default <code>k</code> <code>tensor</code> <p>(mp, mp)/(mp, n); Kernel matrix.          <code>m</code> is the number of inducing points,         <code>p</code> is the number of points each inducing point is mapped,         <code>n</code> is the number of training data points.</p> required <p>Returns:</p> Name Type Description <code>k</code> <code>tensor</code> <p>(m, m)/(m, n); Aggregated kernel matrix</p> Source code in <code>sgptools/models/core/transformations.py</code> <pre><code>def aggregate(self, k):\n    \"\"\"Applies the aggregation transform to kernel matrices\n\n    Args:\n        k (tensor): (mp, mp)/(mp, n); Kernel matrix. \n                    `m` is the number of inducing points,\n                    `p` is the number of points each inducing point is mapped,\n                    `n` is the number of training data points.\n\n    Returns:\n        k (tensor): (m, m)/(m, n); Aggregated kernel matrix\n    \"\"\"\n    if self.aggregation_size is None:\n        return k\n\n    if k.shape[0] == k.shape[1]:\n        # Handle Kuu which is a square matrix\n        k = tf.expand_dims(tf.expand_dims(k, axis=0), axis=-1)\n        k = tf.nn.avg_pool(k,\n                           ksize=[1, self.aggregation_size, self.aggregation_size, 1],\n                           strides=[1, self.aggregation_size, self.aggregation_size, 1],\n                           padding='VALID')\n        k = tf.squeeze(k, axis=[0, -1])\n    else:\n        # Handle Kuf which is a rectangular matrix\n        k = tf.expand_dims(k, axis=0)\n        k = tf.nn.avg_pool(k,\n                           ksize=[1, self.aggregation_size, 1],\n                           strides=[1, self.aggregation_size, 1],\n                           padding='VALID')\n        k = tf.squeeze(k, axis=[0])\n    return k\n</code></pre>"},{"location":"api/models/core/Transformations.html#sgptools.models.core.transformations.Transform.constraints","title":"<code>constraints(Xu)</code>","text":"<p>Computes the constraint terms that are added to the SGP's optimization function</p> <p>Parameters:</p> Name Type Description Default <code>Xu</code> <code>ndarray</code> <p>Inducing points from which to compute the constraints</p> required <p>Returns:</p> Name Type Description <code>c</code> <code>float</code> <p>constraint terms (eg., distance constraint)</p> Source code in <code>sgptools/models/core/transformations.py</code> <pre><code>def constraints(self, Xu):\n    \"\"\"Computes the constraint terms that are added to the SGP's optimization function\n\n    Args:\n        Xu (ndarray): Inducing points from which to compute the constraints\n\n    Returns:\n        c (float): constraint terms (eg., distance constraint)\n    \"\"\"\n    return 0.\n</code></pre>"},{"location":"api/models/core/Transformations.html#sgptools.models.core.transformations.Transform.expand","title":"<code>expand(Xu)</code>","text":"<p>Applies the expansion transform to the inducing points</p> <p>Parameters:</p> Name Type Description Default <code>Xu</code> <code>ndarray</code> <p>Expansion transformed inducing points</p> required Source code in <code>sgptools/models/core/transformations.py</code> <pre><code>def expand(self, Xu):\n    \"\"\"Applies the expansion transform to the inducing points\n\n    Args:\n        Xu (ndarray): Expansion transformed inducing points\n    \"\"\"\n    return Xu\n</code></pre>"},{"location":"api/utils/index.html","title":"","text":""},{"location":"api/utils/index.html#sgptools.utils","title":"<code>sgptools.utils</code>","text":"<p>General utilities to support the functionalities of this package:</p> <ul> <li><code>data</code>: Provides utilities to preprocess datasets</li> <li><code>gpflow</code>: Provides utilities to interface with GPflow</li> <li><code>metrics</code>: Provides utilities to quantify the solution quality</li> <li><code>misc</code>: Provides miscellaneous helper functions</li> <li><code>tsp</code>: Provides utilities to run TSP/VRP solver</li> </ul>"},{"location":"api/utils/Data.html","title":"Data","text":""},{"location":"api/utils/Data.html#sgptools.utils.data","title":"<code>sgptools.utils.data</code>","text":""},{"location":"api/utils/Data.html#sgptools.utils.data.get_dataset","title":"<code>get_dataset(dataset_path=None, num_train=1000, num_test=2500, num_candidates=150, **kwargs)</code>","text":"<p>Method to generate/load datasets and preprocess them for SP/IPP. The method uses kmeans to  generate train and test sets.</p> <p>Parameters:</p> Name Type Description Default <code>dataset_path</code> <code>str</code> <p>Path to a tif dataset file. If None, the method will generate synthetic data.</p> <code>None</code> <code>num_train</code> <code>int</code> <p>Number of training samples to generate.</p> <code>1000</code> <code>num_test</code> <code>int</code> <p>Number of testing samples to generate.</p> <code>2500</code> <code>num_candidates</code> <code>int</code> <p>Number of candidate locations to generate.</p> <code>150</code> <p>Returns:</p> Name Type Description <code>X_train</code> <code>ndarray</code> <p>(n, d); Training set inputs</p> <code>y_train</code> <code>ndarray</code> <p>(n, 1); Training set labels</p> <code>X_test</code> <code>ndarray</code> <p>(n, d); Testing set inputs</p> <code>y_test</code> <code>ndarray</code> <p>(n, 1); Testing set labels</p> <code>candidates</code> <code>ndarray</code> <p>(n, d); Candidate sensor placement locations</p> <code>X</code> <code>ndarray</code> <p>(n, d); Full dataset inputs</p> <code>y</code> <code>ndarray</code> <p>(n, 1); Full dataset labels</p> Source code in <code>sgptools/utils/data.py</code> <pre><code>def get_dataset(dataset_path=None,\n                num_train=1000,\n                num_test=2500, \n                num_candidates=150,\n                **kwargs):\n    \"\"\"Method to generate/load datasets and preprocess them for SP/IPP. The method uses kmeans to \n    generate train and test sets.\n\n    Args:\n        dataset_path (str): Path to a tif dataset file. If None, the method will generate synthetic data.\n        num_train (int): Number of training samples to generate.\n        num_test (int): Number of testing samples to generate.\n        num_candidates (int): Number of candidate locations to generate.\n\n    Returns:\n       X_train (ndarray): (n, d); Training set inputs\n       y_train (ndarray): (n, 1); Training set labels\n       X_test (ndarray): (n, d); Testing set inputs\n       y_test (ndarray): (n, 1); Testing set labels\n       candidates (ndarray): (n, d); Candidate sensor placement locations\n       X (ndarray): (n, d); Full dataset inputs\n       y (ndarray): (n, 1); Full dataset labels\n    \"\"\"\n    # Load the data\n    if dataset_path is not None:\n        X, y = prep_tif_dataset(dataset_path=dataset_path)\n    else:\n        X, y = prep_synthetic_dataset(**kwargs)\n\n    X_train = get_inducing_pts(X, num_train)\n    X_train, y_train = cont2disc(X_train, X, y)\n\n    X_test = get_inducing_pts(X, num_test)\n    X_test, y_test = cont2disc(X_test, X, y)\n\n    candidates = get_inducing_pts(X, num_candidates)\n    candidates = cont2disc(candidates, X)\n\n    # Standardize data\n    X_scaler = StandardScaler()\n    X_scaler.fit(X_train)\n    X_train = X_scaler.transform(X_train)*10.0\n    X_test = X_scaler.transform(X_test)*10.0\n    X = X_scaler.transform(X)*10.0\n    candidates = X_scaler.transform(candidates)*10.0\n\n    y_scaler = StandardScaler()\n    y_scaler.fit(y_train)\n    y_train = y_scaler.transform(y_train)\n    y_test = y_scaler.transform(y_test)\n    y = y_scaler.transform(y)\n\n    return X_train, y_train, X_test, y_test, candidates, X, y\n</code></pre>"},{"location":"api/utils/Data.html#sgptools.utils.data.point_pos","title":"<code>point_pos(point, d, theta)</code>","text":"<p>Generate a point at a distance d from a point at angle theta.</p> <p>Parameters:</p> Name Type Description Default <code>point</code> <code>ndarray</code> <p>(N, 2); array of points</p> required <code>d</code> <code>float</code> <p>distance</p> required <code>theta</code> <code>float</code> <p>angle in radians</p> required <p>Returns:</p> Name Type Description <code>X</code> <code>ndarray</code> <p>(N,); array of x-coordinate</p> <code>Y</code> <code>ndarray</code> <p>(N,); array of y-coordinate</p> Source code in <code>sgptools/utils/data.py</code> <pre><code>def point_pos(point, d, theta):\n    '''\n    Generate a point at a distance d from a point at angle theta.\n\n    Args:\n        point (ndarray): (N, 2); array of points\n        d (float): distance\n        theta (float): angle in radians\n\n    Returns:\n        X  (ndarray): (N,); array of x-coordinate\n        Y  (ndarray): (N,); array of y-coordinate\n    '''\n    return np.c_[point[:, 0] + d*np.cos(theta), point[:, 1] + d*np.sin(theta)]\n</code></pre>"},{"location":"api/utils/Data.html#sgptools.utils.data.prep_synthetic_dataset","title":"<code>prep_synthetic_dataset(shape=(50, 50), min_height=0.0, max_height=30.0, roughness=0.5, random_seed=None, **kwargs)</code>","text":"<p>Generates a 50x50 grid of synthetic elevation data using the diamond square algorithm.</p> Refer to the following repo for more details <ul> <li>https://github.com/buckinha/DiamondSquare</li> </ul> <p>Parameters:</p> Name Type Description Default <code>shape</code> <code>tuple</code> <p>(x, y); Grid size along the x and y axis</p> <code>(50, 50)</code> <code>min_height</code> <code>float</code> <p>Minimum allowed height in the sampled data</p> <code>0.0</code> <code>max_height</code> <code>float</code> <p>Maximum allowed height in the sampled data</p> <code>30.0</code> <code>roughness</code> <code>float</code> <p>Roughness of the sampled data</p> <code>0.5</code> <code>random_seed</code> <code>int</code> <p>Random seed for reproducibility</p> <code>None</code> <p>Returns:</p> Name Type Description <code>X</code> <code>ndarray</code> <p>(n, d); Dataset input features</p> <code>y</code> <code>ndarray</code> <p>(n, 1); Dataset labels</p> Source code in <code>sgptools/utils/data.py</code> <pre><code>def prep_synthetic_dataset(shape=(50, 50), \n                           min_height=0.0, \n                           max_height=30.0, \n                           roughness=0.5,\n                           random_seed=None,\n                           **kwargs):\n    '''Generates a 50x50 grid of synthetic elevation data using the diamond square algorithm.\n\n    Refer to the following repo for more details:\n        - [https://github.com/buckinha/DiamondSquare](https://github.com/buckinha/DiamondSquare)\n\n    Args:\n        shape (tuple): (x, y); Grid size along the x and y axis\n        min_height (float): Minimum allowed height in the sampled data\n        max_height (float): Maximum allowed height in the sampled data\n        roughness (float): Roughness of the sampled data\n        random_seed (int): Random seed for reproducibility\n\n    Returns:\n       X (ndarray): (n, d); Dataset input features\n       y (ndarray): (n, 1); Dataset labels\n    '''\n    data = diamond_square(shape=shape,\n                          min_height=min_height, \n                          max_height=max_height, \n                          roughness=roughness,\n                          random_seed=random_seed,\n                          **kwargs)\n\n    # create x and y coordinates from the extent\n    x_coords = np.arange(0, data.shape[0])/10\n    y_coords = np.arange(0, data.shape[1])/10\n    xx, yy = np.meshgrid(x_coords, y_coords)\n    X = np.c_[xx.ravel(), yy.ravel()]\n    y = data.ravel()\n    y = y.reshape(-1, 1)\n\n    return X.astype(float), y.astype(float)\n</code></pre>"},{"location":"api/utils/Data.html#sgptools.utils.data.prep_tif_dataset","title":"<code>prep_tif_dataset(dataset_path)</code>","text":"<p>Load and preprocess a dataset from a GeoTIFF file (.tif file). The input features  are set to the x and y pixel block coordinates and the labels are read from the file. The method also removes all invalid points.</p> <p>Large tif files  need to be downsampled using the following command:  <code>gdalwarp -tr 50 50 &lt;input&gt;.tif &lt;output&gt;.tif</code></p> <p>Args:     dataset_path (str): Path to the dataset file, used only when dataset_type is 'tif'.</p> <p>Returns:    X (ndarray): (n, d); Dataset input features    y (ndarray): (n, 1); Dataset labels</p> Source code in <code>sgptools/utils/data.py</code> <pre><code>def prep_tif_dataset(dataset_path):\n    '''Load and preprocess a dataset from a GeoTIFF file (.tif file). The input features \n    are set to the x and y pixel block coordinates and the labels are read from the file.\n    The method also removes all invalid points.\n\n    Large tif files \n    need to be downsampled using the following command: \n    ```gdalwarp -tr 50 50 &lt;input&gt;.tif &lt;output&gt;.tif```\n\n    Args:\n        dataset_path (str): Path to the dataset file, used only when dataset_type is 'tif'.\n\n    Returns:\n       X (ndarray): (n, d); Dataset input features\n       y (ndarray): (n, 1); Dataset labels\n    '''\n    data = PIL.Image.open(dataset_path)\n    data = np.array(data)\n\n    # create x and y coordinates from the extent\n    x_coords = np.arange(0, data.shape[1])/10\n    y_coords = np.arange(data.shape[0], 0, -1)/10\n    xx, yy = np.meshgrid(x_coords, y_coords)\n    X = np.c_[xx.ravel(), yy.ravel()]\n    y = data.ravel()\n\n    # Remove invalid labels\n    y[np.where(y==-999999.0)] = np.nan\n    X = X[~np.isnan(y)]\n    y = y[~np.isnan(y)]\n\n    X = X.reshape(-1, 2)\n    y = y.reshape(-1, 1)\n\n    return X.astype(float), y.astype(float)\n</code></pre>"},{"location":"api/utils/Data.html#sgptools.utils.data.remove_circle_patches","title":"<code>remove_circle_patches(X, Y, circle_patches)</code>","text":"<p>Remove points inside polycircle patchesgons.</p> <p>Parameters:</p> Name Type Description Default <code>X</code> <code> (ndarray</code> <p>(N,); array of x-coordinate</p> required <code>Y</code> <code> (ndarray</code> <p>(N,); array of y-coordinate</p> required <code>circle_patches</code> <code>list of matplotlib circle patches</code> <p>Circle patches to remove from the X, Y points</p> required <p>Returns:</p> Name Type Description <code>X</code> <code>ndarray</code> <p>(N,); array of x-coordinate</p> <code>Y</code> <code>ndarray</code> <p>(N,); array of y-coordinate</p> Source code in <code>sgptools/utils/data.py</code> <pre><code>def remove_circle_patches(X, Y, circle_patches):\n    '''\n    Remove points inside polycircle patchesgons.\n\n    Args:\n        X  (ndarray): (N,); array of x-coordinate\n        Y  (ndarray): (N,); array of y-coordinate\n        circle_patches (list of matplotlib circle patches): Circle patches to remove from the X, Y points\n\n    Returns:\n        X  (ndarray): (N,); array of x-coordinate\n        Y  (ndarray): (N,); array of y-coordinate\n    '''\n    points = np.array([X.flatten(), Y.flatten()]).T\n    for circle_patch in circle_patches:\n        points = points[~circle_patch.contains_points(points)]\n    return points[:, 0], points[:, 1]\n</code></pre>"},{"location":"api/utils/Data.html#sgptools.utils.data.remove_polygons","title":"<code>remove_polygons(X, Y, polygons)</code>","text":"<p>Remove points inside polygons.</p> <p>Parameters:</p> Name Type Description Default <code>X</code> <code> (ndarray</code> <p>(N,); array of x-coordinate</p> required <code>Y</code> <code> (ndarray</code> <p>(N,); array of y-coordinate</p> required <code>polygons</code> <code>list of matplotlib path polygon</code> <p>Polygons to remove from the X, Y points</p> required <p>Returns:</p> Name Type Description <code>X</code> <code>ndarray</code> <p>(N,); array of x-coordinate</p> <code>Y</code> <code>ndarray</code> <p>(N,); array of y-coordinate</p> Source code in <code>sgptools/utils/data.py</code> <pre><code>def remove_polygons(X, Y, polygons):\n    '''\n    Remove points inside polygons.\n\n    Args:\n        X  (ndarray): (N,); array of x-coordinate\n        Y  (ndarray): (N,); array of y-coordinate\n        polygons (list of matplotlib path polygon): Polygons to remove from the X, Y points\n\n    Returns:\n        X  (ndarray): (N,); array of x-coordinate\n        Y  (ndarray): (N,); array of y-coordinate\n    '''\n    points = np.array([X.flatten(), Y.flatten()]).T\n    for polygon in polygons:\n        p = path.Path(polygon)\n        points = points[~p.contains_points(points)]\n    return points[:, 0], points[:, 1]\n</code></pre>"},{"location":"api/utils/GPflow.html","title":"GPflow","text":""},{"location":"api/utils/GPflow.html#sgptools.utils.gpflow","title":"<code>sgptools.utils.gpflow</code>","text":""},{"location":"api/utils/GPflow.html#sgptools.utils.gpflow.TraceInducingPts","title":"<code>TraceInducingPts</code>","text":"<p>               Bases: <code>MonitorTask</code></p> <p>GPflow monitoring task, used to trace the inducing points states at every step during optimization. </p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>sgpr</code> <p>GPflow GP/SGP model</p> required Source code in <code>sgptools/utils/gpflow.py</code> <pre><code>class TraceInducingPts(gpflow.monitor.MonitorTask):\n    '''\n    GPflow monitoring task, used to trace the inducing points\n    states at every step during optimization. \n\n    Args:\n        model (gpflow.models.sgpr): GPflow GP/SGP model\n    '''\n    def __init__(self, model):\n        super().__init__()\n        self.trace = []\n        self.model = model\n\n    def run(self, **kwargs):\n        '''\n        Method used to extract the inducing points and \n        apply IPP fixed points transform if available\n        '''\n        Xu = self.model.inducing_variable.Z\n        Xu_exp = self.model.transform.expand(Xu, \n                            expand_sensor_model=False).numpy()\n        self.trace.append(Xu_exp)\n\n    def get_trace(self):\n        '''\n        Returns the inducing points collected at each optimization step\n\n        Returns:\n            trace (ndarray): (n, m, d); Array with the inducing points.\n                            `n` is the number of optimization steps;\n                            `m` is the number of inducing points;\n                            `d` is the dimension of the inducing points.\n        '''\n        return np.array(self.trace)\n</code></pre>"},{"location":"api/utils/GPflow.html#sgptools.utils.gpflow.TraceInducingPts.get_trace","title":"<code>get_trace()</code>","text":"<p>Returns the inducing points collected at each optimization step</p> <p>Returns:</p> Name Type Description <code>trace</code> <code>ndarray</code> <p>(n, m, d); Array with the inducing points.             <code>n</code> is the number of optimization steps;             <code>m</code> is the number of inducing points;             <code>d</code> is the dimension of the inducing points.</p> Source code in <code>sgptools/utils/gpflow.py</code> <pre><code>def get_trace(self):\n    '''\n    Returns the inducing points collected at each optimization step\n\n    Returns:\n        trace (ndarray): (n, m, d); Array with the inducing points.\n                        `n` is the number of optimization steps;\n                        `m` is the number of inducing points;\n                        `d` is the dimension of the inducing points.\n    '''\n    return np.array(self.trace)\n</code></pre>"},{"location":"api/utils/GPflow.html#sgptools.utils.gpflow.TraceInducingPts.run","title":"<code>run(**kwargs)</code>","text":"<p>Method used to extract the inducing points and  apply IPP fixed points transform if available</p> Source code in <code>sgptools/utils/gpflow.py</code> <pre><code>def run(self, **kwargs):\n    '''\n    Method used to extract the inducing points and \n    apply IPP fixed points transform if available\n    '''\n    Xu = self.model.inducing_variable.Z\n    Xu_exp = self.model.transform.expand(Xu, \n                        expand_sensor_model=False).numpy()\n    self.trace.append(Xu_exp)\n</code></pre>"},{"location":"api/utils/GPflow.html#sgptools.utils.gpflow.get_model_params","title":"<code>get_model_params(X_train, y_train, max_steps=1500, lr=0.01, print_params=True, lengthscales=1.0, variance=1.0, noise_variance=0.1, kernel=None, return_gp=False, train_inducing_pts=False, num_inducing_pts=500, **kwargs)</code>","text":"<p>Train a GP on the given training set.  Trains a sparse GP if the training set is larger than 1000 samples.</p> <p>Parameters:</p> Name Type Description Default <code>X_train</code> <code>ndarray</code> <p>(n, d); Training set inputs</p> required <code>y_train</code> <code>ndarray</code> <p>(n, 1); Training set labels</p> required <code>max_steps</code> <code>int</code> <p>Maximum number of optimization steps</p> <code>1500</code> <code>lr</code> <code>float</code> <p>Optimization learning rate</p> <code>0.01</code> <code>print_params</code> <code>bool</code> <p>If True, prints the optimized GP parameters</p> <code>True</code> <code>lengthscales</code> <code>float or list</code> <p>Kernel lengthscale(s), if passed as a list,                      each element corresponds to each data dimension</p> <code>1.0</code> <code>variance</code> <code>float</code> <p>Kernel variance</p> <code>1.0</code> <code>noise_variance</code> <code>float</code> <p>Data noise variance</p> <code>0.1</code> <code>kernel</code> <code>Kernel</code> <p>gpflow kernel function</p> <code>None</code> <code>return_gp</code> <code>bool</code> <p>If True, returns the trained GP model</p> <code>False</code> <code>train_inducing_pts</code> <code>bool</code> <p>If True, trains the inducing points when                        using a sparse GP model</p> <code>False</code> <code>num_inducing_pts</code> <code>int</code> <p>Number of inducing points to use when training                     a sparse GP model</p> <code>500</code> <p>Returns:</p> Name Type Description <code>loss</code> <code>list</code> <p>Loss values obtained during training</p> <code>variance</code> <code>float</code> <p>Optimized data noise variance</p> <code>kernel</code> <code>Kernel</code> <p>Optimized gpflow kernel function</p> <code>gp</code> <code>GPR</code> <p>Optimized gpflow GP model.                      Returned only if <code>return_gp=True</code>.</p> Source code in <code>sgptools/utils/gpflow.py</code> <pre><code>def get_model_params(X_train, y_train, \n                     max_steps=1500, \n                     lr=1e-2, \n                     print_params=True, \n                     lengthscales=1.0, \n                     variance=1.0, \n                     noise_variance=0.1,\n                     kernel=None,\n                     return_gp=False,\n                     train_inducing_pts=False,\n                     num_inducing_pts=500,\n                     **kwargs):\n    \"\"\"Train a GP on the given training set. \n    Trains a sparse GP if the training set is larger than 1000 samples.\n\n    Args:\n        X_train (ndarray): (n, d); Training set inputs\n        y_train (ndarray): (n, 1); Training set labels\n        max_steps (int): Maximum number of optimization steps\n        lr (float): Optimization learning rate\n        print_params (bool): If True, prints the optimized GP parameters\n        lengthscales (float or list): Kernel lengthscale(s), if passed as a list, \n                                each element corresponds to each data dimension\n        variance (float): Kernel variance\n        noise_variance (float): Data noise variance\n        kernel (gpflow.kernels.Kernel): gpflow kernel function\n        return_gp (bool): If True, returns the trained GP model\n        train_inducing_pts (bool): If True, trains the inducing points when\n                                   using a sparse GP model\n        num_inducing_pts (int): Number of inducing points to use when training\n                                a sparse GP model\n\n    Returns:\n        loss (list): Loss values obtained during training\n        variance (float): Optimized data noise variance\n        kernel (gpflow.kernels.Kernel): Optimized gpflow kernel function\n        gp (gpflow.models.GPR): Optimized gpflow GP model. \n                                Returned only if ```return_gp=True```.\n\n    \"\"\"\n    if kernel is None:\n        kernel = gpflow.kernels.SquaredExponential(lengthscales=lengthscales, \n                                                   variance=variance)\n\n    if len(X_train) &lt;= 1500:\n        gpr = gpflow.models.GPR(data=(X_train, y_train), \n                                kernel=kernel,\n                                noise_variance=noise_variance)\n        trainable_variables=gpr.trainable_variables\n    else:\n        inducing_pts = get_inducing_pts(X_train, num_inducing_pts)\n        gpr = gpflow.models.SGPR(data=(X_train, y_train), \n                                 kernel=kernel,\n                                 inducing_variable=inducing_pts,\n                                 noise_variance=noise_variance)\n        if train_inducing_pts:\n            trainable_variables=gpr.trainable_variables\n        else:\n            trainable_variables=gpr.trainable_variables[1:]\n\n    if max_steps &gt; 0:\n        loss = optimize_model(gpr, max_steps=max_steps, lr=lr, \n                              trainable_variables=trainable_variables,\n                              **kwargs)\n    else:\n        loss = 0\n\n    if print_params:\n        print_summary(gpr)\n\n    if return_gp:\n        return loss, gpr.likelihood.variance, kernel, gpr\n    else:\n        return loss, gpr.likelihood.variance, kernel\n</code></pre>"},{"location":"api/utils/GPflow.html#sgptools.utils.gpflow.optimize_model","title":"<code>optimize_model(model, max_steps=2000, kernel_grad=True, lr=0.01, optimizer='scipy', method=None, verbose=False, trace_fn=None, convergence_criterion=True, trainable_variables=None, tol=None)</code>","text":"<p>Trains a GP/SGP model</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>models</code> <p>GPflow GP/SGP model to train.</p> required <code>max_steps</code> <code>int</code> <p>Maximum number of training steps.</p> <code>2000</code> <code>kernel_grad</code> <code>bool</code> <p>If <code>False</code>, the kernel parameters will not be optimized.                  Ignored when <code>trainable_variables</code> are passed.</p> <code>True</code> <code>lr</code> <code>float</code> <p>Optimization learning rate.</p> <code>0.01</code> <code>optimizer</code> <code>str</code> <p>Optimizer to use for training (<code>scipy</code> or <code>tf</code>).</p> <code>'scipy'</code> <code>method</code> <code>str</code> <p>Optimization method refer to scipy minimize            and tf optimizers for full list</p> <code>None</code> <code>verbose</code> <code>bool</code> <p>If <code>True</code>, the training progress will be printed when using Scipy.</p> <code>False</code> <code>trace_fn</code> <code>str</code> <p>Function to trace metrics during training.              If <code>None</code>, the loss values are returned;             If <code>traceXu</code>, it the inducing points states at each optimization step are returned (increases computation time).</p> <code>None</code> <code>convergence_criterion</code> <code>bool</code> <p>If <code>True</code> and using a tensorflow optimizer, it                            enables early stopping when the loss plateaus.</p> <code>True</code> <code>trainable_variables</code> <code>list</code> <p>List of model variables to train.</p> <code>None</code> <code>tol</code> <code>float</code> <p>Convergence tolerance to decide when to stop optimization.</p> <code>None</code> Source code in <code>sgptools/utils/gpflow.py</code> <pre><code>def optimize_model(model, \n                   max_steps=2000, \n                   kernel_grad=True, \n                   lr=1e-2, \n                   optimizer='scipy', \n                   method=None,\n                   verbose=False, \n                   trace_fn=None,\n                   convergence_criterion=True,\n                   trainable_variables=None,\n                   tol=None):\n    \"\"\"\n    Trains a GP/SGP model\n\n    Args:\n        model (gpflow.models): GPflow GP/SGP model to train.\n        max_steps (int): Maximum number of training steps.\n        kernel_grad (bool): If `False`, the kernel parameters will not be optimized. \n                            Ignored when `trainable_variables` are passed.\n        lr (float): Optimization learning rate.\n        optimizer (str): Optimizer to use for training (`scipy` or `tf`).\n        method (str): Optimization method refer to [scipy minimize](https://docs.scipy.org/doc/scipy/reference/generated/scipy.optimize.minimize.html#scipy.optimize.minimize) \n                      and [tf optimizers](https://www.tensorflow.org/api_docs/python/tf/keras/optimizers) for full list\n        verbose (bool): If `True`, the training progress will be printed when using Scipy.\n        trace_fn (str): Function to trace metrics during training. \n                        If `None`, the loss values are returned;\n                        If `traceXu`, it the inducing points states at each optimization step are returned (increases computation time).\n        convergence_criterion (bool): If `True` and using a tensorflow optimizer, it \n                                      enables early stopping when the loss plateaus.\n        trainable_variables (list): List of model variables to train.\n        tol (float): Convergence tolerance to decide when to stop optimization.\n    \"\"\"\n    # Train all variables if trainable_variables are not provided\n    # If kernel_gradient is False, disable the kernel parameter gradient updates\n    if trainable_variables is None and kernel_grad:\n        trainable_variables=model.trainable_variables\n    elif trainable_variables is None and not kernel_grad:\n        trainable_variables=model.trainable_variables[:1]\n\n    if optimizer == 'scipy':\n        if method is None:\n            method = 'L-BFGS-B'\n\n        if trace_fn == 'traceXu':\n            execute_task = TraceInducingPts(model)\n            task_group = gpflow.monitor.MonitorTaskGroup(execute_task, \n                                                         period=1)\n            trace_fn = gpflow.monitor.Monitor(task_group)\n\n        opt = gpflow.optimizers.Scipy()\n        losses = opt.minimize(model.training_loss,\n                              trainable_variables,\n                              method=method,\n                              options=dict(disp=verbose, maxiter=max_steps),\n                              tol=tol,\n                              step_callback=trace_fn)\n        if trace_fn is None:\n            losses = losses.fun\n        else:\n            losses = trace_fn.task_groups[0].tasks[0].get_trace()\n    else:\n        if trace_fn is None:\n            trace_fn = lambda x: x.loss\n        elif trace_fn == 'traceXu':\n            def trace_fn(traceable_quantities):\n                return model.inducing_variable.Z.numpy()\n\n        if method is None:\n            method = 'adam'\n        opt = tf.keras.optimizers.get(method)\n        opt.learning_rate = lr\n        loss_fn = model.training_loss\n        if convergence_criterion:\n            convergence_criterion = tfp.optimizer.convergence_criteria.LossNotDecreasing(\n                                            atol=1e-5, \n                                            window_size=50,\n                                            min_num_steps=int(max_steps*0.1))\n        else:\n            convergence_criterion = None\n        losses = tfp.math.minimize(loss_fn,\n                                   trainable_variables=trainable_variables,\n                                   num_steps=max_steps,\n                                   optimizer=opt,\n                                   convergence_criterion=convergence_criterion,\n                                   trace_fn=trace_fn)\n        losses = losses.numpy()\n\n    return losses\n</code></pre>"},{"location":"api/utils/GPflow.html#sgptools.utils.gpflow.plot_loss","title":"<code>plot_loss(losses, save_file=None)</code>","text":"<p>Helper function to plot the training loss</p> <p>Parameters:</p> Name Type Description Default <code>losses</code> <code>list</code> <p>list of loss values</p> required <code>save_file</code> <code>str</code> <p>If passed, the loss plot will be saved to the <code>save_file</code></p> <code>None</code> Source code in <code>sgptools/utils/gpflow.py</code> <pre><code>def plot_loss(losses, save_file=None):\n    \"\"\"Helper function to plot the training loss\n\n    Args:\n        losses (list): list of loss values\n        save_file (str): If passed, the loss plot will be saved to the `save_file`\n    \"\"\"\n    plt.plot(losses)\n    plt.title('Log Likelihood')\n    plt.xlabel('Iteration')\n    plt.ylabel('Log Likelihood')\n    ax = plt.gca()\n    ax.ticklabel_format(useOffset=False)\n\n    if save_file is not None:\n        plt.savefig(save_file, bbox_inches='tight')\n        plt.close()\n    else:\n        plt.show()\n</code></pre>"},{"location":"api/utils/Metrics.html","title":"Metrics","text":""},{"location":"api/utils/Metrics.html#sgptools.utils.metrics","title":"<code>sgptools.utils.metrics</code>","text":""},{"location":"api/utils/Metrics.html#sgptools.utils.metrics.gaussian_entropy","title":"<code>gaussian_entropy(K)</code>","text":"<p>Computes GP-based entropy from a kernel matrix</p> <p>Parameters:</p> Name Type Description Default <code>K</code> <code>ndarray</code> <p>(n, n); kernel matrix</p> required <p>Returns:</p> Name Type Description <code>entropy</code> <code>float</code> <p>Entropy computed from the kernel matrix</p> Source code in <code>sgptools/utils/metrics.py</code> <pre><code>def gaussian_entropy(K):\n    \"\"\"Computes GP-based entropy from a kernel matrix\n\n    Args:\n        K (ndarray): (n, n); kernel matrix\n\n    Returns:\n        entropy (float): Entropy computed from the kernel matrix\n    \"\"\"\n    return multivariate_normal(mean=None, cov=K, allow_singular=True).entropy()\n</code></pre>"},{"location":"api/utils/Metrics.html#sgptools.utils.metrics.get_distance","title":"<code>get_distance(X)</code>","text":"<p>Compute the length of a path (L2-norm)</p> <p>Parameters:</p> Name Type Description Default <code>X</code> <code>ndarray</code> <p>(m, d); Waypoints of a path</p> required <p>Returns:</p> Name Type Description <code>dist</code> <code>float</code> <p>Total path length</p> Source code in <code>sgptools/utils/metrics.py</code> <pre><code>def get_distance(X):\n    \"\"\"Compute the length of a path (L2-norm)\n\n    Args:\n        X (ndarray): (m, d); Waypoints of a path\n\n    Returns:\n        dist (float): Total path length\n    \"\"\"\n    dist = np.linalg.norm(X[1:] - X[:-1], axis=-1)\n    dist = np.sum(dist)\n    return dist\n</code></pre>"},{"location":"api/utils/Metrics.html#sgptools.utils.metrics.get_elbo","title":"<code>get_elbo(Xu, X_env, noise_variance, kernel, baseline=False)</code>","text":"<p>Computes the ELBO of the SGP, corrected to be positive</p> <p>Parameters:</p> Name Type Description Default <code>Xu</code> <code>ndarray</code> <p>(m, d); Sensing locations</p> required <code>X_env</code> <code>ndarray</code> <p>(n, d); Data points used to approximate the bounds of the environment</p> required <code>noise_variance</code> <code>float</code> <p>data variance</p> required <code>kernel</code> <code>Kernel</code> <p>gpflow kernel function</p> required <code>baseline</code> <code>bool</code> <p>If True, the ELBO is adjusted to be positive</p> <code>False</code> <p>Returns:</p> Name Type Description <code>elbo</code> <code>float</code> <p>ELBO of the SGP</p> Source code in <code>sgptools/utils/metrics.py</code> <pre><code>def get_elbo(Xu, X_env, noise_variance, kernel, baseline=False):\n    \"\"\"Computes the ELBO of the SGP, corrected to be positive\n\n    Args:\n        Xu (ndarray): (m, d); Sensing locations\n        X_env (ndarray): (n, d); Data points used to approximate the bounds of the environment\n        noise_variance (float): data variance\n        kernel (gpflow.kernels.Kernel): gpflow kernel function\n        baseline (bool): If True, the ELBO is adjusted to be positive\n\n    Returns:\n        elbo (float): ELBO of the SGP\n    \"\"\"\n    if baseline:\n        sgpr = gpflow.models.SGPR(X_env,\n                                  noise_variance=noise_variance,\n                                  kernel=kernel,\n                                  inducing_variable=[[0, 0]])\n        baseline = sgpr.elbo().numpy()\n    else:\n        baseline = 0.0\n\n    sgpr = gpflow.models.SGPR(X_env,\n                              noise_variance=noise_variance,\n                              kernel=kernel, \n                              inducing_variable=Xu)\n    return (sgpr.elbo() - baseline).numpy()\n</code></pre>"},{"location":"api/utils/Metrics.html#sgptools.utils.metrics.get_kl","title":"<code>get_kl(Xu, X_env, noise_variance, kernel)</code>","text":"<p>Computes the KL divergence between the SGP and the GP</p> <p>Parameters:</p> Name Type Description Default <code>Xu</code> <code>ndarray</code> <p>(m, d); Sensing locations</p> required <code>X_env</code> <code>ndarray</code> <p>(n, d); Data points used to approximate the bounds of the environment</p> required <code>noise_variance</code> <code>float</code> <p>data variance</p> required <code>kernel</code> <code>Kernel</code> <p>gpflow kernel function</p> required <p>Returns:</p> Name Type Description <code>kl</code> <code>float</code> <p>KL divergence between the SGP and the GP</p> Source code in <code>sgptools/utils/metrics.py</code> <pre><code>def get_kl(Xu, X_env, noise_variance, kernel):\n    \"\"\"Computes the KL divergence between the SGP and the GP\n\n    Args:\n        Xu (ndarray): (m, d); Sensing locations\n        X_env (ndarray): (n, d); Data points used to approximate the bounds of the environment\n        noise_variance (float): data variance\n        kernel (gpflow.kernels.Kernel): gpflow kernel function\n\n    Returns:\n        kl (float): KL divergence between the SGP and the GP\n    \"\"\"\n    sgpr = gpflow.models.SGPR(X_env,\n                              noise_variance=noise_variance,\n                              kernel=kernel,\n                              inducing_variable=Xu)\n\n    common = sgpr._common_calculation()\n    sigma_sq = common.sigma_sq\n    AAT = common.AAT\n\n    x, _ = sgpr.data\n    kdiag = sgpr.kernel(x, full_cov=False)\n\n    # tr(K) / \u03c3\u00b2\n    trace_k = tf.reduce_sum(kdiag / sigma_sq)\n    # tr(Q) / \u03c3\u00b2\n    trace_q = tf.reduce_sum(tf.linalg.diag_part(AAT))\n    # tr(K - Q) / \u03c3\u00b2\n    trace = trace_k - trace_q\n    trace = 0.5 * trace\n\n    return float(trace.numpy())\n</code></pre>"},{"location":"api/utils/Metrics.html#sgptools.utils.metrics.get_mi","title":"<code>get_mi(Xu, candidate_locs, noise_variance, kernel)</code>","text":"<p>Computes mutual information between the sensing locations and the candidate locations</p> <p>Parameters:</p> Name Type Description Default <code>Xu</code> <code>ndarray</code> <p>(m, d); Sensing locations</p> required <code>candidate_locs</code> <code>ndarray</code> <p>(n, d); Candidate sensing locations </p> required <code>noise_variance</code> <code>float</code> <p>data variance</p> required <code>kernel</code> <code>Kernel</code> <p>gpflow kernel function</p> required <p>Returns:</p> Name Type Description <code>mi</code> <code>float</code> <p>Mutual information computed using a GP</p> Source code in <code>sgptools/utils/metrics.py</code> <pre><code>def get_mi(Xu, candidate_locs, noise_variance, kernel):\n    \"\"\"Computes mutual information between the sensing locations and the candidate locations\n\n    Args:\n        Xu (ndarray): (m, d); Sensing locations\n        candidate_locs (ndarray): (n, d); Candidate sensing locations \n        noise_variance (float): data variance\n        kernel (gpflow.kernels.Kernel): gpflow kernel function\n\n    Returns:\n        mi (float): Mutual information computed using a GP\n    \"\"\"\n    Xu = np.array(Xu)\n    candidate_locs = np.array(candidate_locs)\n\n    gp = gpflow.models.GPR(data=(Xu, np.zeros((len(Xu), 1))),\n                           kernel=kernel,\n                           noise_variance=noise_variance)\n    _, sigma_a = gp.predict_f(candidate_locs, full_cov=True)\n    sigma_a = sigma_a.numpy()[0]\n    cond_entropy = gaussian_entropy(sigma_a)\n\n    K = kernel(candidate_locs, full_cov=True).numpy()\n    K += noise_variance * np.eye(len(candidate_locs))\n    entropy = gaussian_entropy(K)\n\n    return float(entropy - cond_entropy)\n</code></pre>"},{"location":"api/utils/Metrics.html#sgptools.utils.metrics.get_reconstruction","title":"<code>get_reconstruction(sensor_data, X_test, noise_variance, kernel)</code>","text":"<p>Computes the GP-based data field estimates with the solution placements as the training set</p> <p>Parameters:</p> Name Type Description Default <code>sensor_data</code> <code>ndarray tuple</code> <p>((m, d), (m, 1)); Sensing locations' input                   and corresponding ground truth labels</p> required <code>X_test</code> <code>ndarray</code> <p>(n, d); Testing data input locations</p> required <code>noise_variance</code> <code>float</code> <p>data variance</p> required <code>kernel</code> <code>Kernel</code> <p>gpflow kernel function</p> required <p>Returns:</p> Name Type Description <code>y_pred</code> <code>ndarray</code> <p>(n, 1); Predicted data field estimates</p> <code>y_var</code> <code>ndarray</code> <p>(n, 1); Prediction variance at each location in the data field</p> Source code in <code>sgptools/utils/metrics.py</code> <pre><code>def get_reconstruction(sensor_data, X_test, noise_variance, kernel):\n    \"\"\"Computes the GP-based data field estimates with the solution placements as the training set\n\n    Args:\n        sensor_data (ndarray tuple): ((m, d), (m, 1)); Sensing locations' input \n                             and corresponding ground truth labels\n        X_test (ndarray): (n, d); Testing data input locations\n        noise_variance (float): data variance\n        kernel (gpflow.kernels.Kernel): gpflow kernel function\n\n    Returns:\n        y_pred (ndarray): (n, 1); Predicted data field estimates\n        y_var (ndarray): (n, 1); Prediction variance at each location in the data field\n    \"\"\"\n    Xu_X, Xu_y = sensor_data\n\n    # Get the GP predictions\n    gpr = gpflow.models.GPR((Xu_X, Xu_y),\n                            noise_variance=noise_variance,\n                            kernel=kernel)\n    y_pred, y_var = gpr.predict_f(X_test)\n    y_pred = y_pred.numpy().reshape(-1, 1)\n\n    return y_pred, y_var\n</code></pre>"},{"location":"api/utils/Metrics.html#sgptools.utils.metrics.get_rmse","title":"<code>get_rmse(y_pred, y_test)</code>","text":"<p>Computes the root-mean-square error between <code>y_pred</code> and <code>y_test</code></p> <p>Parameters:</p> Name Type Description Default <code>y_pred</code> <code>ndarray</code> <p>(n, 1); Predicted data field estimate</p> required <code>y_test</code> <code>ndarray</code> <p>(n, 1); Ground truth data field </p> required <p>Returns:</p> Name Type Description <code>rmse</code> <code>float</code> <p>Computed RMSE</p> Source code in <code>sgptools/utils/metrics.py</code> <pre><code>def get_rmse(y_pred, y_test):\n    \"\"\"Computes the root-mean-square error between `y_pred` and `y_test`\n\n    Args:\n        y_pred (ndarray): (n, 1); Predicted data field estimate\n        y_test (ndarray): (n, 1); Ground truth data field \n\n    Returns:\n        rmse (float): Computed RMSE\n    \"\"\"\n    return np.sqrt(np.mean(np.square(y_pred - y_test)))\n</code></pre>"},{"location":"api/utils/Misc.html","title":"Misc","text":""},{"location":"api/utils/Misc.html#sgptools.utils.misc","title":"<code>sgptools.utils.misc</code>","text":""},{"location":"api/utils/Misc.html#sgptools.utils.misc.cont2disc","title":"<code>cont2disc(Xu, candidates, candidate_labels=None)</code>","text":"<p>Map continuous space locations to a discrete set of candidate location</p> <p>Parameters:</p> Name Type Description Default <code>Xu</code> <code>ndarray</code> <p>(m, 2); Continuous space points</p> required <code>candidates</code> <code>ndarray</code> <p>(n, 2); Discrete set of candidate locations</p> required <code>candidate_labels</code> <code>ndarray</code> <p>(n, 1); Labels corresponding to the discrete set of candidate locations</p> <code>None</code> <p>Returns:</p> Name Type Description <code>Xu_x</code> <code>ndarray</code> <p>Discrete space points' locations </p> <code>Xu_y</code> <code>ndarray</code> <p>Labels of the discrete space points. Returned only if <code>candidate_labels</code>             was passed to the function</p> Source code in <code>sgptools/utils/misc.py</code> <pre><code>def cont2disc(Xu, candidates, candidate_labels=None):\n    \"\"\"Map continuous space locations to a discrete set of candidate location\n\n    Args:\n        Xu (ndarray): (m, 2); Continuous space points\n        candidates (ndarray): (n, 2); Discrete set of candidate locations\n        candidate_labels (ndarray): (n, 1); Labels corresponding to the discrete set of candidate locations\n\n    Returns:\n        Xu_x (ndarray): Discrete space points' locations \n        Xu_y (ndarray): Labels of the discrete space points. Returned only if `candidate_labels`\n                        was passed to the function\n\n    \"\"\"\n    # Sanity check to ensure that there are sensing locations and candidates to match\n    if len(candidates)==0 or len(Xu)==0:\n        if candidate_labels is not None:\n            return [], []\n        else:\n            return []\n\n    dists = pairwise_distances(candidates, Y=Xu, metric='euclidean')\n    row_ind, _ = linear_sum_assignment(dists)\n    Xu_X = candidates[row_ind].copy()\n    if candidate_labels is not None:\n        Xu_y = candidate_labels[row_ind].copy()\n        return Xu_X, Xu_y\n    else:\n        return Xu_X\n</code></pre>"},{"location":"api/utils/Misc.html#sgptools.utils.misc.get_inducing_pts","title":"<code>get_inducing_pts(data, num_inducing, orientation=False, random=False)</code>","text":"<p>Selects a subset of the data points to be used as inducing points.  The default approach uses kmeans to select the subset. </p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>ndarray</code> <p>(n, 2); Data points to select the inducing points from </p> required <code>num_inducing</code> <code>int</code> <p>Number of inducing points</p> required <code>orientation</code> <code>bool</code> <p>If True, add an additional dimension to model the sensor                  FoV rotation angle</p> <code>False</code> <code>random</code> <code>bool</code> <p>If True, the subset of inducing points are selected randomly             instead of using kmeans</p> <code>False</code> <p>Returns:</p> Name Type Description <code>Xu</code> <code>ndarray</code> <p>(m, d); Inducing points in the position and orientation space.             <code>m</code> is the number of inducing points,              <code>d</code> is the dimension of the space (x, y, optional - angle in radians)</p> Source code in <code>sgptools/utils/misc.py</code> <pre><code>def get_inducing_pts(data, num_inducing, orientation=False, random=False):\n    \"\"\"Selects a subset of the data points to be used as inducing points. \n    The default approach uses kmeans to select the subset. \n\n    Args:\n        data (ndarray): (n, 2); Data points to select the inducing points from \n        num_inducing (int): Number of inducing points\n        orientation (bool): If True, add an additional dimension to model the sensor \n                            FoV rotation angle\n        random (bool): If True, the subset of inducing points are selected randomly \n                       instead of using kmeans\n\n    Returns:\n        Xu (ndarray): (m, d); Inducing points in the position and orientation space.\n                        `m` is the number of inducing points, \n                        `d` is the dimension of the space (x, y, optional - angle in radians)\n    \"\"\"\n    if random:\n        idx = np.random.randint(len(data), size=num_inducing)\n        Xu = data[idx]\n    else:\n        Xu = kmeans2(data, num_inducing, minit=\"points\")[0]\n    if orientation:\n        thetas = np.random.uniform(0, 2 * np.pi, size=(Xu.shape[0], 1))\n        Xu = np.concatenate([Xu, thetas], axis=1)\n    return Xu\n</code></pre>"},{"location":"api/utils/Misc.html#sgptools.utils.misc.interpolate_path","title":"<code>interpolate_path(waypoints, sampling_rate=0.05)</code>","text":"<p>Interpolate additional points between the given waypoints to simulate continuous sensing robots</p> <p>Parameters:</p> Name Type Description Default <code>waypoints</code> <code>(n, d)</code> <p>Waypoints of the robot's path</p> required <code>sampling_rate</code> <code>float</code> <p>Distance between each pair of interpolated points</p> <code>0.05</code> <p>Returns:</p> Name Type Description <code>path</code> <code>ndarray</code> <p>(p, d) Interpolated path, <code>p</code> depends on the sampling_rate rate</p> Source code in <code>sgptools/utils/misc.py</code> <pre><code>def interpolate_path(waypoints, sampling_rate=0.05):\n    \"\"\"Interpolate additional points between the given waypoints to simulate continuous sensing robots\n\n    Args:\n        waypoints (n, d): Waypoints of the robot's path\n        sampling_rate (float): Distance between each pair of interpolated points\n\n    Returns:\n        path (ndarray): (p, d) Interpolated path, `p` depends on the sampling_rate rate\n    \"\"\"\n    interpolated_path = []\n    for i in range(2, len(waypoints)+1):\n        dist = get_distance(waypoints[i-2:i])\n        num_samples = int(dist / sampling_rate)\n        points = np.linspace(waypoints[i-1], waypoints[i-2], num_samples)\n        interpolated_path.extend(points)\n    return np.array(interpolated_path)\n</code></pre>"},{"location":"api/utils/Misc.html#sgptools.utils.misc.plot_paths","title":"<code>plot_paths(paths, candidates=None, title=None)</code>","text":"<p>Function to plot the IPP solution paths</p> <p>Parameters:</p> Name Type Description Default <code>paths</code> <code>ndarray</code> <p>(r, m, 2); <code>r</code> paths with <code>m</code> waypoints each</p> required <code>candidates</code> <code>ndarray</code> <p>(n, 2); Candidate unlabeled locations used in the SGP-based sensor placement approach</p> <code>None</code> <code>title</code> <code>str</code> <p>Title of the plot</p> <code>None</code> Source code in <code>sgptools/utils/misc.py</code> <pre><code>def plot_paths(paths, candidates=None, title=None):\n    \"\"\"Function to plot the IPP solution paths\n\n    Args:\n        paths (ndarray): (r, m, 2); `r` paths with `m` waypoints each\n        candidates (ndarray): (n, 2); Candidate unlabeled locations used in the SGP-based sensor placement approach\n        title (str): Title of the plot\n    \"\"\"\n    plt.figure()\n    for i, path in enumerate(paths):\n        plt.plot(path[:, 0], path[:, 1], \n                    c='r', label='Path', zorder=1, marker='o')\n        plt.scatter(path[0, 0], path[0, 1], \n                    c='g', label='Start', zorder=2, marker='o')\n        if candidates is not None:\n            plt.scatter(candidates[:, 0], candidates[:, 1], \n                        c='k', s=1, label='Unlabeled Train-Set Points', zorder=0)\n        if i==0:\n            plt.legend(bbox_to_anchor=(1.0, 1.02))\n    if title is not None:\n        plt.title(title)\n    plt.gca().set_aspect('equal')\n    plt.xlabel('X')\n    plt.ylabel('Y')\n</code></pre>"},{"location":"api/utils/Misc.html#sgptools.utils.misc.ploygon2candidats","title":"<code>ploygon2candidats(vertices, num_samples=5000, random_seed=2024)</code>","text":"<p>Sample unlabeled candidates within a polygon</p> <p>Parameters:</p> Name Type Description Default <code>vertices</code> <code>ndarray</code> <p>(v, 2) of vertices that define the polygon</p> required <code>num_samples</code> <code>int</code> <p>Number of samples to generate</p> <code>5000</code> <code>random_seed</code> <code>int</code> <p>Random seed for reproducibility</p> <code>2024</code> <p>Returns:</p> Name Type Description <code>candidates</code> <code>ndarray</code> <p>(n, 2); Candidate sensor placement locations</p> Source code in <code>sgptools/utils/misc.py</code> <pre><code>def ploygon2candidats(vertices, \n                      num_samples=5000, \n                      random_seed=2024):\n    \"\"\"Sample unlabeled candidates within a polygon\n\n    Args:\n        vertices (ndarray): (v, 2) of vertices that define the polygon\n        num_samples (int): Number of samples to generate\n        random_seed (int): Random seed for reproducibility\n\n    Returns:\n       candidates (ndarray): (n, 2); Candidate sensor placement locations\n    \"\"\"\n    poly = geometry.Polygon(vertices)\n    sampler = gpd.GeoSeries([poly])\n    candidates = sampler.sample_points(size=num_samples,\n                                       rng=random_seed)\n    candidates = candidates.get_coordinates().to_numpy()\n    return candidates\n</code></pre>"},{"location":"api/utils/Misc.html#sgptools.utils.misc.project_waypoints","title":"<code>project_waypoints(waypoints, candidates)</code>","text":"<p>Project the waypoints back to the candidate set while retaining the  waypoint visitation order.</p> <p>Parameters:</p> Name Type Description Default <code>waypoints</code> <code>(n, d)</code> <p>Waypoints of the robot's path</p> required <code>candidates</code> <code>ndarray</code> <p>(n, 2); Discrete set of candidate locations</p> required <p>Returns:</p> Name Type Description <code>waypoints</code> <code>(n, d)</code> <p>Projected waypoints of the robot's path</p> Source code in <code>sgptools/utils/misc.py</code> <pre><code>def project_waypoints(waypoints, candidates):\n    \"\"\"Project the waypoints back to the candidate set while retaining the \n    waypoint visitation order.\n\n    Args:\n        waypoints (n, d): Waypoints of the robot's path\n        candidates (ndarray): (n, 2); Discrete set of candidate locations\n\n    Returns:\n        waypoints (n, d): Projected waypoints of the robot's path\n    \"\"\"\n    waypoints_disc = cont2disc(waypoints, candidates)\n    waypoints_valid = _reoder_path(waypoints, waypoints_disc)\n    return waypoints_valid\n</code></pre>"},{"location":"api/utils/TSP.html","title":"TSP","text":""},{"location":"api/utils/TSP.html#sgptools.utils.tsp","title":"<code>sgptools.utils.tsp</code>","text":""},{"location":"api/utils/TSP.html#sgptools.utils.tsp.resample_path","title":"<code>resample_path(waypoints, num_inducing=10)</code>","text":"<p>Function to map path with arbitrary number of waypoints to  inducing points path with fixed number of waypoints</p> <p>Parameters:</p> Name Type Description Default <code>waypoints</code> <code>ndarray</code> <p>(num_waypoints, ndim); waypoints of path from vrp solver</p> required <code>num_inducing</code> <code>int</code> <p>Number of inducing points (waypoints) in the returned path</p> <code>10</code> <p>Returns:</p> Name Type Description <code>points</code> <code>ndarray</code> <p>(num_inducing, ndim); Resampled path</p> Source code in <code>sgptools/utils/tsp.py</code> <pre><code>def resample_path(waypoints, num_inducing=10):\n    \"\"\"Function to map path with arbitrary number of waypoints to \n    inducing points path with fixed number of waypoints\n\n    Args:\n        waypoints (ndarray): (num_waypoints, ndim); waypoints of path from vrp solver\n        num_inducing (int): Number of inducing points (waypoints) in the returned path\n\n    Returns:\n        points (ndarray): (num_inducing, ndim); Resampled path\n    \"\"\"\n    ndim = np.shape(waypoints)[-1]\n    if not (ndim==2 or ndim==3):\n        raise Exception(f\"ndim={ndim} is not supported for path resampling!\")\n    line = LineString(waypoints)\n    distances = np.linspace(0, line.length, num_inducing)\n    points = [line.interpolate(distance) for distance in distances]\n    if ndim==2:\n        points = np.array([[p.x, p.y] for p in points])\n    elif ndim==3:\n        points = np.array([[p.x, p.y, p.z] for p in points])\n    return points\n</code></pre>"},{"location":"api/utils/TSP.html#sgptools.utils.tsp.run_tsp","title":"<code>run_tsp(nodes, num_vehicles=1, max_dist=25, depth=1, resample=None, start_nodes=None, end_nodes=None, time_limit=10)</code>","text":"<p>Method to run TSP/VRP with arbitrary start and end nodes,  and without any distance constraint</p> <p>Parameters:</p> Name Type Description Default <code>nodes</code> <code>ndarray</code> <p>(# nodes, ndim); Nodes to visit </p> required <code>num_vehicles</code> <code>int</code> <p>Number of robots/vehicles</p> <code>1</code> <code>max_dist</code> <code>float</code> <p>Maximum distance allowed for each path when handling mutli-robot case</p> <code>25</code> <code>depth</code> <code>int</code> <p>Internal parameter used to track re-try recursion depth</p> <code>1</code> <code>resample</code> <code>int</code> <p>Each solution path will be resampled to have             <code>resample</code> number of points</p> <code>None</code> <code>start_nodes</code> <code>ndarray</code> <p>(# num_vehicles, ndim); Optionl array of start nodes from which                      to start each vehicle's solution path</p> <code>None</code> <code>end_nodes</code> <code>ndarray</code> <p>(# num_vehicles, ndim); Optionl array of end nodes at which                      to end each vehicle's solution path</p> <code>None</code> <code>time_limit</code> <code>int</code> <p>TSP runtime time limit in seconds</p> <code>10</code> <p>Returns:</p> Name Type Description <code>paths</code> <code>ndarray</code> <p>Solution paths</p> <code>distances</code> <code>list</code> <p>List of path lengths</p> Source code in <code>sgptools/utils/tsp.py</code> <pre><code>def run_tsp(nodes, \n            num_vehicles=1, \n            max_dist=25, \n            depth=1, \n            resample=None, \n            start_nodes=None,\n            end_nodes=None,\n            time_limit=10):\n    \"\"\"Method to run TSP/VRP with arbitrary start and end nodes, \n    and without any distance constraint\n\n    Args:\n        nodes (ndarray): (# nodes, ndim); Nodes to visit \n        num_vehicles (int): Number of robots/vehicles\n        max_dist (float): Maximum distance allowed for each path when handling mutli-robot case\n        depth (int): Internal parameter used to track re-try recursion depth\n        resample (int): Each solution path will be resampled to have\n                        `resample` number of points\n        start_nodes (ndarray): (# num_vehicles, ndim); Optionl array of start nodes from which \n                                to start each vehicle's solution path\n        end_nodes (ndarray): (# num_vehicles, ndim); Optionl array of end nodes at which \n                                to end each vehicle's solution path\n        time_limit (int): TSP runtime time limit in seconds\n\n    Returns:\n        paths (ndarray): Solution paths\n        distances (list): List of path lengths\n    \"\"\"\n    if depth &gt; 5:\n        print('Warning: Max depth reached')\n        return None, None\n\n    # Add the start and end nodes to the node list\n    if end_nodes is not None:\n        assert end_nodes.shape == (num_vehicles, nodes.shape[-1]), \\\n            \"Incorrect end_nodes shape, should be (num_vehicles, ndim)!\"\n        nodes = np.concatenate([end_nodes, nodes])\n    if start_nodes is not None:\n        assert start_nodes.shape == (num_vehicles, nodes.shape[-1]), \\\n            \"Incorrect start_nodes shape, should be (num_vehicles, ndim)!\"\n        nodes = np.concatenate([start_nodes, nodes])\n\n    # Add dummy 0 location to get arbitrary start and end node sols\n    if start_nodes is None or end_nodes is None:\n        distance_mat = np.zeros((len(nodes)+1, len(nodes)+1))\n        distance_mat[1:, 1:] = pairwise_distances(nodes, nodes)*1e4\n        trim_paths = True #shift to account for dummy node\n    else:\n        distance_mat = pairwise_distances(nodes, nodes)*1e4\n        trim_paths = False\n    distance_mat = distance_mat.astype(int)\n    max_dist = int(max_dist*1e4)\n\n    # Get start and end node indices for ortools\n    if start_nodes is None:\n        start_idx = np.zeros(num_vehicles, dtype=int)\n        num_start_nodes = 0\n    else:\n        start_idx = np.arange(num_vehicles)+int(trim_paths)\n        num_start_nodes = len(start_nodes)\n\n    if end_nodes is None:\n        end_idx = np.zeros(num_vehicles, dtype=int)\n    else:\n        end_idx = np.arange(num_vehicles)+num_start_nodes+int(trim_paths)\n\n    # used by ortools\n    def distance_callback(from_index, to_index):\n        from_node = manager.IndexToNode(from_index)\n        to_node = manager.IndexToNode(to_index)\n        return distance_mat[from_node][to_node]\n\n    # num_locations, num vehicles, start, end\n    manager = pywrapcp.RoutingIndexManager(len(distance_mat), \n                                           num_vehicles, \n                                           start_idx.tolist(),\n                                           end_idx.tolist())\n    routing = pywrapcp.RoutingModel(manager)\n    transit_callback_index = routing.RegisterTransitCallback(distance_callback)\n    routing.SetArcCostEvaluatorOfAllVehicles(transit_callback_index)\n\n    if num_vehicles &gt; 1:\n        # Dummy distaance constraint to ensure all paths have similar length\n        dimension_name = \"Distance\"\n        routing.AddDimension(\n            transit_callback_index,\n            0,  # no slack\n            max_dist,  # vehicle maximum travel distance\n            True,  # start cumul to zero\n            dimension_name,\n        )\n        distance_dimension = routing.GetDimensionOrDie(dimension_name)\n        distance_dimension.SetGlobalSpanCostCoefficient(100)\n\n    search_parameters = pywrapcp.DefaultRoutingSearchParameters()\n    search_parameters.first_solution_strategy = (\n        routing_enums_pb2.FirstSolutionStrategy.PATH_CHEAPEST_ARC\n    )\n    search_parameters.local_search_metaheuristic = (\n        routing_enums_pb2.LocalSearchMetaheuristic.GUIDED_LOCAL_SEARCH\n    )\n    search_parameters.time_limit.seconds = time_limit\n    solution = routing.SolveWithParameters(search_parameters)\n\n    paths = None\n    if solution is not None:\n        paths, distances = get_routes(manager, routing, \n                                      solution, num_vehicles, \n                                      start_idx, end_idx, trim_paths)\n        for path in paths:\n            if len(path) &lt; 2:\n                print('TSP Warning: Empty path detected')\n                return run_tsp(nodes, num_vehicles, int(np.mean(distances)*(1.5/depth)), depth+1)\n    else:\n        print('TSP Warning: No solution found')\n        return run_tsp(nodes, num_vehicles, int(max_dist*1.5), depth+1)\n\n    # Map paths from node indices to node locations\n    paths = [nodes[path] for path in paths]\n\n    # Resample each solution path to have resample number of points\n    if resample is not None:\n        paths = np.array([resample_path(path, resample) for path in paths])\n\n    # Convert distances back to floats in the original scale of the nodes\n    distances = np.array(distances)/1e4\n    return paths, distances\n</code></pre>"},{"location":"examples/AIPP_non_point_FoV.html","title":"Adaptive-IPP with Non-Point FoV","text":"In\u00a0[\u00a0]: Copied! <pre>import os\nos.environ[\"TF_ENABLE_ONEDNN_OPTS\"] = \"0\"\n\nimport numpy as np\n\nfrom sgptools.utils.metrics import *\nfrom sgptools.utils.data import *\nfrom sgptools.utils.misc import *\nfrom sgptools.models.core.osgpr import *\nfrom sgptools.utils.tsp import run_tsp\nfrom sgptools.utils.gpflow import get_model_params\nfrom sgptools.models.continuous_sgp import *\nfrom sgptools.models.core.transformations import *\nfrom gpflow.utilities.traversal import print_summary\n\nfrom matplotlib import colors\nimport matplotlib.pyplot as plt\nfrom matplotlib.patches import Polygon\nimport mpl_toolkits.mplot3d.art3d as art3d\nfrom scipy.optimize import linear_sum_assignment\nfrom sklearn.metrics import pairwise_distances\n\nfrom time import time\nfrom copy import deepcopy\n\nnp.random.seed(1234)\ntf.random.set_seed(1234)\n</pre> import os os.environ[\"TF_ENABLE_ONEDNN_OPTS\"] = \"0\"  import numpy as np  from sgptools.utils.metrics import * from sgptools.utils.data import * from sgptools.utils.misc import * from sgptools.models.core.osgpr import * from sgptools.utils.tsp import run_tsp from sgptools.utils.gpflow import get_model_params from sgptools.models.continuous_sgp import * from sgptools.models.core.transformations import * from gpflow.utilities.traversal import print_summary  from matplotlib import colors import matplotlib.pyplot as plt from matplotlib.patches import Polygon import mpl_toolkits.mplot3d.art3d as art3d from scipy.optimize import linear_sum_assignment from sklearn.metrics import pairwise_distances  from time import time from copy import deepcopy  np.random.seed(1234) tf.random.set_seed(1234) In\u00a0[2]: Copied! <pre>'''\nUtility to map the inducing points to FoV vertices\n'''\ndef get_vertices(Xu, X_fov):\n    X_fov = X_fov.reshape(len(Xu), -1, 2)\n    vertices = np.zeros((len(Xu), 5, 2))\n    vertices[:, 0] = X_fov.min(axis=1)\n    vertices[:, 1] = np.array([X_fov[:, :, 0].min(axis=1), \n                              X_fov[:, :, 1].max(axis=1)]).T\n    vertices[:, 2] = X_fov.max(axis=1)\n    vertices[:, 3] = np.array([X_fov[:, :, 0].max(axis=1), \n                              X_fov[:, :, 1].min(axis=1)]).T\n    vertices[:, 4] = X_fov.mean(axis=1)\n\n    dists = pairwise_distances(vertices[:, 4], Y=Xu[:, :2], \n                               metric='euclidean')\n    _, idx = linear_sum_assignment(dists)\n\n    vertices[:, 4] = Xu[idx][:, :2]\n    return vertices\n\n'''\nMethod to plot the solution with FoVs and environment reconstruction\n'''\ndef plot_results(X_inducing, X_fov, current_idx=0, fname=None):\n    # Setup 3D plot\n    fig = plt.figure(figsize=(5, 5))\n    ax = fig.add_subplot(111, projection='3d', computed_zorder=False)\n\n    current_idx += 1 # Increment by one for plotting\n\n    # Plot the reconstructed environment state\n    num_fov_pts = len(X_fov)//len(X_inducing)\n    sensor_data = cont2disc(X_fov[:num_fov_pts*(current_idx)], X, y)\n    y_pred, _ = get_reconstruction(sensor_data, X_test, \n                                   noise_variance_opt, kernel_opt)\n    norm = colors.Normalize(y_train.min(), y_train.max())\n    _ = ax.plot_surface(X_test[:, 0].reshape(test_dim),\n                        X_test[:, 1].reshape(test_dim),\n                        np.atleast_2d(-0.1),\n                        facecolors=plt.cm.jet(norm(y_pred.reshape(test_dim))),\n                        shade=False,\n                        alpha=0.8,\n                        zorder=0)\n\n    # Plot the solution path\n    ax.scatter(X_inducing[:current_idx, 0], \n               X_inducing[:current_idx, 1], \n               X_inducing[:current_idx, 2], c='C3')\n    ax.scatter(X_inducing[current_idx:, 0], \n               X_inducing[current_idx:, 1], \n               X_inducing[current_idx:, 2], c='C2')\n    ax.plot(X_inducing[:, 0], X_inducing[:, 1], X_inducing[:, 2], 'k-')\n\n    # Plot the FoV vertices\n    vertices = get_vertices(X_inducing, X_fov)\n    for i in range(vertices.shape[0]):\n        color = 'C3' if i &lt; current_idx else 'C2'\n        verts = []\n        verts.append([vertices[i, 0], vertices[i, 1], \n                      vertices[i, 2], vertices[i, 3]])\n        fov = Polygon(np.array(verts)[0, :, :2],\n                      linewidth=1.5, \n                      edgecolor=color,\n                      facecolor=color,\n                      fill=False,\n                      zorder=15)\n        ax.add_patch(fov)\n        art3d.pathpatch_2d_to_3d(fov)\n\n    # Configure other plot settings\n    ax.set_xlabel('X')\n    ax.set_ylabel('Y')\n    ax.set_zlabel('Z')\n\n    ax.set_xlim(np.min(X[:, 0])-0.5, np.max(X[:, 0])+0.5)\n    ax.set_ylim(np.min(X[:, 1])-0.5, np.max(X[:, 1])+0.5)\n    ax.set_zlim(0, 14)\n\n    ax.view_init(elev=30, azim=45+180)\n    ax.set_title(f'Waypoint: {current_idx}, RMSE: {get_rmse(y_pred, y_test):.2f}\\nData Field Reconstruction', y=0.99)\n\n    plt.tight_layout()\n    if fname is not None:\n        plt.savefig(fname, bbox_inches='tight')\n    plt.show()\n</pre> ''' Utility to map the inducing points to FoV vertices ''' def get_vertices(Xu, X_fov):     X_fov = X_fov.reshape(len(Xu), -1, 2)     vertices = np.zeros((len(Xu), 5, 2))     vertices[:, 0] = X_fov.min(axis=1)     vertices[:, 1] = np.array([X_fov[:, :, 0].min(axis=1),                                X_fov[:, :, 1].max(axis=1)]).T     vertices[:, 2] = X_fov.max(axis=1)     vertices[:, 3] = np.array([X_fov[:, :, 0].max(axis=1),                                X_fov[:, :, 1].min(axis=1)]).T     vertices[:, 4] = X_fov.mean(axis=1)      dists = pairwise_distances(vertices[:, 4], Y=Xu[:, :2],                                 metric='euclidean')     _, idx = linear_sum_assignment(dists)      vertices[:, 4] = Xu[idx][:, :2]     return vertices  ''' Method to plot the solution with FoVs and environment reconstruction ''' def plot_results(X_inducing, X_fov, current_idx=0, fname=None):     # Setup 3D plot     fig = plt.figure(figsize=(5, 5))     ax = fig.add_subplot(111, projection='3d', computed_zorder=False)      current_idx += 1 # Increment by one for plotting      # Plot the reconstructed environment state     num_fov_pts = len(X_fov)//len(X_inducing)     sensor_data = cont2disc(X_fov[:num_fov_pts*(current_idx)], X, y)     y_pred, _ = get_reconstruction(sensor_data, X_test,                                     noise_variance_opt, kernel_opt)     norm = colors.Normalize(y_train.min(), y_train.max())     _ = ax.plot_surface(X_test[:, 0].reshape(test_dim),                         X_test[:, 1].reshape(test_dim),                         np.atleast_2d(-0.1),                         facecolors=plt.cm.jet(norm(y_pred.reshape(test_dim))),                         shade=False,                         alpha=0.8,                         zorder=0)      # Plot the solution path     ax.scatter(X_inducing[:current_idx, 0],                 X_inducing[:current_idx, 1],                 X_inducing[:current_idx, 2], c='C3')     ax.scatter(X_inducing[current_idx:, 0],                 X_inducing[current_idx:, 1],                 X_inducing[current_idx:, 2], c='C2')     ax.plot(X_inducing[:, 0], X_inducing[:, 1], X_inducing[:, 2], 'k-')      # Plot the FoV vertices     vertices = get_vertices(X_inducing, X_fov)     for i in range(vertices.shape[0]):         color = 'C3' if i &lt; current_idx else 'C2'         verts = []         verts.append([vertices[i, 0], vertices[i, 1],                        vertices[i, 2], vertices[i, 3]])         fov = Polygon(np.array(verts)[0, :, :2],                       linewidth=1.5,                        edgecolor=color,                       facecolor=color,                       fill=False,                       zorder=15)         ax.add_patch(fov)         art3d.pathpatch_2d_to_3d(fov)      # Configure other plot settings     ax.set_xlabel('X')     ax.set_ylabel('Y')     ax.set_zlabel('Z')      ax.set_xlim(np.min(X[:, 0])-0.5, np.max(X[:, 0])+0.5)     ax.set_ylim(np.min(X[:, 1])-0.5, np.max(X[:, 1])+0.5)     ax.set_zlim(0, 14)      ax.view_init(elev=30, azim=45+180)     ax.set_title(f'Waypoint: {current_idx}, RMSE: {get_rmse(y_pred, y_test):.2f}\\nData Field Reconstruction', y=0.99)      plt.tight_layout()     if fname is not None:         plt.savefig(fname, bbox_inches='tight')     plt.show() In\u00a0[3]: Copied! <pre>'''\nMethod to run AIPP using the SGP-based approach\n'''\ndef run_aipp(X_train, ipp_model, Xu_init, path2data):\n    total_time_param = 0\n    total_time_ipp = 0\n    num_robots = Xu_init.shape[0]\n    num_waypoints = Xu_init.shape[1]\n    curr_sol = Xu_init\n    plot_results(ipp_model.transform.expand(curr_sol.reshape(-1, 3), \n                                            expand_sensor_model=False).numpy(),\n                 ipp_model.transform.expand(curr_sol.reshape(-1, 3)).numpy())\n\n    # Initialize the hyperparameters and SSGP\n    init_kernel = deepcopy(ipp_model.kernel)\n    init_noise_variance = ipp_model.likelihood.variance\n    param_model = init_osgpr(X_train, \n                             num_inducing=40, \n                             kernel=init_kernel,\n                             noise_variance=init_noise_variance)\n\n    sol_data_X = []\n    sol_data_y = []\n    for time_step in range(num_waypoints):\n        # Get a new batch of data from the last visited waypoint\n        last_visited = curr_sol[:, time_step].copy()\n        # Apply the sensor model expansion with fixed points IPP expansion\n        data_pts = ipp_model.transform.sensor_model.expand(last_visited).numpy()\n        data_X_batch, data_y_batch = path2data(data_pts)\n        sol_data_X.extend(data_X_batch)\n        sol_data_y.extend(data_y_batch)\n\n        # Skip param and path update for the last waypoint\n        if time_step == num_waypoints-1:\n            break\n\n        # Update the SSGP hyperparameter model\n        param_model.update((np.array(data_X_batch), \n                            np.array(data_y_batch)),\n                            update_inducing=False)\n        # Train only the kernel parameters and noise variance.\n        # Keeping the SSGP inducing points fixed results in better \n        # hyperparameter estimation\n        start_time = time()\n        optimize_model(param_model, \n                       trainable_variables=param_model.trainable_variables[1:], \n                       optimizer='scipy',\n                       method='CG')\n        end_time = time()\n        total_time_param += end_time - start_time\n        print_summary(param_model.kernel)\n\n        # Update the SGP-IPP model\n        Xu_visited = curr_sol.copy()[:, :time_step+1]\n        ipp_model.transform.update_Xu_fixed(Xu_visited)\n        ipp_model.update(param_model.likelihood.variance, \n                         param_model.kernel)\n        start_time = time()\n        _ = optimize_model(ipp_model,\n                           kernel_grad=False, \n                           optimizer='scipy',\n                           method='CG')\n        end_time = time()\n        total_time_ipp += end_time - start_time\n        curr_sol = ipp_model.inducing_variable.Z\n        curr_sol = ipp_model.transform.expand(curr_sol, \n                                              expand_sensor_model=False).numpy()\n        curr_sol = curr_sol.reshape(num_robots, num_waypoints, 3)\n\n        plot_results(ipp_model.transform.expand(curr_sol.reshape(-1, 3),\n                                                expand_sensor_model=False).numpy(),\n                     ipp_model.transform.expand(curr_sol.reshape(-1, 3)).numpy(),\n                     time_step+1)\n\n    return np.array(sol_data_X), np.array(sol_data_y), total_time_param, total_time_ipp\n</pre> ''' Method to run AIPP using the SGP-based approach ''' def run_aipp(X_train, ipp_model, Xu_init, path2data):     total_time_param = 0     total_time_ipp = 0     num_robots = Xu_init.shape[0]     num_waypoints = Xu_init.shape[1]     curr_sol = Xu_init     plot_results(ipp_model.transform.expand(curr_sol.reshape(-1, 3),                                              expand_sensor_model=False).numpy(),                  ipp_model.transform.expand(curr_sol.reshape(-1, 3)).numpy())      # Initialize the hyperparameters and SSGP     init_kernel = deepcopy(ipp_model.kernel)     init_noise_variance = ipp_model.likelihood.variance     param_model = init_osgpr(X_train,                               num_inducing=40,                               kernel=init_kernel,                              noise_variance=init_noise_variance)      sol_data_X = []     sol_data_y = []     for time_step in range(num_waypoints):         # Get a new batch of data from the last visited waypoint         last_visited = curr_sol[:, time_step].copy()         # Apply the sensor model expansion with fixed points IPP expansion         data_pts = ipp_model.transform.sensor_model.expand(last_visited).numpy()         data_X_batch, data_y_batch = path2data(data_pts)         sol_data_X.extend(data_X_batch)         sol_data_y.extend(data_y_batch)          # Skip param and path update for the last waypoint         if time_step == num_waypoints-1:             break          # Update the SSGP hyperparameter model         param_model.update((np.array(data_X_batch),                              np.array(data_y_batch)),                             update_inducing=False)         # Train only the kernel parameters and noise variance.         # Keeping the SSGP inducing points fixed results in better          # hyperparameter estimation         start_time = time()         optimize_model(param_model,                         trainable_variables=param_model.trainable_variables[1:],                         optimizer='scipy',                        method='CG')         end_time = time()         total_time_param += end_time - start_time         print_summary(param_model.kernel)          # Update the SGP-IPP model         Xu_visited = curr_sol.copy()[:, :time_step+1]         ipp_model.transform.update_Xu_fixed(Xu_visited)         ipp_model.update(param_model.likelihood.variance,                           param_model.kernel)         start_time = time()         _ = optimize_model(ipp_model,                            kernel_grad=False,                             optimizer='scipy',                            method='CG')         end_time = time()         total_time_ipp += end_time - start_time         curr_sol = ipp_model.inducing_variable.Z         curr_sol = ipp_model.transform.expand(curr_sol,                                                expand_sensor_model=False).numpy()         curr_sol = curr_sol.reshape(num_robots, num_waypoints, 3)          plot_results(ipp_model.transform.expand(curr_sol.reshape(-1, 3),                                                 expand_sensor_model=False).numpy(),                      ipp_model.transform.expand(curr_sol.reshape(-1, 3)).numpy(),                      time_step+1)      return np.array(sol_data_X), np.array(sol_data_y), total_time_param, total_time_ipp In\u00a0[4]: Copied! <pre># Get the synthetic elevation data\nX_train, y_train, X_test, y_test, candidates, X, y = get_dataset(random_seed=0)\ntrain_dim = np.sqrt(X_train.shape[0]).astype(int)\n\n# Train a GP and get the model parameters\nprint('Optimized Hyperparameters')\n_, noise_variance_opt, kernel_opt = get_model_params(X_train, y_train, \n                                                     lengthscales=[1.0, 1.0], \n                                                     optimizer='scipy')\n\n# Build a new test set on a grid for easy plotting in a 3D plot\ngpr = gpflow.models.GPR((X_test, y_test),\n                        noise_variance=noise_variance_opt,\n                        kernel=kernel_opt)\ntest_dim = (100, 100)\nX_test1 = np.linspace(np.min(X[:, 0]), np.max(X[:, 0]), test_dim[0])\nX_test2 = np.linspace(np.min(X[:, 1]), np.max(X[:, 1]), test_dim[1])\nX_test1, X_test2 = np.meshgrid(X_test1, X_test2)\nX_test = np.stack([X_test1.ravel(), X_test2.ravel()], axis=1)\ny_test, y_var = gpr.predict_f(X_test)\ny_test = y_test.numpy()\n\n# Plot the data field\nplt.imshow(y_test.reshape(test_dim), cmap=\"jet\", origin='lower')\nplt.xlabel('X')\nplt.ylabel('Y')\nplt.title('Elevation Data Field')\nplt.colorbar(label='Normalized Elevation')\nplt.show()\n</pre> # Get the synthetic elevation data X_train, y_train, X_test, y_test, candidates, X, y = get_dataset(random_seed=0) train_dim = np.sqrt(X_train.shape[0]).astype(int)  # Train a GP and get the model parameters print('Optimized Hyperparameters') _, noise_variance_opt, kernel_opt = get_model_params(X_train, y_train,                                                       lengthscales=[1.0, 1.0],                                                       optimizer='scipy')  # Build a new test set on a grid for easy plotting in a 3D plot gpr = gpflow.models.GPR((X_test, y_test),                         noise_variance=noise_variance_opt,                         kernel=kernel_opt) test_dim = (100, 100) X_test1 = np.linspace(np.min(X[:, 0]), np.max(X[:, 0]), test_dim[0]) X_test2 = np.linspace(np.min(X[:, 1]), np.max(X[:, 1]), test_dim[1]) X_test1, X_test2 = np.meshgrid(X_test1, X_test2) X_test = np.stack([X_test1.ravel(), X_test2.ravel()], axis=1) y_test, y_var = gpr.predict_f(X_test) y_test = y_test.numpy()  # Plot the data field plt.imshow(y_test.reshape(test_dim), cmap=\"jet\", origin='lower') plt.xlabel('X') plt.ylabel('Y') plt.title('Elevation Data Field') plt.colorbar(label='Normalized Elevation') plt.show() <pre>Optimized Hyperparameters\n\u2552\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2555\n\u2502 name                    \u2502 class     \u2502 transform        \u2502 prior   \u2502 trainable   \u2502 shape   \u2502 dtype   \u2502 value               \u2502\n\u255e\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2561\n\u2502 GPR.kernel.variance     \u2502 Parameter \u2502 Softplus         \u2502         \u2502 True        \u2502 ()      \u2502 float64 \u2502 0.8371517535182331  \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502 GPR.kernel.lengthscales \u2502 Parameter \u2502 Softplus         \u2502         \u2502 True        \u2502 (2,)    \u2502 float64 \u2502 [2.45802 2.50701]   \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502 GPR.likelihood.variance \u2502 Parameter \u2502 Softplus + Shift \u2502         \u2502 True        \u2502 ()      \u2502 float64 \u2502 0.04342255274528023 \u2502\n\u2558\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u255b\n</pre> <pre>2024-12-30 19:14:07.494792: W external/local_tsl/tsl/framework/cpu_allocator_impl.cc:83] Allocation of 200000000 exceeds 10% of free system memory.\n2024-12-30 19:14:07.523254: W external/local_tsl/tsl/framework/cpu_allocator_impl.cc:83] Allocation of 200000000 exceeds 10% of free system memory.\n2024-12-30 19:14:07.667834: W external/local_tsl/tsl/framework/cpu_allocator_impl.cc:83] Allocation of 200000000 exceeds 10% of free system memory.\n2024-12-30 19:14:07.739423: W external/local_tsl/tsl/framework/cpu_allocator_impl.cc:83] Allocation of 200000000 exceeds 10% of free system memory.\n2024-12-30 19:14:08.063816: W external/local_tsl/tsl/framework/cpu_allocator_impl.cc:83] Allocation of 200000000 exceeds 10% of free system memory.\n</pre> In\u00a0[5]: Copied! <pre>num_robots = 1\nnum_placements = 12\nnum_side = 5  # Use a 5x5 point FoV approximation\n\n# Setup the initial AIPP hyperparameters\nprint('Initial AIPP Hyperparameters')\n_, noise_variance, kernel = get_model_params(X_train, y_train, \n                                             lengthscales=[1.0, 1.0], \n                                             optimizer='scipy',\n                                             max_steps=0)\n\n# Get initial inducing points\nXu_init = get_inducing_pts(X_train, num_placements)\n# Initalize the height dimension at 2.0 for all points\nXu_init = np.concatenate((Xu_init, np.ones((Xu_init.shape[0], 1))*2.0), axis=1)\nXu_init[:, :2] = run_tsp(Xu_init[:, :2])[0][0]\n\n# Initialize the non-point FoV transform\nfov_transform = SquareHeightTransform(num_side=num_side)\ntransform = IPPTransform(num_dim=3,\n                         num_robots=1,\n                         sensor_model=fov_transform,\n                         aggregate_fov=True)\n\n# Initialize the IPP model\nsgpr, _ = continuous_sgp(num_placements,\n                         X_train, \n                         noise_variance,\n                         kernel,\n                         transform=transform,\n                         Xu_init=Xu_init,\n                         max_steps=0)\nsgp_sol_sp = sgpr.inducing_variable.Z.numpy()\n\n# Setup the data acquisition function to emulate the robot's sensor\npath2data = lambda x : cont2disc(x, X, y)\n\n# Generate the SGP-based AIPP solution\n_, _, param_time, ipp_time = run_aipp(X_train, \n                                      sgpr,\n                                      Xu_init.reshape(1, -1, 3), \n                                      path2data)\n\nprint(f'Total Hyperparameter Update Time:   {param_time:.2f}s')\nprint(f'Total IPP Update Time:              {ipp_time:.2f}s')\n\nprint(f'Average Hyperparameter Update Time: {param_time/num_placements:.2f}s')\nprint(f'Average IPP Update Time:            {ipp_time/num_placements:.2f}s')\n</pre> num_robots = 1 num_placements = 12 num_side = 5  # Use a 5x5 point FoV approximation  # Setup the initial AIPP hyperparameters print('Initial AIPP Hyperparameters') _, noise_variance, kernel = get_model_params(X_train, y_train,                                               lengthscales=[1.0, 1.0],                                               optimizer='scipy',                                              max_steps=0)  # Get initial inducing points Xu_init = get_inducing_pts(X_train, num_placements) # Initalize the height dimension at 2.0 for all points Xu_init = np.concatenate((Xu_init, np.ones((Xu_init.shape[0], 1))*2.0), axis=1) Xu_init[:, :2] = run_tsp(Xu_init[:, :2])[0][0]  # Initialize the non-point FoV transform fov_transform = SquareHeightTransform(num_side=num_side) transform = IPPTransform(num_dim=3,                          num_robots=1,                          sensor_model=fov_transform,                          aggregate_fov=True)  # Initialize the IPP model sgpr, _ = continuous_sgp(num_placements,                          X_train,                           noise_variance,                          kernel,                          transform=transform,                          Xu_init=Xu_init,                          max_steps=0) sgp_sol_sp = sgpr.inducing_variable.Z.numpy()  # Setup the data acquisition function to emulate the robot's sensor path2data = lambda x : cont2disc(x, X, y)  # Generate the SGP-based AIPP solution _, _, param_time, ipp_time = run_aipp(X_train,                                        sgpr,                                       Xu_init.reshape(1, -1, 3),                                        path2data)  print(f'Total Hyperparameter Update Time:   {param_time:.2f}s') print(f'Total IPP Update Time:              {ipp_time:.2f}s')  print(f'Average Hyperparameter Update Time: {param_time/num_placements:.2f}s') print(f'Average IPP Update Time:            {ipp_time/num_placements:.2f}s') <pre>Initial AIPP Hyperparameters\n\u2552\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2555\n\u2502 name                    \u2502 class     \u2502 transform        \u2502 prior   \u2502 trainable   \u2502 shape   \u2502 dtype   \u2502 value               \u2502\n\u255e\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2561\n\u2502 GPR.kernel.variance     \u2502 Parameter \u2502 Softplus         \u2502         \u2502 True        \u2502 ()      \u2502 float64 \u2502 1.0                 \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502 GPR.kernel.lengthscales \u2502 Parameter \u2502 Softplus         \u2502         \u2502 True        \u2502 (2,)    \u2502 float64 \u2502 [1. 1.]             \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502 GPR.likelihood.variance \u2502 Parameter \u2502 Softplus + Shift \u2502         \u2502 True        \u2502 ()      \u2502 float64 \u2502 0.09999999999999999 \u2502\n\u2558\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u255b\n</pre> <pre>\u2552\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2555\n\u2502 name                            \u2502 class     \u2502 transform   \u2502 prior   \u2502 trainable   \u2502 shape   \u2502 dtype   \u2502 value               \u2502\n\u255e\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2561\n\u2502 SquaredExponential.variance     \u2502 Parameter \u2502 Softplus    \u2502         \u2502 True        \u2502 ()      \u2502 float64 \u2502 0.02830909998623969 \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502 SquaredExponential.lengthscales \u2502 Parameter \u2502 Softplus    \u2502         \u2502 True        \u2502 (2,)    \u2502 float64 \u2502 [2.52935 4.99828]   \u2502\n\u2558\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u255b\n</pre> <pre>\u2552\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2555\n\u2502 name                            \u2502 class     \u2502 transform   \u2502 prior   \u2502 trainable   \u2502 shape   \u2502 dtype   \u2502 value               \u2502\n\u255e\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2561\n\u2502 SquaredExponential.variance     \u2502 Parameter \u2502 Softplus    \u2502         \u2502 True        \u2502 ()      \u2502 float64 \u2502 0.03608860733575918 \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502 SquaredExponential.lengthscales \u2502 Parameter \u2502 Softplus    \u2502         \u2502 True        \u2502 (2,)    \u2502 float64 \u2502 [3.66389 4.2298 ]   \u2502\n\u2558\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u255b\n</pre> <pre>\u2552\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2555\n\u2502 name                            \u2502 class     \u2502 transform   \u2502 prior   \u2502 trainable   \u2502 shape   \u2502 dtype   \u2502 value                \u2502\n\u255e\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2561\n\u2502 SquaredExponential.variance     \u2502 Parameter \u2502 Softplus    \u2502         \u2502 True        \u2502 ()      \u2502 float64 \u2502 0.047688497211455655 \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502 SquaredExponential.lengthscales \u2502 Parameter \u2502 Softplus    \u2502         \u2502 True        \u2502 (2,)    \u2502 float64 \u2502 [3.6262  4.20678]    \u2502\n\u2558\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u255b\n</pre> <pre>\u2552\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2555\n\u2502 name                            \u2502 class     \u2502 transform   \u2502 prior   \u2502 trainable   \u2502 shape   \u2502 dtype   \u2502 value                \u2502\n\u255e\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2561\n\u2502 SquaredExponential.variance     \u2502 Parameter \u2502 Softplus    \u2502         \u2502 True        \u2502 ()      \u2502 float64 \u2502 0.047733701955419004 \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502 SquaredExponential.lengthscales \u2502 Parameter \u2502 Softplus    \u2502         \u2502 True        \u2502 (2,)    \u2502 float64 \u2502 [3.09326 4.03863]    \u2502\n\u2558\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u255b\n</pre> <pre>\u2552\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2555\n\u2502 name                            \u2502 class     \u2502 transform   \u2502 prior   \u2502 trainable   \u2502 shape   \u2502 dtype   \u2502 value               \u2502\n\u255e\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2561\n\u2502 SquaredExponential.variance     \u2502 Parameter \u2502 Softplus    \u2502         \u2502 True        \u2502 ()      \u2502 float64 \u2502 0.05702257365194937 \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502 SquaredExponential.lengthscales \u2502 Parameter \u2502 Softplus    \u2502         \u2502 True        \u2502 (2,)    \u2502 float64 \u2502 [3.27988 4.50424]   \u2502\n\u2558\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u255b\n</pre> <pre>\u2552\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2555\n\u2502 name                            \u2502 class     \u2502 transform   \u2502 prior   \u2502 trainable   \u2502 shape   \u2502 dtype   \u2502 value               \u2502\n\u255e\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2561\n\u2502 SquaredExponential.variance     \u2502 Parameter \u2502 Softplus    \u2502         \u2502 True        \u2502 ()      \u2502 float64 \u2502 0.06604187384338019 \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502 SquaredExponential.lengthscales \u2502 Parameter \u2502 Softplus    \u2502         \u2502 True        \u2502 (2,)    \u2502 float64 \u2502 [3.3692  4.76543]   \u2502\n\u2558\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u255b\n</pre> <pre>\u2552\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2555\n\u2502 name                            \u2502 class     \u2502 transform   \u2502 prior   \u2502 trainable   \u2502 shape   \u2502 dtype   \u2502 value               \u2502\n\u255e\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2561\n\u2502 SquaredExponential.variance     \u2502 Parameter \u2502 Softplus    \u2502         \u2502 True        \u2502 ()      \u2502 float64 \u2502 0.09160363580094916 \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502 SquaredExponential.lengthscales \u2502 Parameter \u2502 Softplus    \u2502         \u2502 True        \u2502 (2,)    \u2502 float64 \u2502 [3.83409 5.53632]   \u2502\n\u2558\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u255b\n</pre> <pre>\u2552\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2555\n\u2502 name                            \u2502 class     \u2502 transform   \u2502 prior   \u2502 trainable   \u2502 shape   \u2502 dtype   \u2502 value               \u2502\n\u255e\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2561\n\u2502 SquaredExponential.variance     \u2502 Parameter \u2502 Softplus    \u2502         \u2502 True        \u2502 ()      \u2502 float64 \u2502 0.09052131666299722 \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502 SquaredExponential.lengthscales \u2502 Parameter \u2502 Softplus    \u2502         \u2502 True        \u2502 (2,)    \u2502 float64 \u2502 [3.84722 5.51582]   \u2502\n\u2558\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u255b\n</pre> <pre>\u2552\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2555\n\u2502 name                            \u2502 class     \u2502 transform   \u2502 prior   \u2502 trainable   \u2502 shape   \u2502 dtype   \u2502 value               \u2502\n\u255e\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2561\n\u2502 SquaredExponential.variance     \u2502 Parameter \u2502 Softplus    \u2502         \u2502 True        \u2502 ()      \u2502 float64 \u2502 0.08877117097798798 \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502 SquaredExponential.lengthscales \u2502 Parameter \u2502 Softplus    \u2502         \u2502 True        \u2502 (2,)    \u2502 float64 \u2502 [3.63615 4.87825]   \u2502\n\u2558\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u255b\n</pre> <pre>\u2552\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2555\n\u2502 name                            \u2502 class     \u2502 transform   \u2502 prior   \u2502 trainable   \u2502 shape   \u2502 dtype   \u2502 value              \u2502\n\u255e\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2561\n\u2502 SquaredExponential.variance     \u2502 Parameter \u2502 Softplus    \u2502         \u2502 True        \u2502 ()      \u2502 float64 \u2502 0.0936660767287354 \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502 SquaredExponential.lengthscales \u2502 Parameter \u2502 Softplus    \u2502         \u2502 True        \u2502 (2,)    \u2502 float64 \u2502 [3.72264 5.59398]  \u2502\n\u2558\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u255b\n</pre> <pre>\u2552\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2555\n\u2502 name                            \u2502 class     \u2502 transform   \u2502 prior   \u2502 trainable   \u2502 shape   \u2502 dtype   \u2502 value               \u2502\n\u255e\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2561\n\u2502 SquaredExponential.variance     \u2502 Parameter \u2502 Softplus    \u2502         \u2502 True        \u2502 ()      \u2502 float64 \u2502 0.10593340089380918 \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502 SquaredExponential.lengthscales \u2502 Parameter \u2502 Softplus    \u2502         \u2502 True        \u2502 (2,)    \u2502 float64 \u2502 [3.63767 6.41376]   \u2502\n\u2558\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u255b\n</pre> <pre>Total Hyperparameter Update Time:   15.67s\nTotal IPP Update Time:              22.32s\nAverage Hyperparameter Update Time: 1.31s\nAverage IPP Update Time:            1.86s\n</pre>"},{"location":"examples/AIPP_non_point_FoV.html#adaptive-ipp-with-non-point-fov","title":"Adaptive-IPP with Non-Point FoV\u00b6","text":""},{"location":"examples/AIPP_non_point_FoV.html#generate-synthetic-data-and-initial-sgp-hyperparameters","title":"Generate synthetic data and initial SGP hyperparameters\u00b6","text":""},{"location":"examples/AIPP_non_point_FoV.html#generate-the-aipp-solution","title":"Generate the AIPP solution\u00b6","text":""},{"location":"examples/Baselines.html","title":"Baselines","text":"In\u00a0[\u00a0]: Copied! <pre>import os\nos.environ[\"TF_ENABLE_ONEDNN_OPTS\"] = \"0\"\n\nimport numpy as np\nfrom time import time\nimport tensorflow as tf\n\nfrom sgptools.utils.data import *\nfrom sgptools.utils.misc import *\nfrom sgptools.utils.metrics import *\nfrom sgptools.utils.tsp import run_tsp\nfrom sgptools.utils.gpflow import get_model_params\n\nfrom sgptools.models.bo import *\nfrom sgptools.models.cma_es import *\nfrom sgptools.models.greedy_mi import *\nfrom sgptools.models.greedy_sgp import *\nfrom sgptools.models.continuous_sgp import *\nfrom sgptools.models.core.transformations import IPPTransform\n\nimport matplotlib.pyplot as plt\n\nnp.random.seed(1234)\ntf.random.set_seed(1234)\n</pre> import os os.environ[\"TF_ENABLE_ONEDNN_OPTS\"] = \"0\"  import numpy as np from time import time import tensorflow as tf  from sgptools.utils.data import * from sgptools.utils.misc import * from sgptools.utils.metrics import * from sgptools.utils.tsp import run_tsp from sgptools.utils.gpflow import get_model_params  from sgptools.models.bo import * from sgptools.models.cma_es import * from sgptools.models.greedy_mi import * from sgptools.models.greedy_sgp import * from sgptools.models.continuous_sgp import * from sgptools.models.core.transformations import IPPTransform  import matplotlib.pyplot as plt  np.random.seed(1234) tf.random.set_seed(1234) In\u00a0[2]: Copied! <pre>num_robots = 2\nnum_placements = 10\n\n# Configure IPPTransform\ntransform = IPPTransform(num_robots=num_robots)\n\n# Get the synthetic elevation data\nX_train, y_train, X_test, y_test, candidates, X, y = get_dataset()\n\n# Train the GP and get the model parameters\n_, noise_variance, kernel = get_model_params(X_train, y_train, \n                                             lengthscales=[3.0, 3.0], \n                                             optimizer='scipy',\n                                             max_steps=0)\nplt.scatter(X[:, 0], X[:, 1], c=y, cmap='jet')\nplt.title(\"Ground Truth\")\nplt.xlabel('X')\nplt.ylabel('Y')\nplt.gca().set_aspect('equal')\nplt.show()\n\n# Get the initial waypoints\nXu_init = get_inducing_pts(X_train, num_placements*num_robots)\nXu_init, _ = run_tsp(Xu_init, \n                     num_vehicles=num_robots,\n                     max_dist=100,\n                     resample=num_placements)\nXu_init = Xu_init.reshape(-1, 2)\n</pre> num_robots = 2 num_placements = 10  # Configure IPPTransform transform = IPPTransform(num_robots=num_robots)  # Get the synthetic elevation data X_train, y_train, X_test, y_test, candidates, X, y = get_dataset()  # Train the GP and get the model parameters _, noise_variance, kernel = get_model_params(X_train, y_train,                                               lengthscales=[3.0, 3.0],                                               optimizer='scipy',                                              max_steps=0) plt.scatter(X[:, 0], X[:, 1], c=y, cmap='jet') plt.title(\"Ground Truth\") plt.xlabel('X') plt.ylabel('Y') plt.gca().set_aspect('equal') plt.show()  # Get the initial waypoints Xu_init = get_inducing_pts(X_train, num_placements*num_robots) Xu_init, _ = run_tsp(Xu_init,                       num_vehicles=num_robots,                      max_dist=100,                      resample=num_placements) Xu_init = Xu_init.reshape(-1, 2) <pre>\u2552\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2555\n\u2502 name                    \u2502 class     \u2502 transform        \u2502 prior   \u2502 trainable   \u2502 shape   \u2502 dtype   \u2502 value               \u2502\n\u255e\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2561\n\u2502 GPR.kernel.variance     \u2502 Parameter \u2502 Softplus         \u2502         \u2502 True        \u2502 ()      \u2502 float64 \u2502 1.0                 \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502 GPR.kernel.lengthscales \u2502 Parameter \u2502 Softplus         \u2502         \u2502 True        \u2502 (2,)    \u2502 float64 \u2502 [3. 3.]             \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502 GPR.likelihood.variance \u2502 Parameter \u2502 Softplus + Shift \u2502         \u2502 True        \u2502 ()      \u2502 float64 \u2502 0.09999999999999999 \u2502\n\u2558\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u255b\n</pre> In\u00a0[3]: Copied! <pre># Convert a given path to the data (X, y) alongst the path\ndef path2data(path, X, y):\n    data_X = []\n    data_y = []\n    num_robots, _, _ = path.shape\n    for r in range(num_robots):\n        X_new, y_new = cont2disc(path[r], X, y)\n        data_X.extend(X_new)\n        data_y.extend(y_new)\n    return np.array(data_X), np.array(data_y)\n\n# Plot the solution paths\ndef plot_paths(paths, title=None):\n    plt.figure()\n    for i, path in enumerate(paths):\n        plt.plot(path[:, 0], path[:, 1], \n                 c='r', label='Path', \n                 zorder=1, marker='o')\n    if title is not None:\n        plt.title(title)\n    plt.xlabel('X')\n    plt.ylabel('Y')\n</pre> # Convert a given path to the data (X, y) alongst the path def path2data(path, X, y):     data_X = []     data_y = []     num_robots, _, _ = path.shape     for r in range(num_robots):         X_new, y_new = cont2disc(path[r], X, y)         data_X.extend(X_new)         data_y.extend(y_new)     return np.array(data_X), np.array(data_y)  # Plot the solution paths def plot_paths(paths, title=None):     plt.figure()     for i, path in enumerate(paths):         plt.plot(path[:, 0], path[:, 1],                   c='r', label='Path',                   zorder=1, marker='o')     if title is not None:         plt.title(title)     plt.xlabel('X')     plt.ylabel('Y') In\u00a0[4]: Copied! <pre>start = time()\nmodel, _ = continuous_sgp(num_placements, \n                          X_train, \n                          noise_variance, \n                          kernel,\n                          transform,\n                          Xu_init=Xu_init.copy())\nend = time()\nrun_time = end - start\n\n# Get solution inducing points\nsol = model.inducing_variable.Z.numpy()\nsol_paths = sol.reshape(num_robots, num_placements, 2)\n\n# Convert solution path waypoints to sensing points along the whole path\nX_path, y_path = path2data(sol_paths, X, y)\n\n# Compute RMSE\ny_pred, _ = get_reconstruction((X_path, y_path), \n                                X, \n                                noise_variance, \n                                kernel)\nrmse = get_rmse(y_pred, y)\n\n# Plot the solution\nplot_paths(sol_paths, title=f\"Continuous-SGP | RMSE: {rmse:.2f} | Time: {run_time:.2f}s\")\nplt.scatter(X[:, 0], X[:, 1], c=y_pred, zorder=0, cmap='jet')\nplt.gca().set_aspect('equal')\n</pre> start = time() model, _ = continuous_sgp(num_placements,                            X_train,                            noise_variance,                            kernel,                           transform,                           Xu_init=Xu_init.copy()) end = time() run_time = end - start  # Get solution inducing points sol = model.inducing_variable.Z.numpy() sol_paths = sol.reshape(num_robots, num_placements, 2)  # Convert solution path waypoints to sensing points along the whole path X_path, y_path = path2data(sol_paths, X, y)  # Compute RMSE y_pred, _ = get_reconstruction((X_path, y_path),                                  X,                                  noise_variance,                                  kernel) rmse = get_rmse(y_pred, y)  # Plot the solution plot_paths(sol_paths, title=f\"Continuous-SGP | RMSE: {rmse:.2f} | Time: {run_time:.2f}s\") plt.scatter(X[:, 0], X[:, 1], c=y_pred, zorder=0, cmap='jet') plt.gca().set_aspect('equal') In\u00a0[5]: Copied! <pre>start = time()\nsol = get_greedy_sgp_sol(num_placements*num_robots, \n                         candidates, \n                         candidates, \n                         noise_variance, \n                         kernel,\n                         transform=transform)\nend = time()\nrun_time = end - start\nsol_paths = sol.reshape(num_robots, num_placements, 2)\n\n# Convert solution path waypoints to sensing points along the whole path\nX_path, y_path = path2data(sol_paths, X, y)\n\n# Compute RMSE\ny_pred, _ = get_reconstruction((X_path, y_path), \n                                X, \n                                noise_variance, \n                                kernel)\nrmse = get_rmse(y_pred, y)\n\n# Plot the solution\nplot_paths(sol_paths, title=f\"Discrete-SGP | RMSE: {rmse:.2f} | Time: {run_time:.2f}s\")\nplt.scatter(X[:, 0], X[:, 1], c=y_pred, zorder=0, cmap='jet')\nplt.gca().set_aspect('equal')\n</pre> start = time() sol = get_greedy_sgp_sol(num_placements*num_robots,                           candidates,                           candidates,                           noise_variance,                           kernel,                          transform=transform) end = time() run_time = end - start sol_paths = sol.reshape(num_robots, num_placements, 2)  # Convert solution path waypoints to sensing points along the whole path X_path, y_path = path2data(sol_paths, X, y)  # Compute RMSE y_pred, _ = get_reconstruction((X_path, y_path),                                  X,                                  noise_variance,                                  kernel) rmse = get_rmse(y_pred, y)  # Plot the solution plot_paths(sol_paths, title=f\"Discrete-SGP | RMSE: {rmse:.2f} | Time: {run_time:.2f}s\") plt.scatter(X[:, 0], X[:, 1], c=y_pred, zorder=0, cmap='jet') plt.gca().set_aspect('equal') In\u00a0[6]: Copied! <pre>start = time()\nsol = get_greedy_mi_sol(num_placements*num_robots, \n                        candidates, \n                        candidates, \n                        noise_variance, \n                        kernel,\n                        transform=transform)\nend = time()\nrun_time = end - start\nsol_paths = sol.reshape(num_robots, num_placements, 2)\n\n# Convert solution path waypoints to sensing points along the whole path\nX_path, y_path = path2data(sol_paths, X, y)\n\n# Compute RMSE\ny_pred, _ = get_reconstruction((X_path, y_path), \n                                X, \n                                noise_variance, \n                                kernel)\nrmes = get_rmse(y_pred, y)\n\n# Plot the solution\nplot_paths(sol_paths, title=f\"Greedy-MI | RMSE: {rmes:.2f} | Time: {run_time:.2f}s\")\nplt.scatter(X[:, 0], X[:, 1], c=y_pred, zorder=0, cmap='jet')\nplt.gca().set_aspect('equal')\n</pre> start = time() sol = get_greedy_mi_sol(num_placements*num_robots,                          candidates,                          candidates,                          noise_variance,                          kernel,                         transform=transform) end = time() run_time = end - start sol_paths = sol.reshape(num_robots, num_placements, 2)  # Convert solution path waypoints to sensing points along the whole path X_path, y_path = path2data(sol_paths, X, y)  # Compute RMSE y_pred, _ = get_reconstruction((X_path, y_path),                                  X,                                  noise_variance,                                  kernel) rmes = get_rmse(y_pred, y)  # Plot the solution plot_paths(sol_paths, title=f\"Greedy-MI | RMSE: {rmes:.2f} | Time: {run_time:.2f}s\") plt.scatter(X[:, 0], X[:, 1], c=y_pred, zorder=0, cmap='jet') plt.gca().set_aspect('equal') In\u00a0[7]: Copied! <pre>start = time()\nmodel = CMA_ES(candidates, \n               noise_variance, \n               kernel, \n               transform=transform)\nsol = model.optimize(num_sensors=num_placements*num_robots, \n                     X_init=Xu_init.copy())\nend = time()\nrun_time = end - start\nsol_paths = sol.reshape(num_robots, num_placements, 2)\n\n# Convert solution path waypoints to sensing points along the whole path\nX_path, y_path = path2data(sol_paths, X, y)\ny_pred, _ = get_reconstruction((X_path, y_path), \n                                X, \n                                noise_variance, \n                                kernel)\n\n# Compute RMSE\nrmes = get_rmse(y_pred, y)\n\n# Plot the solution\nplot_paths(sol_paths, title=f\"CMA-ES-MI | RMSE: {rmes:.2f} | Time: {run_time:.2f}s\")\nplt.scatter(X[:, 0], X[:, 1], c=y_pred, zorder=0, cmap='jet')\nplt.gca().set_aspect('equal')\n</pre> start = time() model = CMA_ES(candidates,                 noise_variance,                 kernel,                 transform=transform) sol = model.optimize(num_sensors=num_placements*num_robots,                       X_init=Xu_init.copy()) end = time() run_time = end - start sol_paths = sol.reshape(num_robots, num_placements, 2)  # Convert solution path waypoints to sensing points along the whole path X_path, y_path = path2data(sol_paths, X, y) y_pred, _ = get_reconstruction((X_path, y_path),                                  X,                                  noise_variance,                                  kernel)  # Compute RMSE rmes = get_rmse(y_pred, y)  # Plot the solution plot_paths(sol_paths, title=f\"CMA-ES-MI | RMSE: {rmes:.2f} | Time: {run_time:.2f}s\") plt.scatter(X[:, 0], X[:, 1], c=y_pred, zorder=0, cmap='jet') plt.gca().set_aspect('equal') In\u00a0[8]: Copied! <pre>start = time()\nmodel = BayesianOpt(candidates, \n                    noise_variance,\n                    kernel,\n                    transform=transform)\nsol = model.optimize(num_sensors=num_placements*num_robots, \n                     X_init=Xu_init.copy())\nend = time()\nrun_time = end - start\nsol_paths = sol.reshape(num_robots, num_placements, 2)\n\n# Convert solution path waypoints to sensing points along the whole path\nX_path, y_path = path2data(sol_paths, X, y)\ny_pred, _ = get_reconstruction((X_path, y_path), \n                                X, \n                                noise_variance, \n                                kernel)\n\n# Compute RMSE\nrmes = get_rmse(y_pred, y)\n\n# Plot the solution\nplot_paths(sol_paths, title=f\"BO-MI | RMSE: {rmes:.2f} | Time: {run_time:.2f}s\")\nplt.scatter(X[:, 0], X[:, 1], c=y_pred, zorder=0, cmap='jet')\nplt.gca().set_aspect('equal')\n</pre> start = time() model = BayesianOpt(candidates,                      noise_variance,                     kernel,                     transform=transform) sol = model.optimize(num_sensors=num_placements*num_robots,                       X_init=Xu_init.copy()) end = time() run_time = end - start sol_paths = sol.reshape(num_robots, num_placements, 2)  # Convert solution path waypoints to sensing points along the whole path X_path, y_path = path2data(sol_paths, X, y) y_pred, _ = get_reconstruction((X_path, y_path),                                  X,                                  noise_variance,                                  kernel)  # Compute RMSE rmes = get_rmse(y_pred, y)  # Plot the solution plot_paths(sol_paths, title=f\"BO-MI | RMSE: {rmes:.2f} | Time: {run_time:.2f}s\") plt.scatter(X[:, 0], X[:, 1], c=y_pred, zorder=0, cmap='jet') plt.gca().set_aspect('equal')"},{"location":"examples/Baselines.html#baselines","title":"Baselines\u00b6","text":""},{"location":"examples/Baselines.html#set-the-initial-parameters-and-generate-synthetic-data","title":"Set the initial parameters and generate synthetic data\u00b6","text":"<p>(no distance budget constraints)</p>"},{"location":"examples/Baselines.html#helper-functions","title":"Helper functions\u00b6","text":""},{"location":"examples/Baselines.html#continuous-sgp","title":"Continuous-SGP\u00b6","text":""},{"location":"examples/Baselines.html#discrete-sgp","title":"Discrete-SGP\u00b6","text":""},{"location":"examples/Baselines.html#greedy-mutual-information","title":"Greedy Mutual Information\u00b6","text":""},{"location":"examples/Baselines.html#cma-es-mutual-information-genetic-algorithm","title":"CMA-ES Mutual Information (Genetic Algorithm)\u00b6","text":""},{"location":"examples/Baselines.html#bayesian-optimization-mutual-information","title":"Bayesian Optimization Mutual Information\u00b6","text":""},{"location":"examples/IPP.html","title":"SGP-Based IPP","text":"In\u00a0[\u00a0]: Copied! <pre>import os\nos.environ[\"TF_ENABLE_ONEDNN_OPTS\"] = \"0\"\n\nimport numpy as np\nimport tensorflow as tf\n\nfrom sgptools.utils.metrics import *\nfrom sgptools.utils.data import *\nfrom sgptools.utils.misc import *\nfrom sgptools.utils.tsp import run_tsp\nfrom sgptools.utils.gpflow import get_model_params\nfrom sgptools.models.continuous_sgp import *\nfrom sgptools.models.core.transformations import *\n\nimport matplotlib.pyplot as plt\n\nnp.random.seed(1234)\ntf.random.set_seed(1234)\n</pre> import os os.environ[\"TF_ENABLE_ONEDNN_OPTS\"] = \"0\"  import numpy as np import tensorflow as tf  from sgptools.utils.metrics import * from sgptools.utils.data import * from sgptools.utils.misc import * from sgptools.utils.tsp import run_tsp from sgptools.utils.gpflow import get_model_params from sgptools.models.continuous_sgp import * from sgptools.models.core.transformations import *  import matplotlib.pyplot as plt  np.random.seed(1234) tf.random.set_seed(1234) In\u00a0[2]: Copied! <pre># Get the synthetic elevation data\nX_train, y_train, X_test, y_test, candidates, X, y = get_dataset()\n\n# Train the GP and get the model parameters\n_, noise_variance, kernel = get_model_params(X_train, y_train, \n                                             lengthscales=[1.0, 1.0], \n                                             optimizer='scipy')\nplt.scatter(X[:, 0], X[:, 1], c=y, cmap='jet')\nplt.title(\"Ground Truth\")\nplt.xlabel('X')\nplt.ylabel('Y')\nplt.gca().set_aspect('equal')\nplt.show()\n</pre> # Get the synthetic elevation data X_train, y_train, X_test, y_test, candidates, X, y = get_dataset()  # Train the GP and get the model parameters _, noise_variance, kernel = get_model_params(X_train, y_train,                                               lengthscales=[1.0, 1.0],                                               optimizer='scipy') plt.scatter(X[:, 0], X[:, 1], c=y, cmap='jet') plt.title(\"Ground Truth\") plt.xlabel('X') plt.ylabel('Y') plt.gca().set_aspect('equal') plt.show() <pre>\u2552\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2555\n\u2502 name                    \u2502 class     \u2502 transform        \u2502 prior   \u2502 trainable   \u2502 shape   \u2502 dtype   \u2502 value               \u2502\n\u255e\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2561\n\u2502 GPR.kernel.variance     \u2502 Parameter \u2502 Softplus         \u2502         \u2502 True        \u2502 ()      \u2502 float64 \u2502 0.6576268242419765  \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502 GPR.kernel.lengthscales \u2502 Parameter \u2502 Softplus         \u2502         \u2502 True        \u2502 (2,)    \u2502 float64 \u2502 [2.95895 2.25221]   \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502 GPR.likelihood.variance \u2502 Parameter \u2502 Softplus + Shift \u2502         \u2502 True        \u2502 ()      \u2502 float64 \u2502 0.02576014673391919 \u2502\n\u2558\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u255b\n</pre> In\u00a0[3]: Copied! <pre>num_robots = 1\nnum_placements = 6\n\n# Initialize inducing points and get initial path\nXu_init = get_inducing_pts(X_train, \n                           num_placements*num_robots)\nXu_init, _ = run_tsp(Xu_init, \n                     num_vehicles=num_robots)\nXu_init = Xu_init[0]\n\n# Setup unconstrained IPP transform and optimize it\ntransform = IPPTransform(num_robots=num_robots, num_dim=2)\nsgpr, _ = continuous_sgp(num_placements, \n                         X_train, \n                         noise_variance, \n                         kernel,\n                         transform,\n                         Xu_init=Xu_init, \n                         max_steps=1000)\n\n# Get solution inducing points\nsgp_sol_sp = sgpr.inducing_variable.Z.numpy()\nsgp_paths = sgp_sol_sp.reshape(num_robots, num_placements, 2)\n\n# Plot the solution\nplot_paths(sgp_paths, candidates,\n           title=\"Offline Single-Robot IPP with Point Sensing\")\n</pre> num_robots = 1 num_placements = 6  # Initialize inducing points and get initial path Xu_init = get_inducing_pts(X_train,                             num_placements*num_robots) Xu_init, _ = run_tsp(Xu_init,                       num_vehicles=num_robots) Xu_init = Xu_init[0]  # Setup unconstrained IPP transform and optimize it transform = IPPTransform(num_robots=num_robots, num_dim=2) sgpr, _ = continuous_sgp(num_placements,                           X_train,                           noise_variance,                           kernel,                          transform,                          Xu_init=Xu_init,                           max_steps=1000)  # Get solution inducing points sgp_sol_sp = sgpr.inducing_variable.Z.numpy() sgp_paths = sgp_sol_sp.reshape(num_robots, num_placements, 2)  # Plot the solution plot_paths(sgp_paths, candidates,            title=\"Offline Single-Robot IPP with Point Sensing\") In\u00a0[4]: Copied! <pre>num_robots = 1\nnum_placements = 6\n\n# Initialize inducing points and get initial path\nXu_init = get_inducing_pts(X_train, num_placements*num_robots)\nXu_init, _ = run_tsp(Xu_init, \n                     num_vehicles=num_robots)\nXu_init = Xu_init[0]\n\n# Setup unconstrained IPP transform with sampling rate=5\n# and optimize it\ntransform = IPPTransform(num_robots=num_robots,\n                           num_dim=2,\n                           sampling_rate=5)\nsgpr, _ = continuous_sgp(num_placements, \n                         X_train, \n                         noise_variance, \n                         kernel,\n                         transform,\n                         Xu_init=Xu_init, \n                         max_steps=1000)\n\n# Get solution inducing points\nsgp_sol_sp = sgpr.inducing_variable.Z.numpy()\nsgp_paths = sgp_sol_sp.reshape(num_robots, num_placements, 2)\n\n# Plot the solution\nplot_paths(sgp_paths, candidates, \n           title=\"Offline Single-Robot IPP with Continuous Sensing\")\n</pre> num_robots = 1 num_placements = 6  # Initialize inducing points and get initial path Xu_init = get_inducing_pts(X_train, num_placements*num_robots) Xu_init, _ = run_tsp(Xu_init,                       num_vehicles=num_robots) Xu_init = Xu_init[0]  # Setup unconstrained IPP transform with sampling rate=5 # and optimize it transform = IPPTransform(num_robots=num_robots,                            num_dim=2,                            sampling_rate=5) sgpr, _ = continuous_sgp(num_placements,                           X_train,                           noise_variance,                           kernel,                          transform,                          Xu_init=Xu_init,                           max_steps=1000)  # Get solution inducing points sgp_sol_sp = sgpr.inducing_variable.Z.numpy() sgp_paths = sgp_sol_sp.reshape(num_robots, num_placements, 2)  # Plot the solution plot_paths(sgp_paths, candidates,             title=\"Offline Single-Robot IPP with Continuous Sensing\") In\u00a0[5]: Copied! <pre>num_robots = 1\nnum_placements = 4\n\n# Initialize inducing points and get initial path\nXu_init = get_inducing_pts(X_train, num_placements*num_robots)\nXu_init, _ = run_tsp(Xu_init, \n                     num_vehicles=num_robots)\nXu_init = Xu_init[0]\n\n# Add random angles to inducing points to initialize the rotation angle of the FoVs\nangles = np.random.uniform(-np.pi, np.pi, num_placements).reshape(-1, 1)\nXu_init = np.concatenate([Xu_init, angles], axis=1)\n\n# Setup unconstrained IPP transform with square FoV transform \n# with length 5 and 3x3 point approximation, \n# and continuous sensing model (sampling rate of 3), and optimize it\nsquare_transform = SquareTransform(length=5, num_side=3)\ntransform = IPPTransform(num_robots=num_robots,\n                         num_dim=3,\n                         sampling_rate=3,\n                         sensor_model=square_transform)\nsgpr, _ = continuous_sgp(num_placements, \n                         X_train, \n                         noise_variance, \n                         kernel,\n                         transform,\n                         Xu_init=Xu_init, \n                         max_steps=1000)\n\n# Get solution inducing points\nsgp_sol_sp = sgpr.inducing_variable.Z.numpy()[:, :2]\nsgp_paths = sgp_sol_sp.reshape(num_robots, -1 , 2)\n\n# Plot the solution path and the FoVs\nplot_paths(sgp_paths, candidates, \n           title=\"Offline Single-Robot IPP with Continuous Sensing\")\nsgp_sol_sp = sgpr.inducing_variable.Z\nsgp_sol_sp = sgpr.transform.expand(sgp_sol_sp,).numpy()\nplt.scatter(sgp_sol_sp[:, 0], sgp_sol_sp[:, 1], s=10, c='r')\n</pre> num_robots = 1 num_placements = 4  # Initialize inducing points and get initial path Xu_init = get_inducing_pts(X_train, num_placements*num_robots) Xu_init, _ = run_tsp(Xu_init,                       num_vehicles=num_robots) Xu_init = Xu_init[0]  # Add random angles to inducing points to initialize the rotation angle of the FoVs angles = np.random.uniform(-np.pi, np.pi, num_placements).reshape(-1, 1) Xu_init = np.concatenate([Xu_init, angles], axis=1)  # Setup unconstrained IPP transform with square FoV transform  # with length 5 and 3x3 point approximation,  # and continuous sensing model (sampling rate of 3), and optimize it square_transform = SquareTransform(length=5, num_side=3) transform = IPPTransform(num_robots=num_robots,                          num_dim=3,                          sampling_rate=3,                          sensor_model=square_transform) sgpr, _ = continuous_sgp(num_placements,                           X_train,                           noise_variance,                           kernel,                          transform,                          Xu_init=Xu_init,                           max_steps=1000)  # Get solution inducing points sgp_sol_sp = sgpr.inducing_variable.Z.numpy()[:, :2] sgp_paths = sgp_sol_sp.reshape(num_robots, -1 , 2)  # Plot the solution path and the FoVs plot_paths(sgp_paths, candidates,             title=\"Offline Single-Robot IPP with Continuous Sensing\") sgp_sol_sp = sgpr.inducing_variable.Z sgp_sol_sp = sgpr.transform.expand(sgp_sol_sp,).numpy() plt.scatter(sgp_sol_sp[:, 0], sgp_sol_sp[:, 1], s=10, c='r') Out[5]: <pre>&lt;matplotlib.collections.PathCollection at 0x7f5688999990&gt;</pre> In\u00a0[6]: Copied! <pre>num_robots = 3\nnum_placements = 6\n\n# Initialize inducing points and get initial path \n# resample to ensure same number of waypoints in each path\nXu_init = get_inducing_pts(X_train, num_placements*num_robots)\nXu_init, _ = run_tsp(Xu_init, \n                     num_vehicles=num_robots,\n                     max_dist=100,\n                     resample=num_placements)\nXu_init = Xu_init.reshape(-1, 2)\n\n# Setup distance constrained 3 robot IPP transform\n# with continuous sensing model (sampling rate of 5) and \n# a distance budget of 30.0 and optimize it\ntransform = IPPTransform(num_robots=num_robots,\n                         num_dim=2,\n                         sampling_rate=5,\n                         distance_budget=30.,\n                         constraint_weight=100.)\nsgpr, _ = continuous_sgp(num_placements, \n                         X_train, \n                         noise_variance, \n                         kernel,\n                         transform,\n                         Xu_init=Xu_init, \n                         max_steps=5000)\n\n# Get solution inducing points\nsol_sp = sgpr.inducing_variable.Z.numpy()\nsol_paths = sol_sp.reshape(num_robots, \n                           num_placements, 2)\n\n# Plot the solution path\nplot_paths(sol_paths, candidates, title=\"Optimized paths\")\nprint(\"Distances:\", transform.distance(sol_sp).numpy())\n</pre> num_robots = 3 num_placements = 6  # Initialize inducing points and get initial path  # resample to ensure same number of waypoints in each path Xu_init = get_inducing_pts(X_train, num_placements*num_robots) Xu_init, _ = run_tsp(Xu_init,                       num_vehicles=num_robots,                      max_dist=100,                      resample=num_placements) Xu_init = Xu_init.reshape(-1, 2)  # Setup distance constrained 3 robot IPP transform # with continuous sensing model (sampling rate of 5) and  # a distance budget of 30.0 and optimize it transform = IPPTransform(num_robots=num_robots,                          num_dim=2,                          sampling_rate=5,                          distance_budget=30.,                          constraint_weight=100.) sgpr, _ = continuous_sgp(num_placements,                           X_train,                           noise_variance,                           kernel,                          transform,                          Xu_init=Xu_init,                           max_steps=5000)  # Get solution inducing points sol_sp = sgpr.inducing_variable.Z.numpy() sol_paths = sol_sp.reshape(num_robots,                             num_placements, 2)  # Plot the solution path plot_paths(sol_paths, candidates, title=\"Optimized paths\") print(\"Distances:\", transform.distance(sol_sp).numpy()) <pre>Distances: [30.         29.9984054  29.98115566]\n</pre>"},{"location":"examples/IPP.html#sgp-based-ipp","title":"SGP-Based IPP\u00b6","text":""},{"location":"examples/IPP.html#get-the-data-and-initial-sgp-hyperparameters","title":"Get the data and initial SGP hyperparameters\u00b6","text":""},{"location":"examples/IPP.html#single-robot-ipp-with-point-sensing","title":"Single-Robot IPP with Point Sensing\u00b6","text":""},{"location":"examples/IPP.html#single-robot-ipp-with-continuous-sensing","title":"Single-Robot IPP with Continuous Sensing\u00b6","text":""},{"location":"examples/IPP.html#single-robot-ipp-with-continuous-non-point-fov-sensor","title":"Single-Robot IPP with Continuous Non-point FoV Sensor\u00b6","text":""},{"location":"examples/IPP.html#multi-robot-ipp-with-continuous-sensing-and-a-distance-budget","title":"Multi-Robot IPP with Continuous Sensing and a Distance Budget\u00b6","text":""},{"location":"examples/non_point_FoV.html","title":"IPP with Non-Point FoV","text":"In\u00a0[\u00a0]: Copied! <pre>import os\nos.environ[\"TF_ENABLE_ONEDNN_OPTS\"] = \"0\"\n\nimport numpy as np\n\nfrom sgptools.utils.metrics import *\nfrom sgptools.utils.data import *\nfrom sgptools.utils.misc import *\nfrom sgptools.models.core.osgpr import *\nfrom sgptools.utils.tsp import run_tsp\nfrom sgptools.utils.gpflow import get_model_params\nfrom sgptools.models.continuous_sgp import *\nfrom sgptools.models.core.transformations import *\nfrom gpflow.utilities.traversal import print_summary\n\nfrom matplotlib import colors\nimport matplotlib.pyplot as plt\nfrom matplotlib.patches import Polygon\nimport mpl_toolkits.mplot3d.art3d as art3d\nfrom scipy.optimize import linear_sum_assignment\nfrom sklearn.metrics import pairwise_distances\n\nfrom time import time\nfrom copy import deepcopy\n\nnp.random.seed(1234)\ntf.random.set_seed(1234)\n</pre> import os os.environ[\"TF_ENABLE_ONEDNN_OPTS\"] = \"0\"  import numpy as np  from sgptools.utils.metrics import * from sgptools.utils.data import * from sgptools.utils.misc import * from sgptools.models.core.osgpr import * from sgptools.utils.tsp import run_tsp from sgptools.utils.gpflow import get_model_params from sgptools.models.continuous_sgp import * from sgptools.models.core.transformations import * from gpflow.utilities.traversal import print_summary  from matplotlib import colors import matplotlib.pyplot as plt from matplotlib.patches import Polygon import mpl_toolkits.mplot3d.art3d as art3d from scipy.optimize import linear_sum_assignment from sklearn.metrics import pairwise_distances  from time import time from copy import deepcopy  np.random.seed(1234) tf.random.set_seed(1234) In\u00a0[2]: Copied! <pre>'''\nUtility to map the inducing points to FoV vertices\n'''\ndef get_vertices(Xu, X_fov):\n    X_fov = X_fov.reshape(len(Xu), -1, 2)\n    vertices = np.zeros((len(Xu), 5, 2))\n    vertices[:, 0] = X_fov.min(axis=1)\n    vertices[:, 1] = np.array([X_fov[:, :, 0].min(axis=1), \n                              X_fov[:, :, 1].max(axis=1)]).T\n    vertices[:, 2] = X_fov.max(axis=1)\n    vertices[:, 3] = np.array([X_fov[:, :, 0].max(axis=1), \n                              X_fov[:, :, 1].min(axis=1)]).T\n    vertices[:, 4] = X_fov.mean(axis=1)\n\n    dists = pairwise_distances(vertices[:, 4], Y=Xu[:, :2], \n                               metric='euclidean')\n    _, idx = linear_sum_assignment(dists)\n\n    vertices[:, 4] = Xu[idx][:, :2]\n    return vertices\n\n'''\nMethod to plot the solution with FoVs and environment reconstruction\n'''\ndef plot_results(X_inducing, X_fov, fname=None):\n    # Setup 3D plot\n    fig = plt.figure(figsize=(5, 5))\n    ax = fig.add_subplot(111, projection='3d', computed_zorder=False)\n\n    # Plot the reconstructed environment state\n    sensor_data = cont2disc(X_fov, X, y)\n    y_pred, _ = get_reconstruction(sensor_data, X_test, \n                                   noise_variance_opt, kernel_opt)\n    norm = colors.Normalize(y_train.min(), y_train.max())\n    _ = ax.plot_surface(X_test[:, 0].reshape(test_dim),\n                        X_test[:, 1].reshape(test_dim),\n                        np.atleast_2d(-0.1),\n                        facecolors=plt.cm.jet(norm(y_pred.reshape(test_dim))),\n                        shade=False,\n                        alpha=0.8,\n                        zorder=0)\n\n    # Plot the solution path\n    ax.scatter(X_inducing[:, 0], \n               X_inducing[:, 1], \n               X_inducing[:, 2], c='C3')\n    ax.plot(X_inducing[:, 0], X_inducing[:, 1], X_inducing[:, 2], 'k-')\n\n    # Plot the FoV vertices\n    vertices = get_vertices(X_inducing, X_fov)\n    for i in range(vertices.shape[0]):\n        color = 'C3'\n        verts = []\n        verts.append([vertices[i, 0], vertices[i, 1], \n                      vertices[i, 2], vertices[i, 3]])\n        fov = Polygon(np.array(verts)[0, :, :2],\n                      linewidth=1.5, \n                      edgecolor=color,\n                      facecolor=color,\n                      fill=False,\n                      zorder=15)\n        ax.add_patch(fov)\n        art3d.pathpatch_2d_to_3d(fov)\n\n    # Configure other plot settings\n    ax.set_xlabel('X')\n    ax.set_ylabel('Y')\n    ax.set_zlabel('Z')\n\n    ax.set_xlim(np.min(X[:, 0])-0.5, np.max(X[:, 0])+0.5)\n    ax.set_ylim(np.min(X[:, 1])-0.5, np.max(X[:, 1])+0.5)\n    ax.set_zlim(0, 14)\n\n    ax.view_init(elev=30, azim=45+180)\n    ax.set_title(f'Num Waypoints: {num_placements}, RMSE: {get_rmse(y_pred, y_test):.2f}\\nData Field Reconstruction', y=0.99)\n\n    plt.tight_layout()\n    if fname is not None:\n        plt.savefig(fname, bbox_inches='tight')\n    plt.show()\n</pre> ''' Utility to map the inducing points to FoV vertices ''' def get_vertices(Xu, X_fov):     X_fov = X_fov.reshape(len(Xu), -1, 2)     vertices = np.zeros((len(Xu), 5, 2))     vertices[:, 0] = X_fov.min(axis=1)     vertices[:, 1] = np.array([X_fov[:, :, 0].min(axis=1),                                X_fov[:, :, 1].max(axis=1)]).T     vertices[:, 2] = X_fov.max(axis=1)     vertices[:, 3] = np.array([X_fov[:, :, 0].max(axis=1),                                X_fov[:, :, 1].min(axis=1)]).T     vertices[:, 4] = X_fov.mean(axis=1)      dists = pairwise_distances(vertices[:, 4], Y=Xu[:, :2],                                 metric='euclidean')     _, idx = linear_sum_assignment(dists)      vertices[:, 4] = Xu[idx][:, :2]     return vertices  ''' Method to plot the solution with FoVs and environment reconstruction ''' def plot_results(X_inducing, X_fov, fname=None):     # Setup 3D plot     fig = plt.figure(figsize=(5, 5))     ax = fig.add_subplot(111, projection='3d', computed_zorder=False)      # Plot the reconstructed environment state     sensor_data = cont2disc(X_fov, X, y)     y_pred, _ = get_reconstruction(sensor_data, X_test,                                     noise_variance_opt, kernel_opt)     norm = colors.Normalize(y_train.min(), y_train.max())     _ = ax.plot_surface(X_test[:, 0].reshape(test_dim),                         X_test[:, 1].reshape(test_dim),                         np.atleast_2d(-0.1),                         facecolors=plt.cm.jet(norm(y_pred.reshape(test_dim))),                         shade=False,                         alpha=0.8,                         zorder=0)      # Plot the solution path     ax.scatter(X_inducing[:, 0],                 X_inducing[:, 1],                 X_inducing[:, 2], c='C3')     ax.plot(X_inducing[:, 0], X_inducing[:, 1], X_inducing[:, 2], 'k-')      # Plot the FoV vertices     vertices = get_vertices(X_inducing, X_fov)     for i in range(vertices.shape[0]):         color = 'C3'         verts = []         verts.append([vertices[i, 0], vertices[i, 1],                        vertices[i, 2], vertices[i, 3]])         fov = Polygon(np.array(verts)[0, :, :2],                       linewidth=1.5,                        edgecolor=color,                       facecolor=color,                       fill=False,                       zorder=15)         ax.add_patch(fov)         art3d.pathpatch_2d_to_3d(fov)      # Configure other plot settings     ax.set_xlabel('X')     ax.set_ylabel('Y')     ax.set_zlabel('Z')      ax.set_xlim(np.min(X[:, 0])-0.5, np.max(X[:, 0])+0.5)     ax.set_ylim(np.min(X[:, 1])-0.5, np.max(X[:, 1])+0.5)     ax.set_zlim(0, 14)      ax.view_init(elev=30, azim=45+180)     ax.set_title(f'Num Waypoints: {num_placements}, RMSE: {get_rmse(y_pred, y_test):.2f}\\nData Field Reconstruction', y=0.99)      plt.tight_layout()     if fname is not None:         plt.savefig(fname, bbox_inches='tight')     plt.show() In\u00a0[3]: Copied! <pre># Get the synthetic elevation data\nX_train, y_train, X_test, y_test, candidates, X, y = get_dataset(random_seed=0)\ntrain_dim = np.sqrt(X_train.shape[0]).astype(int)\n\n# Train a GP and get the model parameters\nprint('Optimized Hyperparameters')\n_, noise_variance_opt, kernel_opt = get_model_params(X_train, y_train, \n                                                     lengthscales=[1.0, 1.0], \n                                                     optimizer='scipy')\n\n# Build a new test set on a grid for easy plotting in a 3D plot\ngpr = gpflow.models.GPR((X_test, y_test),\n                        noise_variance=noise_variance_opt,\n                        kernel=kernel_opt)\ntest_dim = (100, 100)\nX_test1 = np.linspace(np.min(X[:, 0]), np.max(X[:, 0]), test_dim[0])\nX_test2 = np.linspace(np.min(X[:, 1]), np.max(X[:, 1]), test_dim[1])\nX_test1, X_test2 = np.meshgrid(X_test1, X_test2)\nX_test = np.stack([X_test1.ravel(), X_test2.ravel()], axis=1)\ny_test, y_var = gpr.predict_f(X_test)\ny_test = y_test.numpy()\n\n# Plot the data field\nplt.imshow(y_test.reshape(test_dim), cmap=\"jet\", origin='lower')\nplt.xlabel('X')\nplt.ylabel('Y')\nplt.title('Elevation Data Field')\nplt.colorbar(label='Normalized Elevation')\nplt.show()\n</pre> # Get the synthetic elevation data X_train, y_train, X_test, y_test, candidates, X, y = get_dataset(random_seed=0) train_dim = np.sqrt(X_train.shape[0]).astype(int)  # Train a GP and get the model parameters print('Optimized Hyperparameters') _, noise_variance_opt, kernel_opt = get_model_params(X_train, y_train,                                                       lengthscales=[1.0, 1.0],                                                       optimizer='scipy')  # Build a new test set on a grid for easy plotting in a 3D plot gpr = gpflow.models.GPR((X_test, y_test),                         noise_variance=noise_variance_opt,                         kernel=kernel_opt) test_dim = (100, 100) X_test1 = np.linspace(np.min(X[:, 0]), np.max(X[:, 0]), test_dim[0]) X_test2 = np.linspace(np.min(X[:, 1]), np.max(X[:, 1]), test_dim[1]) X_test1, X_test2 = np.meshgrid(X_test1, X_test2) X_test = np.stack([X_test1.ravel(), X_test2.ravel()], axis=1) y_test, y_var = gpr.predict_f(X_test) y_test = y_test.numpy()  # Plot the data field plt.imshow(y_test.reshape(test_dim), cmap=\"jet\", origin='lower') plt.xlabel('X') plt.ylabel('Y') plt.title('Elevation Data Field') plt.colorbar(label='Normalized Elevation') plt.show() <pre>Optimized Hyperparameters\n\u2552\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2555\n\u2502 name                    \u2502 class     \u2502 transform        \u2502 prior   \u2502 trainable   \u2502 shape   \u2502 dtype   \u2502 value               \u2502\n\u255e\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2561\n\u2502 GPR.kernel.variance     \u2502 Parameter \u2502 Softplus         \u2502         \u2502 True        \u2502 ()      \u2502 float64 \u2502 0.8371517535182331  \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502 GPR.kernel.lengthscales \u2502 Parameter \u2502 Softplus         \u2502         \u2502 True        \u2502 (2,)    \u2502 float64 \u2502 [2.45802 2.50701]   \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502 GPR.likelihood.variance \u2502 Parameter \u2502 Softplus + Shift \u2502         \u2502 True        \u2502 ()      \u2502 float64 \u2502 0.04342255274528023 \u2502\n\u2558\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u255b\n</pre> In\u00a0[4]: Copied! <pre>num_robots = 1\nnum_placements = 9\nnum_side = 5  # Use a 5x5 point FoV approximation\n\n# Setup the hyperparameters\n_, noise_variance, kernel = get_model_params(X_train, y_train, \n                                             lengthscales=[3.0, 3.0], \n                                             optimizer='scipy',\n                                             max_steps=0,\n                                             print_params=False)\n\n# Get initial inducing points\nXu_init = get_inducing_pts(X_train, num_placements)\n# Initalize the height dimension at 2.0 for all points\nXu_init = np.concatenate((Xu_init, np.ones((Xu_init.shape[0], 1))*2.0), axis=1)\nXu_init[:, :2] = run_tsp(Xu_init[:, :2])[0][0]\n\n# Initialize the non-point FoV IPP model and optimize it\ntransform = SquareHeightTransform(num_side=num_side)\nsgpr, _ = continuous_sgp(num_placements,\n                         X_train, \n                         noise_variance,\n                         kernel,\n                         transform=transform,\n                         Xu_init=Xu_init.copy())\nsgp_sol_sp = sgpr.inducing_variable.Z.numpy()\n</pre> num_robots = 1 num_placements = 9 num_side = 5  # Use a 5x5 point FoV approximation  # Setup the hyperparameters _, noise_variance, kernel = get_model_params(X_train, y_train,                                               lengthscales=[3.0, 3.0],                                               optimizer='scipy',                                              max_steps=0,                                              print_params=False)  # Get initial inducing points Xu_init = get_inducing_pts(X_train, num_placements) # Initalize the height dimension at 2.0 for all points Xu_init = np.concatenate((Xu_init, np.ones((Xu_init.shape[0], 1))*2.0), axis=1) Xu_init[:, :2] = run_tsp(Xu_init[:, :2])[0][0]  # Initialize the non-point FoV IPP model and optimize it transform = SquareHeightTransform(num_side=num_side) sgpr, _ = continuous_sgp(num_placements,                          X_train,                           noise_variance,                          kernel,                          transform=transform,                          Xu_init=Xu_init.copy()) sgp_sol_sp = sgpr.inducing_variable.Z.numpy() In\u00a0[5]: Copied! <pre>plot_results(Xu_init.reshape(-1, 3), \n             sgpr.transform.expand(Xu_init.reshape(-1, 3)).numpy())\n</pre> plot_results(Xu_init.reshape(-1, 3),               sgpr.transform.expand(Xu_init.reshape(-1, 3)).numpy()) In\u00a0[6]: Copied! <pre>plot_results(sgp_sol_sp.reshape(-1, 3), \n             sgpr.transform.expand(sgp_sol_sp.reshape(-1, 3)).numpy())\n</pre> plot_results(sgp_sol_sp.reshape(-1, 3),               sgpr.transform.expand(sgp_sol_sp.reshape(-1, 3)).numpy())"},{"location":"examples/non_point_FoV.html#ipp-with-non-point-fov","title":"IPP with Non-Point FoV\u00b6","text":""},{"location":"examples/non_point_FoV.html#generate-synthetic-data-and-initial-sgp-hyperparameters","title":"Generate synthetic data and initial SGP hyperparameters\u00b6","text":""},{"location":"examples/non_point_FoV.html#generate-the-ipp-solution","title":"Generate the IPP solution\u00b6","text":""},{"location":"examples/non_point_FoV.html#initial-solution","title":"Initial Solution\u00b6","text":""},{"location":"examples/non_point_FoV.html#optimized-solution","title":"Optimized Solution\u00b6","text":""},{"location":"examples/non_stationary_kernel.html","title":"Non-Stationary Kernel","text":"In\u00a0[\u00a0]: Copied! <pre>import tensorflow as tf\nimport numpy as np\nimport gpflow\ngpflow.config.set_default_float(np.float32)\n\nfrom sgptools.utils.metrics import *\nfrom sgptools.utils.misc import *\nfrom sgptools.models.continuous_sgp import *\nfrom sgptools.kernels.neural_kernel import init_neural_kernel\nfrom sgptools.utils.gpflow import get_model_params\n\nfrom sklearn.preprocessing import StandardScaler\nimport matplotlib.pyplot as plt\n\nnp.random.seed(10)\ntf.random.set_seed(10)\n</pre> import tensorflow as tf import numpy as np import gpflow gpflow.config.set_default_float(np.float32)  from sgptools.utils.metrics import * from sgptools.utils.misc import * from sgptools.models.continuous_sgp import * from sgptools.kernels.neural_kernel import init_neural_kernel from sgptools.utils.gpflow import get_model_params  from sklearn.preprocessing import StandardScaler import matplotlib.pyplot as plt  np.random.seed(10) tf.random.set_seed(10) In\u00a0[2]: Copied! <pre>def non_stationary_function(X, Y):\n    Z1 = np.sin(X/10) + np.cos(Y/10)\n    Z2 = np.cos(X/6) + np.cos(Y/6)\n    Z = np.concatenate([Z1[:, ::2], Z2[:, ::2]], \n                       axis=1)\n    return Z\n\n# Generate data\ntrain_data_dims = (40, 20)\nx = np.linspace(0, 200, train_data_dims[0])\ny = np.linspace(0, 100, train_data_dims[1])\nX, Y = np.meshgrid(x, y)\nZ = non_stationary_function(X, Y)\n\nX_train = np.stack([X.ravel(), Y.ravel()], \n                   axis=1).astype(np.float32)\nY_train = Z.ravel()[:, None].astype(np.float32)\nprint('Train Data:', X_train.shape, Y_train.shape)\n\ntest_data_dims = (100, 50)\nx = np.linspace(0, 200, test_data_dims[0])\ny = np.linspace(0, 100, test_data_dims[1])\nX, Y = np.meshgrid(x, y)\nZ = non_stationary_function(X, Y)\n\nX_test = np.stack([X.ravel(), Y.ravel()], \n                  axis=1).astype(np.float32)\nY_test = Z.ravel()[:, None].astype(np.float32)\nprint('Test Data:', X_test.shape, Y_test.shape)\n\n# Normalize the data\nXscalar = StandardScaler()\nX_train = Xscalar.fit_transform(X_train)\nX_test = Xscalar.transform(X_test)\n\nyScalar = StandardScaler()\nY_train = yScalar.fit_transform(Y_train)\nY_test = yScalar.transform(Y_test)\n\n# Plot the data\nplt.figure()\nplt.imshow(Y_train.reshape(train_data_dims[1], \n                           train_data_dims[0]), \n           cmap='jet')\nplt.gca().set_aspect('equal')\nplt.title('Train Data')\nplt.show()\n\nplt.figure()\nplt.imshow(Y_test.reshape(test_data_dims[1], \n                          test_data_dims[0]), \n           cmap='jet')\nplt.gca().set_aspect('equal')\nplt.title('Test Data')\nplt.show()\n</pre> def non_stationary_function(X, Y):     Z1 = np.sin(X/10) + np.cos(Y/10)     Z2 = np.cos(X/6) + np.cos(Y/6)     Z = np.concatenate([Z1[:, ::2], Z2[:, ::2]],                         axis=1)     return Z  # Generate data train_data_dims = (40, 20) x = np.linspace(0, 200, train_data_dims[0]) y = np.linspace(0, 100, train_data_dims[1]) X, Y = np.meshgrid(x, y) Z = non_stationary_function(X, Y)  X_train = np.stack([X.ravel(), Y.ravel()],                     axis=1).astype(np.float32) Y_train = Z.ravel()[:, None].astype(np.float32) print('Train Data:', X_train.shape, Y_train.shape)  test_data_dims = (100, 50) x = np.linspace(0, 200, test_data_dims[0]) y = np.linspace(0, 100, test_data_dims[1]) X, Y = np.meshgrid(x, y) Z = non_stationary_function(X, Y)  X_test = np.stack([X.ravel(), Y.ravel()],                    axis=1).astype(np.float32) Y_test = Z.ravel()[:, None].astype(np.float32) print('Test Data:', X_test.shape, Y_test.shape)  # Normalize the data Xscalar = StandardScaler() X_train = Xscalar.fit_transform(X_train) X_test = Xscalar.transform(X_test)  yScalar = StandardScaler() Y_train = yScalar.fit_transform(Y_train) Y_test = yScalar.transform(Y_test)  # Plot the data plt.figure() plt.imshow(Y_train.reshape(train_data_dims[1],                             train_data_dims[0]),             cmap='jet') plt.gca().set_aspect('equal') plt.title('Train Data') plt.show()  plt.figure() plt.imshow(Y_test.reshape(test_data_dims[1],                            test_data_dims[0]),             cmap='jet') plt.gca().set_aspect('equal') plt.title('Test Data') plt.show() <pre>Train Data: (800, 2) (800, 1)\nTest Data: (5000, 2) (5000, 1)\n</pre> In\u00a0[3]: Copied! <pre># Model / inference hyperparameters\nnum_inducing = 100\nlr = 1e-2\nmax_steps = 5000\nQ = 3\n\n# Initialize non-stationary kernel model\nprint('Initializing model...')\nZ = get_inducing_pts(X_train, num_inducing)\nmodel = init_neural_kernel(X_train, Y_train, \n                           n_inits=5,\n                           inducing_variable=Z,\n                           Q=Q, \n                           hidden_sizes=(4, 4))\n\n# Create optimizer\nlearning_rate = tf.keras.optimizers.schedules.ExponentialDecay(\n                      lr, \n                      decay_steps=50, \n                      decay_rate=0.98)\nloss = optimize_model(model, \n                      max_steps=max_steps,\n                      lr=learning_rate,\n                      optimizer='tf')\n\n# Plot loss\nplt.plot(loss)\nplt.xlabel('Iteration')\nplt.ylabel('Loss')\nplt.show()\n\nkernels = []\nnoice_vars = []\nlabels = ['Non Stationary Kernel', 'Stationary Kernel']\n\nkernels.append(model.kernel)\nnoice_vars.append(model.likelihood.variance.numpy())\n\n# Get stationary kernel\n_, noise_variance, kernel = get_model_params(X_train, Y_train, \n                                             lengthscales=[1.0, 0.5])\nkernels.append(kernel)\nnoice_vars.append(noise_variance)\n</pre> # Model / inference hyperparameters num_inducing = 100 lr = 1e-2 max_steps = 5000 Q = 3  # Initialize non-stationary kernel model print('Initializing model...') Z = get_inducing_pts(X_train, num_inducing) model = init_neural_kernel(X_train, Y_train,                             n_inits=5,                            inducing_variable=Z,                            Q=Q,                             hidden_sizes=(4, 4))  # Create optimizer learning_rate = tf.keras.optimizers.schedules.ExponentialDecay(                       lr,                        decay_steps=50,                        decay_rate=0.98) loss = optimize_model(model,                        max_steps=max_steps,                       lr=learning_rate,                       optimizer='tf')  # Plot loss plt.plot(loss) plt.xlabel('Iteration') plt.ylabel('Loss') plt.show()  kernels = [] noice_vars = [] labels = ['Non Stationary Kernel', 'Stationary Kernel']  kernels.append(model.kernel) noice_vars.append(model.likelihood.variance.numpy())  # Get stationary kernel _, noise_variance, kernel = get_model_params(X_train, Y_train,                                               lengthscales=[1.0, 0.5]) kernels.append(kernel) noice_vars.append(noise_variance) <pre>Initializing model...\nInitializing neural spectral kernel...\n</pre> <pre>2024-05-05 12:08:35.281864: W tensorflow/core/common_runtime/gpu/gpu_device.cc:1960] Cannot dlopen some GPU libraries. Please make sure the missing libraries mentioned above are installed properly if you would like to use GPU. Follow the guide at https://www.tensorflow.org/install/gpu for how to download and setup the required libraries for your platform.\nSkipping registering GPU devices...\n</pre> <pre>Best init: -1165.898315\n</pre> <pre>\u2552\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2555\n\u2502 name                    \u2502 class     \u2502 transform        \u2502 prior   \u2502 trainable   \u2502 shape   \u2502 dtype   \u2502 value             \u2502\n\u255e\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2561\n\u2502 GPR.kernel.variance     \u2502 Parameter \u2502 Softplus         \u2502         \u2502 True        \u2502 ()      \u2502 float32 \u2502 0.59679           \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502 GPR.kernel.lengthscales \u2502 Parameter \u2502 Softplus         \u2502         \u2502 True        \u2502 (2,)    \u2502 float32 \u2502 [1.16673 0.37082] \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502 GPR.likelihood.variance \u2502 Parameter \u2502 Softplus + Shift \u2502         \u2502 True        \u2502 ()      \u2502 float32 \u2502 0.52522           \u2502\n\u2558\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u255b\n</pre> In\u00a0[4]: Copied! <pre>for i, (kernel, noice_var) in enumerate(zip(kernels, noice_vars)):\n        print(labels[i])\n        for num_placements in [4, 9]:\n                # Get initial inducing points\n                Xu_init = get_inducing_pts(X_train, num_placements)*0.5\n                Xu_init.astype(np.float32)\n\n                # Setup SGP and optimize it\n                sgpr, _ = continuous_sgp(num_placements, \n                                         X_train,\n                                         noise_variance=noice_var,\n                                         kernel=kernel,\n                                         Xu_init=Xu_init)\n                sgp_sol_sp = sgpr.inducing_variable.Z.numpy()\n\n                # Get the GP predictions\n                Xu_X, Xu_y = cont2disc(sgp_sol_sp, candidates=X_test,\n                                       candidate_labels=Y_test)\n                gpr = gpflow.models.GPR((Xu_X, Xu_y),\n                                        noise_variance=noice_var,\n                                        kernel=kernel)\n                y_pred, y_var = gpr.predict_f(X_test)\n                y_pred = y_pred.numpy()\n\n                rmse = np.sqrt(np.mean((y_pred - Y_test)**2))\n\n                # Plot the results\n                plt.figure()\n                plt.imshow(y_pred.reshape(test_data_dims[1], test_data_dims[0]),\n                        cmap='jet', origin='lower',\n                        extent=[np.min(X_test[:, 0]), np.max(X_test[:, 0]),\n                                np.min(X_test[:, 1]), np.max(X_test[:, 1])])\n                plt.gca().set_aspect('equal')\n                plt.scatter(sgp_sol_sp[:, 0], sgp_sol_sp[:, 1], c='k', marker='p', s=100)\n                plt.title('Number of Sensors: {}    RMSE: {:.3f}'.format(num_placements, rmse))\n                plt.xlabel('X')\n                plt.ylabel('Y')\n                plt.show()\n</pre> for i, (kernel, noice_var) in enumerate(zip(kernels, noice_vars)):         print(labels[i])         for num_placements in [4, 9]:                 # Get initial inducing points                 Xu_init = get_inducing_pts(X_train, num_placements)*0.5                 Xu_init.astype(np.float32)                  # Setup SGP and optimize it                 sgpr, _ = continuous_sgp(num_placements,                                           X_train,                                          noise_variance=noice_var,                                          kernel=kernel,                                          Xu_init=Xu_init)                 sgp_sol_sp = sgpr.inducing_variable.Z.numpy()                  # Get the GP predictions                 Xu_X, Xu_y = cont2disc(sgp_sol_sp, candidates=X_test,                                        candidate_labels=Y_test)                 gpr = gpflow.models.GPR((Xu_X, Xu_y),                                         noise_variance=noice_var,                                         kernel=kernel)                 y_pred, y_var = gpr.predict_f(X_test)                 y_pred = y_pred.numpy()                  rmse = np.sqrt(np.mean((y_pred - Y_test)**2))                  # Plot the results                 plt.figure()                 plt.imshow(y_pred.reshape(test_data_dims[1], test_data_dims[0]),                         cmap='jet', origin='lower',                         extent=[np.min(X_test[:, 0]), np.max(X_test[:, 0]),                                 np.min(X_test[:, 1]), np.max(X_test[:, 1])])                 plt.gca().set_aspect('equal')                 plt.scatter(sgp_sol_sp[:, 0], sgp_sol_sp[:, 1], c='k', marker='p', s=100)                 plt.title('Number of Sensors: {}    RMSE: {:.3f}'.format(num_placements, rmse))                 plt.xlabel('X')                 plt.ylabel('Y')                 plt.show() <pre>Non Stationary Kernel\n</pre> <pre>Stationary Kernel\n</pre>"},{"location":"examples/non_stationary_kernel.html#non-stationary-kernel","title":"Non-Stationary Kernel\u00b6","text":""},{"location":"examples/non_stationary_kernel.html#generate-synthetic-non-stationary-data","title":"Generate synthetic non-stationary data\u00b6","text":""},{"location":"examples/non_stationary_kernel.html#learn-kernel-parameters-from-train-data","title":"Learn kernel parameters from train data\u00b6","text":""},{"location":"examples/non_stationary_kernel.html#get-continuous-sgp-solution-with-a-baseline-stationary-kernel-and-the-learned-neural-kernel-function","title":"Get Continuous-SGP solution with a baseline stationary kernel and the learned neural kernel function\u00b6","text":""},{"location":"examples/obstacles.html","title":"Environment with Obstacles","text":"In\u00a0[\u00a0]: Copied! <pre>import matplotlib.pyplot as plt\n\nimport numpy as np\nimport tensorflow as tf\n\nfrom sgptools.utils.gpflow import get_model_params\nfrom sgptools.utils.data import remove_polygons, remove_circle_patches\nfrom sgptools.models.continuous_sgp import *\n\nnp.random.seed(0)\ntf.random.set_seed(0)\n</pre> import matplotlib.pyplot as plt  import numpy as np import tensorflow as tf  from sgptools.utils.gpflow import get_model_params from sgptools.utils.data import remove_polygons, remove_circle_patches from sgptools.models.continuous_sgp import *  np.random.seed(0) tf.random.set_seed(0) In\u00a0[2]: Copied! <pre># Generate synthetic training data\ndata_dims = (50, 25)\nx = np.linspace(0, 200, data_dims[0])\ny = np.linspace(0, 100, data_dims[1])\nX, Y = np.meshgrid(x, y)\n\nX, Y = remove_circle_patches(X, Y, [plt.Circle((30, 60), 15)])\nX, Y = remove_polygons(X, Y, [[[80, 20], [105, 20], [105, 60], [80, 60]],])\nX, Y = remove_polygons(X, Y, [[[130, 20], [180, 20], [160, 60]],])\n\nplt.figure(figsize=(10, 5))\nplt.scatter(X, Y, s=1)\n\nax = plt.gca()\nax.add_patch(plt.Polygon([[80, 20], [105, 20], [105, 60], [80, 60]], \n             fill=None, hatch='//', color='C1', label='Obstacle'))\nax.add_patch(plt.Polygon([[130, 20], [180, 20], [160, 60]], \n             fill=None, hatch='//', color='C1'))\nax.add_patch(plt.Circle((30, 60), 15, \n             fill=None, hatch='//', color='C1'))\nax.set_aspect('equal')\n\nplt.xlabel('x', fontsize=14)\nplt.ylabel('y', fontsize=14)\nplt.xlim(0, 200)\nplt.ylim(0, 100)\n\nplt.show()\n\nX_train = np.array([X, Y]).T\ny_train = np.zeros((X_train.shape[0], 1))\nX_train = X_train.astype(float)\ny_train = y_train.astype(float)\nprint(X_train.shape, y_train.shape)\n</pre> # Generate synthetic training data data_dims = (50, 25) x = np.linspace(0, 200, data_dims[0]) y = np.linspace(0, 100, data_dims[1]) X, Y = np.meshgrid(x, y)  X, Y = remove_circle_patches(X, Y, [plt.Circle((30, 60), 15)]) X, Y = remove_polygons(X, Y, [[[80, 20], [105, 20], [105, 60], [80, 60]],]) X, Y = remove_polygons(X, Y, [[[130, 20], [180, 20], [160, 60]],])  plt.figure(figsize=(10, 5)) plt.scatter(X, Y, s=1)  ax = plt.gca() ax.add_patch(plt.Polygon([[80, 20], [105, 20], [105, 60], [80, 60]],               fill=None, hatch='//', color='C1', label='Obstacle')) ax.add_patch(plt.Polygon([[130, 20], [180, 20], [160, 60]],               fill=None, hatch='//', color='C1')) ax.add_patch(plt.Circle((30, 60), 15,               fill=None, hatch='//', color='C1')) ax.set_aspect('equal')  plt.xlabel('x', fontsize=14) plt.ylabel('y', fontsize=14) plt.xlim(0, 200) plt.ylim(0, 100)  plt.show()  X_train = np.array([X, Y]).T y_train = np.zeros((X_train.shape[0], 1)) X_train = X_train.astype(float) y_train = y_train.astype(float) print(X_train.shape, y_train.shape) <pre>(1089, 2) (1089, 1)\n</pre> In\u00a0[3]: Copied! <pre>num_inducing = 200\n\n# Get initial GP parameters\n_, noise_variance, rbf = get_model_params(np.zeros((1, 2)), np.zeros((1, 1)), \n                             lengthscales=12.3254, \n                             variance=0.561265,\n                             noise_variance=2e-6,\n                             max_steps=0)\n\n# Optimize the inducing points/sensor locations\nsgpr, _ = continuous_sgp(num_inducing,\n                         X_train,\n                         kernel=rbf,\n                         noise_variance=noise_variance,\n                         lr=1e-2,\n                         max_steps=5000)\nsgp_sol_sp = sgpr.inducing_variable.Z.numpy()\n</pre> num_inducing = 200  # Get initial GP parameters _, noise_variance, rbf = get_model_params(np.zeros((1, 2)), np.zeros((1, 1)),                               lengthscales=12.3254,                               variance=0.561265,                              noise_variance=2e-6,                              max_steps=0)  # Optimize the inducing points/sensor locations sgpr, _ = continuous_sgp(num_inducing,                          X_train,                          kernel=rbf,                          noise_variance=noise_variance,                          lr=1e-2,                          max_steps=5000) sgp_sol_sp = sgpr.inducing_variable.Z.numpy() <pre>\u2552\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2564\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2555\n\u2502 name                    \u2502 class     \u2502 transform        \u2502 prior   \u2502 trainable   \u2502 shape   \u2502 dtype   \u2502     value \u2502\n\u255e\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u256a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2561\n\u2502 GPR.kernel.variance     \u2502 Parameter \u2502 Softplus         \u2502         \u2502 True        \u2502 ()      \u2502 float64 \u2502  0.561265 \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502 GPR.kernel.lengthscales \u2502 Parameter \u2502 Softplus         \u2502         \u2502 True        \u2502 ()      \u2502 float64 \u2502 12.3254   \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502 GPR.likelihood.variance \u2502 Parameter \u2502 Softplus + Shift \u2502         \u2502 True        \u2502 ()      \u2502 float64 \u2502  2e-06    \u2502\n\u2558\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2567\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u255b\n</pre> In\u00a0[4]: Copied! <pre># Plot the sensor locations and the obstacles\nplt.figure(figsize=(10, 5))\nplt.scatter(sgp_sol_sp[:, 0], sgp_sol_sp[:, 1], \n            color='C0', \n            s=2, \n            label='Solution Placements')\n\nax = plt.gca()\nax.add_patch(plt.Polygon([[80, 20], [105, 20], [105, 60], [80, 60]], \n             fill=None, hatch='//', color='C1', label='Obstacle'))\nax.add_patch(plt.Polygon([[130, 20], [180, 20], [160, 60]], \n             fill=None, hatch='//', color='C1'))\nax.add_patch(plt.Circle((30, 60), 15, \n             fill=None, hatch='//', color='C1'))\nax.set_aspect('equal')\n\nplt.xlabel('x', fontsize=14)\nplt.ylabel('y', fontsize=14)\nplt.xlim(0, 200)\nplt.ylim(0, 100)\n\nplt.show()\n</pre> # Plot the sensor locations and the obstacles plt.figure(figsize=(10, 5)) plt.scatter(sgp_sol_sp[:, 0], sgp_sol_sp[:, 1],              color='C0',              s=2,              label='Solution Placements')  ax = plt.gca() ax.add_patch(plt.Polygon([[80, 20], [105, 20], [105, 60], [80, 60]],               fill=None, hatch='//', color='C1', label='Obstacle')) ax.add_patch(plt.Polygon([[130, 20], [180, 20], [160, 60]],               fill=None, hatch='//', color='C1')) ax.add_patch(plt.Circle((30, 60), 15,               fill=None, hatch='//', color='C1')) ax.set_aspect('equal')  plt.xlabel('x', fontsize=14) plt.ylabel('y', fontsize=14) plt.xlim(0, 200) plt.ylim(0, 100)  plt.show()"},{"location":"examples/obstacles.html#environment-with-obstacles","title":"Environment with Obstacles\u00b6","text":""},{"location":"examples/obstacles.html#note-that-all-the-solution-placements-avoid-the-obstacles","title":"Note that all the solution placements avoid the obstacles\u00b6","text":""}]}